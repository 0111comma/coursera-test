> **最新の生成範囲：まず1本だけ（2026-09-08）。** ユーザー「一つだけね、まず」。FZ-P02をFZ002として制作する。残りの2企画は、追加の指示があるまで生成しない。状況は [制作状況](docs/finance-zundamon/production-status.md) を参照。

> **2026-09-08：金融チャンネルの設計変更（現在の優先仕様）**
>
> ユーザーは、実際の現チャンネル名「ゼロからわかるお金の学校」を **「やけに金融リテラシーの高いずんだもん」** へ変更し、人物・チャンネル設計からショートのプロットを作り直すよう指定した。
> 新しい金融動画は [docs/strategy.md](docs/strategy.md)、[人物仕様](docs/brand/character-spec.md)、[新3企画](docs/finance-zundamon/pilot-plots.md) を参照する。
> 女性FPの既存台本を、音声だけ変えて新ブランドの完成版にしない。音声は **VOICEVOX:ずんだもん、ノーマル speaker_id=3**。
> 以下に残る女性FP等の旧金融仕様と矛盾する場合は、この更新を優先する。数値検証・初見での分かりやすさ・日本語の品質基準は引き継ぐ。
> 別系統の channel-zunda/・Zシリーズ、既存動画の履歴は今回の変更対象ではない。
> 設計・プロットは初稿。FZ002の生成状況は上記の制作状況を参照。YouTube上の名称変更・公開は未実施。

# 教育系YouTubeチャンネル 企画リポジトリ

お金・金融リテラシーをテーマにした教育系YouTubeチャンネルの企画を管理するリポジトリ。
チャンネル戦略は `docs/strategy.md`、テーマ選定の経緯は `docs/research/theme-candidates.md` を参照。

## いちばん大事な順序 — 欲求 → 場面 → 決定 → 数字

**どの欲求に訴求するかを設計してから、プロットを書く。**(ループ67のユーザー指示)

面白い数字を先に見つけて、それに合う視聴者を後付けすると、必ず「へえ」で終わる雑学になる。
実際に S012 がそれで2回却下された:

> 「会社が出してるから何なの?っていう。ただの雑学ですか?っていう。
>   会社が払ってくれてんだね、ありがとうで終わる動画をわざわざ見続けようと思わなくない?」

**しかも2回とも「オチを鋭くする」方向で直していた。** 出発点が数字である以上、
オチをどれだけ鋭くしても雑学のままだった。直すべきは出発点だった。

欲求の一覧・選び方・判定法は `docs/research/yokkyu-map.md`。
企画書に「この人の欲求」と「視聴後に決められること」が無いと
`production/check_yokkyu.py` が落とす。後者が「知る」「分かる」だけでも落ちる。

**落ちているかどうかの一文**: この動画を見た人は、見る前と何が変わるのか。
「◯◯を知った」しか出てこないなら雑学。

## ショート1本にメッセージは1つ(2026-09-07のユーザー指摘)

> 「19~がもうぐちゃぐちゃ。このショート動画に込められるメッセージは一つだよ?
>   なんでこんな詰め込もうとしてるの??」

前の指摘(「誰が言ったの? なぜこの本が残ってるの? 誰になぜ評価されているの?」)に、
**3つの問いへ事実の鎖で全部答えにいった**のがこの失敗の中身。
Z001 は伝記 → 書誌 → 皇帝の伝記 → 修道院 → エリス → ABC理論 と並ぶ
**学説史の年表**になり、60カット3分になった。

**3つの問いは、メッセージを1つ深くするために答えるもの。**事実を足す許可ではない。
企画書の一行目(「この人の欲求」と「視聴後に決められること」)から1本の線を引き、
**その線に乗らないカットは、面白くても落とす。**
落としたネタは次の動画1本になる(Z001 で落とした『提要』第5章の3連と ABC理論は、
それぞれ1本ぶんの中身がある)。

役目あたりの上限は **4〜5カット**。「なぜ残ったか」に12カット使うなら、
それはもう別の動画の分量。

## ユーザーの指摘は、直すだけで終わらせない(2026-09-07)

> 「と言うか言われたことをただ直してるよね? ちゃんと俺からの指摘も貯めてね。」

サブエージェントの指摘は `nihongo-stock.md` / `shiteki-stock.md` に貯めて
ゲートで守っていたのに、**ユーザー本人の指摘だけ、その場で直して終わりにしていた。**
いちばん重い指摘が記録に残らないので、次の動画で同じことをやる。

`docs/research/user-shiteki-stock.md` に、**原文のまま**貯める。
その1本の直しだけでなく、**抽象化したルール**を必ず書く(ルール欄が空なら未完成)。
`production/check_user.py` が `check_all` の**先頭**で、未対応の行が1つでも
あれば落とす。

## ショートは、知らない人のフィードに突然現れる(2026-09-08)

> 「なんか毎度言ってる気がするんだけど、ショート動画は自分以外が作成したコンテンツが
>   溢れかえってるフィードの中急に現れるから過去の文脈とか過去に話したことが
>   相手に伝わるとか思わないで。」
> 「ネロの話前したみたいなノリで話してるのはなぜ?」

**毎度言われている**。視聴者はこのチャンネルを見たことがなく、前の動画も、
いま出した人名も知らない。それなのに Z002 は

    #14 セネカも、皇帝ネロの家庭教師だった人。
    #15 ネロが即位してからは、その右腕。

と、ネロの生涯を知っている前提で話していた(しかも皇帝と呼んだ人の即位を後から説明する)。

`production/check_koyuu.py` を `check_all` の上から4番目に置いた。判定は2つ:

1. **固有名詞は初出のその場で「何者か」を言う。**次のカットでは遅い
   (1カットは3秒で、知らない名前が出た時点で視聴者は考えるのをやめる)
2. **説明していない前後関係(即位・引退・改宗…)を後から持ち出さない。**
   人物の変化は、その人を出すカットで1回で言い切る

**なぜ既存のゲートが止めなかったか**: check_yougo は「同じ語を3回以上出して意味を
一度も言っていない」ときだけ落ちる。ネロは2回、モンテーニュは2回なので素通りした。
**回数ではなく、初出かどうかで見る。**

## いちばん落としてはいけないゲート — 説明していない数字を出さない

**(2026-08-23のユーザー指示)**

> 「65歳から始めて? なんで? なんで95歳までを想定してるのかな?
>   ちゃんと論理的に説明しよう!
>   誰がどうみても何を言っているのか、何を伝えたいのかわかる文章を書こう!
>   **それがまず直す第一優先で絶対今後落としてほしくないゲートです!**」

S032 は 65歳・95歳・3%・5% を**理由なしで**画面に出し、それでも14本のゲートを
全部通った。どのゲートも「文の作り」「語の省略」「重なり」しか見ておらず、
**その数字がどこから来たのか**を一度も見ていなかったため。

`production/check_zentei.py` を作り、`check_all` の**先頭**に置いた。判定は4つ:

1. plan.md に「## 前提と根拠」の表があること
2. 表の各行に**根拠**と**出典(URLと確認日)**があること(仮定なら「仮定」と書く)
3. 表の値が、**ナレーションの中でも根拠とともに語られている**こと
   (企画書に書いてあっても、動画で言わなければ視聴者には届かない)
4. ナレーションの数値が、表にも `verify.py` の出力にも無いなら落とす

つまり動画に出る数は、**導出値**(verify.py が計算した)か
**前提値**(出典つきで plan.md に書いた)のどちらかでなければならない。

**仮定の数字を足すときは、その仮定の根拠がまた必要になる。**
S032 が「利回り3%なら4万7000円」を捨てたのはこのため。



## 台本の審査は「前情報ゼロの読者」から始める(2026-09-04。何回も受けた指摘)

ユーザー: 「なんの前情報もない人がこの動画見ても何を言ってるのか理解できる状況を作るんだよ? 何回言わせんの?」

- **1周目は必ず、plan.md を渡さない読者に字幕だけを順番に読ませる**(戻らない・1回だけ)。
  カットごとに「いま何の話だと思うか」を書かせ、最後に「誰が・何をされて・どうする話か」を言い直させる。
  それが企画書の場面と一致しないカットが、直す場所
- 面白さ・日本語・翻訳調の審査は**そのあと**。文脈を知っている審査員は、文脈なしで分かるかを判定できない
  (Z001 で12人回して誰も1コマ目の欠陥に気づかなかった)
- **1コマ目には「誰が・何をされて・どこで」を全部入れる**(check_toi が Z 番台に人物語を要求する)
- **読者には全カットで「自分なら絶対に言わない文」を挙げさせる**(2026-09-04 ユーザー「そのチェック全てで行え」)。
  理解できたかだけ聞くと、「言い方を直す」のような**分かるが人は言わない文**が通る。
  これは `.claude/workflows/coldread.js` に固定した: `Workflow({name:'coldread', args:{video:'videos/<ID>-<slug>'}})`。
  3人以上(4人中の過半数)が「言わない」とした文は、読者の言い方に置き換える。
  2人止まりは好みの差(前の周で読者自身が書いた文に次の周で2票つくことがあり、収束しない。2026-09-04 冷読6〜8周目)

## 二度と同じ指摘を受けないためのゲート(2026-09-04)

ユーザーの問い: 「これ、もう二度と同じ指摘こないように管理できてる?」
——**指摘をログに貯めてルール文書にしただけでは、次の動画で誰も止めない。**
ルールは機械ゲートにして `check_all` に入れる。サブエージェントは**新しい種類の指摘を出すためだけ**に使い、
一度出た指摘はゲートが止める(トークン代を毎回払わない)。

| 指摘(原文は shiteki-log.md) | ルール文書 | 機械ゲート |
|---|---|---|
| 「英語の翻訳すぎるので日本語が不自然」(2026-09-03) | `docs/research/ai-nihongo-rules.md` T1〜T10・§3 | `production/check_honyaku.py`(T4/T6/T8/T9/T10/§3/T5。文脈の要る T1・T3・T7 は台本を書く工程で同文書を読んで避ける) |
| 「プロットが面白くない」(2026-09-03) | shiteki-log「誰でも知っていることを丁寧に説明する台本は退屈」 | `check_yokkyu.py`: Z 番台は plan.md に **「この動画の「知らなかった」」** の1行が無いと落ちる |
| 「学びがほぼない。これ誰が言ったの? なぜこの本が残ってるの? 誰になぜ評価されているの?」(2026-09-05) | shiteki-log 同日 | `check_manabi.py`: Z 番台は plan.md に「学び」の表(誰が言った・なぜ残った・誰がどう評価した+出典)が無いと落ちる。ナレーションに人名が無くても落ちる。**尺は 3分まで**(check_video)。学びを削って60秒に収めない |
| 「英語を直訳したものに見える。専門用語は日本語で使われている表現を」(2026-09-05) | `production/teiyaku.txt`(定訳の一覧) | `check_teiyaku.py`: 定訳を言わずに言い換えだけで済ませたら落ちる(権内・判断・論理療法・ABC理論…)。同じ接続語で始まる文が全体の22%超・「〜じゃなく〜」が4回以上・「〜なの。」が34%超も落とす。**サブエージェント側は `/nihongo`**(`.claude/workflows/nihongo.js`。定訳・翻訳調・耳・話し言葉・校閲の5人が100点満点で採点し、**平均80点未満、または定訳の欠落が1件でもあれば不合格**。指摘は `docs/research/nihongo-stock.md` に貯まる) |
| 「参考値と呼んでも判定式に入っていれば依存」(2026-09-02) | channel-zunda/strategy.md §9 | (企画書の判定式に他チャンネルの数字を入れない。目で見る) |
| ゲートを通った台本が焼いている途中で落ちた(2026-09-07) | shiteki-log 同日 | `check_video` の字幕行数を、**レンダラと同じ `fplib._fit_rows`** で数えるようにした(`_fp_rows_measured`)。**尺・行数・音量のように本番側にも判定があるものは、ゲートで別の式を書かず本番の関数を呼ぶ** |
| 「直したはずの文が、尺詰めで元に戻っている」(2026-09-07 日本語パネル4周目) | shiteki-log 同日 | `production/check_modoshi.py`(`check_all` の2番目)。ストックで「対応済み」の行の**直す前の文**が字幕に残っていたら落とす。**尺を削るときは、削る文が過去の指摘の直しでないかを先に見る** |
| ゲートが**ユニット0本**として黙って合格した(2026-09-07 Z002) | shiteki-log 同日 | `check_tempo` は `Unit("場面", "字幕"` の**字面を正規表現で**拾っていた。Z002 が `def U(scene, sub, **kw)` の助け関数で Unit を作った瞬間、1件も拾えず、**40カット4.3秒/カットが素通りした**。render.py を読み込んで `UNITS` を取るようにした。**台本の書き方を変えるとゲートが効かなくなるのは、ゲートの作りのほうが悪い。**同じ形の正規表現は check_video / check_yougo / check_zentei / check_modoshi にも残っている |
| 字数→秒の係数がチャンネル違いだった(2026-09-07) | shiteki-log 同日 | `CHARS_PER_SEC = 6.05` はお金のチャンネル29本の実測で、Z 番台では尺を1.4倍に見積もっていた(Z001 の実測は 972字/112.2秒 = **8.66字/秒**)。長く見積もるゲートは「足りているのに足せ」と言う。`CHARS_PER_SEC_Z = 8.4` を足した。**推定で判定する数は、そのチャンネルの実測で置き直す** |
| 免除は次の動画で誰も止めない(2026-09-07) | — | `gate_exempt` の Z001:toi:1 / Z001:teinei:1 を消し、`check_teinei` に **EVERYDAY_Z**(心理学チャンネルの日常語)を足した。EVERYDAY はお金のチャンネル用の一覧で、1文目に「給料」を要求すると場面が壊れる。**チャンネルが増えたら、免除ではなく判定を分ける** |
| パネルが「どのゲートがなぜ止めなかったか」を報告(2026-09-07 5周目) | shiteki-log 同日 | 穴を3つ塞いだ: `check_honyaku` T4 に否定形「ことはでき」/ `check_teiyaku` に T2b(**接続語で始まるカットの総数**の上限25%。語別の22%だけだと32%が素通りした)/ `check_modoshi` に**言い換えた巻き戻しの疑い**(文字bigram 0.62以上。誤検出するので落とさず疑いだけ出す)。**ゲートを通すいちばん安い操作は毎回ゲートの穴を突く形になるので、パネルには「どのゲートがなぜ止めなかったか」も書かせる** |

**台本を書く工程(/write-script)では、書く前に `docs/research/ai-nihongo-rules.md` を読む。**
ゲートは書いたあとに落とすもので、書く前に避けるほうが安い。
「ゲートを満たすいちばん安い形が次の欠陥を作る」(check_flow を「その〜」で満たして check_bunsho が落ちた)
は既知の型なので、**接続語や指示語を貼る前に、名詞をもう一度言えないかを先に試す**。

## 焼きは、見張りを付けてから始める(2026-09-08)

> 「止まらないようにするにはどうするべき?
>   なぜ止まっても感知できずに私から今何してるのと聞かれて動き出すの?」

焼きは1本1時間かかる。それを `nohup python3 render.py &` で投げてターンを終えていたので、
**次に動くのはユーザーに聞かれたとき**だった。しかも render.py は正常時に何も出さないので、
ログが0バイトでも「順調」と「VOICEVOX が落ちて静かに止まった」の区別がつかなかった。

1. **shortlib が進捗を1行ずつ出す**(`[tts] i/n` / `[draw] i/n`)。無音は死んだ証拠になる
2. **`production/bake.sh` で焼く。**VOICEVOX を起こしてから始め、進捗行が420秒増えなければ
   固まったと見なして殺し、**描いたフレームを残したまま再開**する(render.py の `[resume]`)。
   3回とも駄目なら非0で終わる
3. **終了を待つ仕掛けを必ず付ける。**`Bash(run_in_background: true)` の待ちループか Monitor。
   待たずにターンを終えたら、それは投げっぱなしと同じ

**手順書に書くだけでは、次に急いだときの自分が守らない。**だから機械で塞いである:

- `shortlib.render_video()` の先頭で `require_supervision()` が走る。bake.sh が立てる
  `BAKE_SUPERVISED=1` が無ければ **焼き始める前に止まる**。
  承知のうえで前景で回すときだけ `BAKE_UNSUPERVISED=1` を付ける
- **現況は記憶で答えない。**`bash production/bake_status.sh [videos/<ID>-<slug>]` が
  pgrep と最新フレームの時刻から「焼いている/固まっている疑い/落ちている/完了」を言う。
  **ユーザーに状況を報告する前に必ず走らせる**(mp4 があるだけで「完了」と言わない。
  古い mp4 を残したまま焼き直していることがある)

## ワークフロー(企画 → 動画まで)

```
/ideate        **先に欲求を選ぶ**(yokkyu-map の A〜D)。そのうえでネタを出し、
   ↓           ideas/backlog.md に追加する
/plan-video    ネタ1つを選び、videos/<ID>-<slug>/plan.md(企画書)を作る
   ↓           **一行目は「この人の欲求」と「視聴後に決められること」**。
   ↓           ここが動作で書けないなら、その企画は捨てる
   ↓
/write-script  企画書から videos/<ID>-<slug>/script.md(台本)を作る。
   ↓           **書く工程は docs/research/sakubun-gensoku.md に従う**(鎖→書き下ろし→
   ↓           縦読み→検収。ゲート不合格は段落ごと書き直す。1行パッチ禁止)
   ↓           計算を含む場合は verify.py で数値を検証してから台本に反映
/nihongo       **焼く前に必ず**日本語パネルを回す(2026-09-05 ユーザー「英語を直訳したものに
   ↓           見えるので、しっかり専門用語は日本語で使われている表現をしましょう」)。
   ↓           `Workflow({scriptPath:'.claude/workflows/nihongo.js', args:{video, date, pass:80}})`
   ↓           5人(定訳・翻訳調・耳・話し言葉・校閲)が100点満点で採点し、
   ↓           **平均80点未満か、定訳の欠落が1件でもあれば不合格**。
   ↓           指摘は `docs/research/nihongo-stock.md` に貯まる。定訳の一覧は `production/teiyaku.txt`
/critique      **焼く前に必ず**批評パネルを回す(2026-09-05 ユーザー「勝手に焼いてるけど…
   ↓           サブエージェント展開して指摘をどんどんストックする仕組みは? やれよ」)。
   ↓           `Workflow({scriptPath:'.claude/workflows/critique.js', args:{video, feedback, date}})`
   ↓           5人(指摘クリア判定・学びの深さ・事実検証・維持率・視聴者)が指摘を出し、
   ↓           `docs/research/shiteki-stock.md` に自動で貯まる。**high が 0 になるまで焼かない。**
   ↓           対応した指摘は状態を「対応済み」に書き換え、2本以上で出た指摘はゲートへ
/produce-video 台本から videos/<ID>-<slug>/output/<ID>.mp4 をレンダリングする。
   ↓           **必ず `bash production/bake.sh videos/<ID>-<slug> <ログ>` で焼き、
   ↓           Bash(run_in_background) か Monitor で終了を待つ**(2026-09-08 U19)。
   ↓           `nohup render.py &` で投げっぱなしにしない。止まっても誰も気づかない
   ↓           (production/shortlib.py + VOICEVOX + ffmpeg。詳細はスキル参照)
(投稿はユーザーのローカル環境から。このリポジトリの成果物はmp4まで)
```

## ディレクトリ構成

- `docs/` — 戦略・調査ドキュメント
- `ideas/backlog.md` — ネタのストック(ステータス管理付き)
- `templates/` — 企画書・台本のテンプレート
- `production/` — 制作パイプライン
  - `shortlib.py` — 描画/TTS/結合。`use_landscape()` で横型1920×1080に切り替わる
  - `scenes_common.py` — 縦型ショート用のシーン部品
  - `scenes_long.py` — 横型長尺用のシーン部品(棒6本・左右比較・曲線・年またぎ・表)
  - `scenes_zunda.py` — **ずんだもんチャンネル(Z 番台)用の場面の絵**(電車・上司・日めくり・鍵・メモ・本・1900年前の人)。
    scenes_fp の数の図(compare/formula/arrow/hero)は**数があるカット以外で使わない**
    (2026-09-04 ユーザー「もっとイラストつかって。よくわからん画面に出てる図」)
  - `preview_long.py` — 台本を書く前に横型のレイアウトを目で見るための下見
  - `timeline_long.py` — 長尺を秒単位で見る道具(目視用。落とさない)
  - `check_*.py` — 機械ゲート。**縦型と横型で基準が変わるものは形式を見て切り替える**
    - `check_bunsho.py` — 日本語の文の組み方(指示語の連続・余分な語・読点の位置)
    - `check_goi.py` — **語を略していないか**(形態素解析。`pip install janome` が要る)。
      許可する普通語を `production/goi_futsu.txt` に置く。**一覧に無い語は落ちる**ので、
      登録し忘れは黙って通らない。ループ71のユーザー指摘「枠って一般的な用語じゃないだろ?
      省略してんじゃん。なんでそういうのチェックできないかな」への対応
    - `check_modoshi.py` — **直したはずの文が戻っていないか**。ストック(nihongo-stock /
      shiteki-stock)で「対応済み」の行の**直す前の文**が字幕に残っていたら落とす
    - `check_long.py` — **長尺専用。通しの流れ**(冒頭の秒・章の尺と継ぎ目・止め・話速・
      図の置き所・章の出口の判定・立ち絵・締め)。**音声が無くても推定で走る**
  - `setup_voicevox.sh`
- `videos/<ID>-<slug>/` — 動画1本ごとの作業ディレクトリ
  - `plan.md` — 企画書
  - `script.md` — 台本
  - `verify.py` — 動画内の数値の検証スクリプト(計算を含む動画は必須)
  - `render.py` — レンダリングスクリプト(shortlibを使用)
  - `output/<ID>.mp4` — 完成動画(output/work/ は中間生成物でコミットしない)

動画IDは `S001`(ショート・縦1080×1920・60秒未満)/
`L001`(長尺・横1920×1080)の連番。
**長尺に「8分以上」の下限は置かない。**KGI(視聴維持100%)が収益条件より上だと決めたため、
中身が尽きたら短く終わる(2026-08-22)。L001=6.7分・L002=6.5分はその判断による。

## 長尺の最重要KGI: 視聴維持100%

**長尺で迷ったら、視聴維持率が上がるほうを選ぶ。**(2026-08-22のユーザー指示)
「最後まで見られること」を唯一の最上位指標に置く。他の指標はこれに従属する。

そこから自動的に決まること:

- **収益条件(ミッドロール8分)よりKGIが優先**。中身が尽きたら短く終わる。
  8分に届かせるために内容を足すのは、KGIに反するので禁止
- **離脱の山(45〜75秒)に章の切れ目を置かない**。切れ目は視聴者に「ここで抜けていい」と
  教える合図になる。ここは `check_long` が機械で見る
- **図が無い時間を作らない**。実測でL001は図なしが合計139.6秒(全体の34.8%)・最長18.2秒。
  数字は図の上で言う
- **各章の出口に損得の判定を1文**。「まだ得があるのか」が分からないと次の章に進まない
- **1つでも嘘や読み違いがあると、その時点で信用が切れて離脱する**。
  だから出荷物の欠陥(字幕の取り違え・目盛のずれ・説明していない中心概念)は
  すべてKGI違反として最優先で直す

判断に迷ったときの順序: **維持率 > 正確さ > 網羅性 > 尺 > 収益条件**。
(正確さが維持率より下なのではない。不正確は維持率を壊すので、実質は同じ側にある)

## 長尺(横型)の作り方

設計の型は `docs/research/longform-design.md`。ショートの引き伸ばしにはしない。要点:

- **ショートは押し付けられ、長尺は選ばれる。** だから長尺は入口に専門語を使ってよく、
  むしろタイトルで争点を明示する。そして**冒頭40秒で答えを出し切る**
  (最初の数値は15秒以内、最初の図は20秒以内)
- **章は4つ。1章100〜130秒。** 「章 = ショート1本」は**間違いだった**。
  60〜90秒の拍は**章の中に**置くもので、章を刻む理由にはならない
- **各章の出口に損得の判定を1文。問いは判定ではない。**
  その直前に **pad 0.6 の止め**を置く(BGMもそこで切れる)
- **章札は前章の問いを繰り返さず、答えの側を予告する**
- **数字は図の上で言う。** 図が無い時間を25秒以上続けない
- **図が主役のユニットは立ち絵を消す**(`chara="none"`)。立ち絵は図への注視を奪う
- **画面は道標、文はナレーション。** 図に文を貼るとナレーションと同義になり読みにくくなる
- **話速は下げる。** `use_landscape()` が `SPEED_SCALE` を 1.15 にする(約330字/分)
- 概要欄のチャプターは目次ではなく**問い**にする。時刻は `sl.chapter_lines()` が出す
- **免責を最後の20秒に置かない。** 締めでは次に見るものを名指しする
- render.py の先頭で `S.use_landscape()` を **`new_canvas()` より前に1回だけ**呼ぶ

**台本を書いたら、焼く前に `python3 production/check_long.py videos/<ID>-<slug>` を通す。**
2〜3時間かけて焼いたあとに落ちるのでは意味がない。このゲートは音声が無くても
字数から尺を推定して走る。不合格の内容と根拠は
`docs/research/longform-loop-2026-08/11-gate.md`。

制作前に `python3 production/preview_long.py <出力先>` でレイアウトを目視すること。
155ユニットを書いてからレイアウト崩れを見つけるのは高くつく。

**IDは公開順に揃える**(ループ63のユーザー指摘「S13、S14を出してて、なんでS17? 普通S15でしょ」):

- **公開済みのIDは固定**。変えると過去の記録とズレる(2026-08-16時点で S001〜S011 が公開済み)
- **未公開のIDは、公開予定順に詰める**。飛び番号を残さない
- **5ゲートを通らない在庫は `videos/_wip/` に置き、番号を占有しない**。
  直して基準を通った時点で、その時の空き番号を取る
- ボツにしたものは `videos/_rejected/` に移す(番号は再利用してよい)

## 全動画で守るルール

**指摘は `docs/research/shiteki-log.md` に生のまま貯める。**
審査・テストで出た指摘は、その日のうちに要約せず追記する
(ユーザーの言葉は原文のまま)。2本以上の動画で出たもの・同じ動画で再発したものを
そこから抽象化して、下記のルール集へ移す。ログが判断材料で、ルール集が結論。

**批評パネル2回分の指摘は恒久ルール化してある。制作時に必ず参照する:**
視覚系(タイポ・レイアウト・色・図表・モーション・サムネ・ピクセル精度)は
`docs/research/design-rules.md`、言葉・プロット系(開示管理・日本語・コピー)は
`docs/research/kotoba-rules.md`。⚠️印は再発・両パネル一致の項目で、最優先で守る。

`docs/strategy.md` のセクション6(コンプライアンス)とセクション8(量産型規制対策)は
すべての企画・台本に適用する。要点:

1. 投資助言をしない。個別銘柄・特定商品・特定業者を**推奨**しない。
   **名指しは禁じていない**(2026-08-24。「一切言及しない」は Claude が
   最初のコミットで独断で入れた過剰な制限だった。詳細は strategy.md §6.2)
2. 断定表現(「儲かる」「必ず」)を使わない。利回りは「仮定」と明示する
3. すべての数字・制度説明に出典を付ける(官公庁の一次資料を優先)
4. 動画内の計算はすべて `verify.py` で再計算して一致を確認する
5. 「2026年◯月時点の制度です」の時点表記を入れる
6. 独自性要素(独自計算・独自グラフ・原文ベース解説・複数パターン比較)を2つ以上含める

## 調査の合格基準

リサーチ→検証→改善ループでは `docs/research/research-standards.md` に従う。要点:
**行動(コード・台本・設定)を変える主張は独立ソース5本以上+一次資料1本以上**。
相互コピペのベンダーブログ群は1本と数える。主張ごとに確度ラベル(A/B/C)を記録し、
確度Cで行動を変えない(自チャンネルのアナリティクスで検証する仮説として扱う)。

## 制度情報の鮮度

税制・社会保険・NISA等の制度は毎年変わる。企画時は必ずWebSearchで最新の制度内容を
確認し、企画書の「出典」欄に確認日とURLを記録すること。学習時点の知識だけで
制度の数値(控除額・上限額・税率)を書かない。
