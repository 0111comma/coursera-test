# 教育系YouTubeチャンネル 企画リポジトリ

お金・金融リテラシーをテーマにした教育系YouTubeチャンネルの企画を管理するリポジトリ。
チャンネル戦略は `docs/strategy.md`、テーマ選定の経緯は `docs/research/theme-candidates.md` を参照。

## ワークフロー(企画 → 動画まで)

```
/ideate        ネタ出し。戦略に沿ったネタを ideas/backlog.md に追加する
   ↓
/plan-video    ネタ1つを選び、videos/<ID>-<slug>/plan.md(企画書)を作る
   ↓
/write-script  企画書から videos/<ID>-<slug>/script.md(台本)を作る
   ↓           計算を含む場合は verify.py で数値を検証してから台本に反映
/produce-video 台本から videos/<ID>-<slug>/output/<ID>.mp4 をレンダリングする
   ↓           (production/shortlib.py + VOICEVOX + ffmpeg。詳細はスキル参照)
(投稿はユーザーのローカル環境から。このリポジトリの成果物はmp4まで)
```

## ディレクトリ構成

- `docs/` — 戦略・調査ドキュメント
- `ideas/backlog.md` — ネタのストック(ステータス管理付き)
- `templates/` — 企画書・台本のテンプレート
- `production/` — 制作パイプライン(shortlib.py=描画/TTS/結合、setup_voicevox.sh)
- `videos/<ID>-<slug>/` — 動画1本ごとの作業ディレクトリ
  - `plan.md` — 企画書
  - `script.md` — 台本
  - `verify.py` — 動画内の数値の検証スクリプト(計算を含む動画は必須)
  - `render.py` — レンダリングスクリプト(shortlibを使用)
  - `output/<ID>.mp4` — 完成動画(output/work/ は中間生成物でコミットしない)

動画IDは `S001`(ショート)/ `L001`(解説動画)の連番。

**IDは公開順に揃える**(ループ63のユーザー指摘「S13、S14を出してて、なんでS17? 普通S15でしょ」):

- **公開済みのIDは固定**。変えると過去の記録とズレる(2026-08-16時点で S001〜S011 が公開済み)
- **未公開のIDは、公開予定順に詰める**。飛び番号を残さない
- **5ゲートを通らない在庫は `videos/_wip/` に置き、番号を占有しない**。
  直して基準を通った時点で、その時の空き番号を取る
- ボツにしたものは `videos/_rejected/` に移す(番号は再利用してよい)

## 全動画で守るルール

`docs/strategy.md` のセクション6(コンプライアンス)とセクション8(量産型規制対策)は
すべての企画・台本に適用する。要点:

1. 投資助言をしない。個別銘柄・特定商品・特定業者を推奨しない
2. 断定表現(「儲かる」「必ず」)を使わない。利回りは「仮定」と明示する
3. すべての数字・制度説明に出典を付ける(官公庁の一次資料を優先)
4. 動画内の計算はすべて `verify.py` で再計算して一致を確認する
5. 「2026年◯月時点の制度です」の時点表記を入れる
6. 独自性要素(独自計算・独自グラフ・原文ベース解説・複数パターン比較)を2つ以上含める

## 調査の合格基準

リサーチ→検証→改善ループでは `docs/research/research-standards.md` に従う。要点:
**行動(コード・台本・設定)を変える主張は独立ソース5本以上+一次資料1本以上**。
相互コピペのベンダーブログ群は1本と数える。主張ごとに確度ラベル(A/B/C)を記録し、
確度Cで行動を変えない(自チャンネルのアナリティクスで検証する仮説として扱う)。

## 制度情報の鮮度

税制・社会保険・NISA等の制度は毎年変わる。企画時は必ずWebSearchで最新の制度内容を
確認し、企画書の「出典」欄に確認日とURLを記録すること。学習時点の知識だけで
制度の数値(控除額・上限額・税率)を書かない。
