# 実装の優先順位 — 2ラウンドで確定した約191件をコードに流す(2026-08-22)

## 0. なぜこの文書が要るか

**確定した変更が、ほぼ1件も実装されていない。** 作業ツリーを抜き取りで確認した結果
(確度A・自前測定。再現コマンドは各行に付けた):

```
[audio]B2-1  volume 0.15→0.50        未実装   grep -q 'volume=0.15' production/shortlib.py
[audio]B2-2  ratio=6→2               未実装   grep -q 'ratio=6' production/shortlib.py
[audio]B2-4a chords[bi%len]          未実装   grep -q 'chords\[bi % 4\]' production/shortlib.py
[audio]B3-3  peak正規化→clip         未実装   grep -q 'track = track / peak' production/shortlib.py
[bg]A1-1-fg  GRID #454440            未実装
[emph]A7-2   止め 6→10箇所           未実装   grep -c 'pad=0.5\|pad=0.6' videos/L001-nisa-son/render.py → 6
[emph]A7-3-1 SE 18→10箇所以下        未実装   pop 9 + don 5 + impact 4 = 18
[voice]B1-1  signature bug            実装済   check_long.py:112
[meta]D8     production/fmt.py        存在しない(A5-7 は着手不能)
gates G1     JARGONに「相殺」         未実装
metaL D2L    L_LEAD に L002           未実装(L002の概要欄の結論行が空で出荷される)
metaL D8L    make_srt.py 数値順        実装済   コミット 1ea1611
```

**13件中11件が未実装(85%)。実装済みの2件はどちらも「出荷物が壊れているバグ」で、
研究の成果としてではなく事故として直された。**

したがって本計画は **「何を調べるか」ではなく「どの順で手を動かすか」** だけを書く。

## 1. 全体の並べ方

```
[0] 裁定(3件・机上30分)         ← これを閉じないと(a)の一部が着手できない
 ↓
[a] 確度A・低リスク・すぐ効く     ← 再レンダリング不要。文書 / キット / ゲート / 企画工程
 ↓                                  約70件。1日〜2日
[b] 確度A・要再レンダリング       ← shortlib と scenes_long の値。L002 完成 → L001 焼き直し
 ↓                                  約60件。数日(レンダリング時間が支配的)
[c] A/Bテスト待ち                 ← 約40本。ほとんど回らないので仕分けが先
```

**(a) を全部終える前に (b) を始めない。** (b) はレンダリング待ちが長く、
途中で (a) の変更が入ると焼き直しが二重になる。

---

## [0] 先に閉じる裁定(3件)

これは調査ではなく判断である。**ユーザーの一言で閉じる。**

| # | 争点 | 決めること | 影響する件数 |
|---|---|---|---|
| 0-1 | 第1ラウンドの確度C **33件**は「実装しない」か「確定」か。`00-changes-round1.md` の見出しは前者、`script-hook.md` は後者として引用している | `00-gaps-v2.md` §1-1 の3分割(C-a 設計値の締め上げ=実装 / C-b 視聴者の反応の仮定=A/B / C-c 具体値だけC=計測WARN)を採るか | **33件** |
| 0-2 | `SPEED_SCALE` 1.15 → 1.00(話速 349→307字/分)を採るか。採るなら `longform-design.md` §4 の「330〜345字/分」も書き換える | 採る / 採らない。**採らないなら §4 を実測349に合わせて訂正する**(どちらにせよ現状の矛盾は残せない) | C7-1・C7-2・C7-3・§4・`check_long.py:51`・`shortlib.py:281`・`check_video.py` |
| 0-3 | 章尺ゲート 60〜150 → 90〜140 を、**公開済み L001 に適用するか** | 「新しく書く長尺にだけ適用」と明記する(推奨)/ しない | C7-2。しないと L001 が恒久的に不合格 |

**0-2 は測ってから決められる。** VOICEVOX は localhost:50021 で起動中なので、
`/audio_query` に L001 の113ユニットを `speedScale=1.00` で投げれば、
**焼かずに**総尺・話速・章尺が確定する(所要 数分)。**先にこれを測ること。**

---

## [a] 確度A・低リスク・すぐ効く(再レンダリング不要)

**この群は動画のピクセルにも音にも触らない。** だから焼き直しが要らず、今日から効く。

### a-1 出荷物の実害(最優先。いま壊れているもの)

| # | ファイル | 変更 | 出どころ |
|---|---|---|---|
| a-1-1 | `production/make_upload_kit.py` `L_LEAD`(179行) | **`"L002"` の項を追加する。** いまは L001 しか無く、**L002 の概要欄の結論行が空文字で出荷される** | R2 meta-long D2L(A) |
| a-1-2 | 同 `long_kit()`(190行) | **固定コメント欄を追加**。`pinned` を参照していないため L001 の固定コメントが0件(S031 は3件) | R2 meta-long D6L(A) |
| a-1-3 | 同 `L_LEAD["L001"]` | 冒頭を**3行・各行 全角幅24以下**に整形 | R2 meta-long D2L(A) |
| a-1-4 | 同 チャプター生成 | チャプター行から **「第N章 」接頭辞を外す**(全角幅3.0の無駄) | R2 meta-long D3L(A) |
| a-1-5 | 同 概要欄テンプレ | 「**verify.py で再計算しています**」を、ファイル名を出さない日本語に置き換える。対象 **20本** | R2 C5-6(A) |
| a-1-6 | 同 概要欄CTA | **L001 / L002 / S012 / S027** の CTA を他16本と同じ「…?コメントで教えてください。」型に。L001 の問いは Yes/No から「どちらでしたか」型に開く | R2 C6-3(A) |
| a-1-7 | 同 固定コメント | 詐欺注意の1行(60字)を `pinned` に**連結して完成形で印字**(約258字)。いまは境界が欠けている | R2 meta-short D6S(A) |
| a-1-8 | 同 サムネ行 | 「フレーム選択は**スマホのYouTubeアプリ**から(PCブラウザ不可)。**公開後も選び直せる(2024年9月以降)**。カスタム画像は**YPP参加後**」を追記 | R2 meta-short D8S(B) |
| a-1-9 | `videos/L001-nisa-son/render.py` `make_thumbnail()`(498行) | 先頭に **`S.CURRENT_BAND = None`**。いまサムネ左上に「第4章 自分の場合」の緑チップ `#3ecf8e` が焼き込まれている(実測: (30,30)-(420,110) の最頻色) | R1 [cover] A8-1-chip-bug(A) |
| a-1-10 | `docs/research/posting-plan.md` §1§2 | 投稿間隔の「4〜6時間以上」を実装(`make_upload_kit.py` の「30分以上離す」)に合わせて統一。**社内ルールが2本に割れている** | R2 meta-short D5S(A) |

> a-1-9 は**サムネ画像の作り直しだけ**で済む(`make_thumbnail()` は `render_video()` と独立に呼べる)。
> 動画本体の再レンダリングは要らない。

### a-2 企画工程(次に立てる企画から効く。2ラウンドで最も強い実測がここ)

**根拠**: `kikaku.md` §0。「損得が起きるのは全員か / 条件つきか」の分離力は **AUC = 1.000・平均差 +27.6pt**(n=14)。
題材型(AUC 0.571)・欲求型(0.521)・自前計算か(**0.364 = 逆向き**)を大きく上回る。

| # | ファイル | 変更 |
|---|---|---|
| a-2-1 | `templates/plan.md` | **`## 損得が起きる人(母数)`** を必須節として追加 |
| a-2-2 | `production/check_yokkyu.py` | 上の見出しがあり、**行に `%` か「人に1人」を含む数値がある**ことを課す。**閾値による自動不合格は設けない**(自己申告なので機械で真偽を判定できない)。公開済み S001〜S015 は「記録」扱いで落とさない |
| a-2-3 | `docs/research/yokkyu-map.md` | ① A>B>C>D の**優先順位を削除**(語彙としては残す)② **§7「捨てる判定」**を新設(5基準を実測の裏づけ有/無を明記して並べる)③「**題材の型は視聴率の予測に使わない**」を明記 ④ `598c73d` の根拠「答えは視聴者が自分では出さない計算の結果であること」が**実測で逆向き(AUC 0.364)**だったことを併記 |
| a-2-4 | `ideas/backlog.md` | ショート表の列を `\| # \| 損得が起きる人(母数) \| 欲求 \| 場面 \| 決定 \| フックの数字 \| ピラー \| 優先度 \| ステータス \|` に。**「フックの数字」を最終列に落とす**(先に数字を見つける事故の再発防止) |
| a-2-5 | `docs/strategy.md` §2 / `.claude/skills/ideate/SKILL.md` 手順1 | 「20〜35歳」→「**25-34歳と45-54歳の二山。男性が総再生時間の約9割**」(実測の転記) |
| a-2-6 | `docs/persona.md` | **P-C(45〜54歳・男性・会社員)** を追加。場面: 住宅ローンの後半 / 退職金 / 年金の受け取り方 / 親の介護費 / 役職定年 |
| a-2-7 | `production/check_dokujisei.py`(新規)+ `check_all.py` | 判定は3つだけ: `## 独自性要素` 節がある / 箇条書きが2つ以上 / `verify.py` が存在する。**すべて strategy §8 と CLAUDE.md §6-4 の既存義務**で、新しい閾値ではない |
| a-2-8 | `production/check_toi.py` docstring | 「この基準は自チャンネルでは未検証であり、**実績1位(S015 94.6%)・2位(S011 81.0%)は不合格になる**」を明記。**基準は変えない**(ユーザーの明示指示によるもので、確度Cの調査で上書きしない) |

### a-3 語のゲート(誤検出を増やさない方向だけ)

| # | ファイル | 変更 | 出どころ |
|---|---|---|---|
| a-3-1 | `production/check_teinei.py` `JARGON` | **「相殺」を追加**。L001 の #70 / #74 でナレーション2回、画面文字を含めると**視聴者が目にするのは8回**、どこでも意味を言っていない。**納品済みの長尺に残っている見逃し** | R2 gates G1(A) |
| a-3-2 | `production/goi_futsu.txt` | 「**足さないと決めた語**」の記録欄を設ける。いまは追加の履歴しか残らず、「枠は入れない」という判断がコメント1行にしかない | R2 gates G1(A) |
| a-3-3 | `production/check_ryakugo.py` | 検出を**語ごとに1件へ集約**する(過去の欠陥版で16件 → 語単位に) | R2 gates G2(A) |
| a-3-4 | `production/check_flow.py` COUNTER | 逆接に**対義語とひらがな表記の動詞**を足す(「損」「得を\|得に」「ふえ\|へっ\|減っ」)。112件→110件で、消えるのは狙った2件だけ。**裸の「得」は禁止**(「所得税」「取得」「納得」に一致する) | R2 gates G4(A) |
| a-3-5 | `production/check_flow.py` `referent_issues` | 「**直前が疑問文なら新出主語を許す**」除外を追加(16件中2件だけ抑制、真の指摘は0件減る) | R2 C4-4(A) |
| a-3-6 | `production/check_bunsho.py` | 「同一文頭接続語が **4回以上 かつ 10%超**」を **WARN** として追加(落とさない) | R2 C4-3(A) |
| a-3-7 | `production/check_teinei.py` | 旧世代9本を**不合格ではなく `[SKIP]`** で出す(`videos/_legacy/` に移すか明示リストを置く)。いま落ちているのは「ゲートの効き」ではなく「いつ作られたか」 | R2 gates G3(A) |

> **a-3 で「同語尾5連続WARN」は入れない**(R2 C4-5 で却下)。
> 「語尾」の定義が文書に無く、落ちる本数が再現しなかった(原案11本 / 再測6本)。**定義を書いてから測り直す。**

### a-4 ゲートの閾値・計測(まず落とさない側から)

| # | ファイル | 変更 | 出どころ |
|---|---|---|---|
| a-4-1 | `production/check_video.py:132-133` `_w()` | 近似幅を実幅に寄せる: 約物 `。、!?…` を **0.6→1.0**、半角数字を **0.55→0.61**、`%` を **0.55→0.99**。**いまの近似は幅を小さく見積もり、実測で0.75を割る4行を全部PASSさせている** | R2 A3-18(A) |
| a-4-2 | `production/check_long.py` | 最初の数値を「**答えの数値**(render.py に明示)が **15秒以内**」に置き換える(カバーフレーム0.07秒は初出に数えない)。閾値15秒は据え置き | R2 C1-2(A) |
| a-4-3 | 同 | 最初の図 **20秒→12秒**。加えて「**章の頭から15秒以内に最初の図**」(締めブロックは対象外) | R1 C1-3 / R2 C1-3 |
| a-4-4 | 同 | 「**章札は判定語か数値を含み、手順語で終わらない**」。L001 は #12/#65/#82 の3本を書き直す | R2 C2-3(A) |
| a-4-5 | 同 | **SEを持つユニットが全体の9%を超えたら不合格**(現状 L001 は 18/113 = 15.9%)。現行の check_long には SE のゲートが1行も無い | R1 [emphasis] A7-3-1(A) |
| a-4-6 | 同:156行 | 止めのゲートを「pad≥0.5 が3箇所以上」→「**6箇所以上、かつ章札の直前4箇所すべてに pad≥0.5**」 | R1 [emphasis] A7-2(A) |
| a-4-7 | 同 | 鎖: 「**章あたり初出数値 ≤10 → 不合格**」「**鎖の長さ >40 → WARN**」。L001 は無修正で通る(最大7・長さ24)が、**L002 の47連鎖は現行を素通りする**。数え方は `check_ikko.py` の `NUM_RUN` + `to_value` に揃える | R2 C5-1(A) |
| a-4-8 | 同 | **章尺 90秒未満を WARN**(不合格は 60〜150 のまま)。裁定 0-3 の対象 | R2 C2-2(A) |
| a-4-9 | 同 | 締めブロックを章から出す。切る位置は **#109(`shime` の頭)= 16.25秒**。**判定は `heads` ではなく `BANDS` を見る**(この2つは別物で、`heads` を見ると空振りする) | R2 C6-1 / C2-1(A) |
| a-4-10 | 同 | 「次に見るものの名指し」を、**`script_meta.json` の description にその名前かURLが実在するか**まで確認する判定に(現状 URL 0件・「ショート」0件で、言っているのに概要欄に無い) | R2 C6-4(A) |
| a-4-11 | 同 | **字幕の数値が図にも出ているか**の照合を足す(**落とさず一覧を出す**。L001 の現状は約8割一致) | R2 A3-20(A) |
| a-4-12 | 同 | 「章が90秒を超えるとき、内部に止めを1つ以上」(現状追認の床) | R2 C2-6(A) |
| a-4-13 | 同 | 「章の中で前向きの引きが90秒を超えて途切れない。引きは数値か固有名を含む」を **WARN**(不合格ではない。語を1つ足せば通るため) | R2 C1-6(B) |
| a-4-14 | `production/check_video.py` | ハッシュタグを **`== 3`** に締め、`#お金の勉強` を**含む**ことを課す(posting-plan §7 と実装の食い違いが解消する)。S012 を修正 | R2 meta-short D4S(A) |
| a-4-15 | 同 | 概要欄1行目 **≤50字**(現状6本が超過)/ タイトル **≤28字**・**【】禁止**・検索語が先頭15字 | R2 meta-short D1S/D2S(A) |
| a-4-16 | 同 | RELATED の**網羅・自己参照・未来参照**をチェック(S010以降のみ)。**関連動画に指定できるのは公開済み動画だけ**なので、未来参照(2件)は様式でなく運用が止まる話 | R2 meta-short D7S(A/B) |
| a-4-17 | `production/scenes_long.py` のゲート | **barsN**: `N×見出し字数≤40` / `N×値字数≤60` / N≥3 で highlight 未指定は WARN / N≤9。**table**: 行数≤7・列数≤6。**timeline**: n≤5・箱の中身が縮小したらNG。**hero**: main ≤9字で WARN。**compare2**: m の上限4 | R2 figure2 #9/#10(A) |

> **a-4 の注意**: これで `check_long` に**11本のゲートが増える**。
> `check_long` の15の不合格条件は**人の合否で1度も検証されていない**(ユーザーが合格させた長尺は0本)。
> **較正されていないゲートに足している**ので、a-4 を入れたら **L001 と S001〜S031 の全部に走らせ、
> 「合格作を落としていないか」を1回だけ人が見る**こと(`check_ikko.py` の較正 `02-calibration.md` と同じ手続き)。

### a-5 文書の訂正(実測と食い違ったまま次の書き手に渡さない)

| # | ファイル | 変更 |
|---|---|---|
| a-5-1 | `docs/research/longform-design.md` §4 | 話速対応表を**実測値に差し替え**、「L001 の per-unit speed 分布での値・平均実効 speed 1.214」と但し書き。1章の字数目安 581〜755字。**裁定 0-2 の結論をここに反映する** |
| a-5-2 | 同 冒頭 | 「0〜40秒」→「**0〜45秒**」(実測43.2秒)。§2 に**再フックの型**(25〜35秒に1つ。器=ずんだもんの驚き1台詞)を明文化 |
| a-5-3 | 同 §5 | 「**図の更新の1回を章の中央付近(章開始+50〜70秒)に置く**」の一文(回数のゲート化はしない — 全章が既に3〜8回で一度も発火しない) |
| a-5-4 | 同 §8 | 章の出口の1文を **12字→20字**(ゲートにはしない)。内容条件(数値+判定語)を併記 |
| a-5-5 | `docs/research/duo-skit-2026-08.md` | ①「かしら」を削る(L001 に**0本**)②「章の入口は聞き手が問う」→「**章の入口は解説役が答えの側を予告する。問いは章の中に置く**」(L001 は 0/4) |
| a-5-6 | `docs/research/sakubun-gensoku.md` と `longform-design.md` | 「**入力(仮定)は切りのよい数。出力(計算結果)は円単位の正確値。混ぜない**」を明記。L001 の13個すべてが既にこの規則に一致(混在表記0個)。**数値そのものは変えない** |
| a-5-7 | `production/shortlib.py` `use_landscape()` docstring | 実測に訂正。「横幅が1.78倍なので12字→26字」は誤り(実収容量 **縦10.5字 / 横29.5字 = 2.81倍**)。「横型40pt=4.9%」は実測 **5.19%** |
| a-5-8 | `production/scenes_long.py` | 「役割→pt」の現状表をコメントで書く(**数値は変えない**。いまの値に基準が無いことだけが分かっている) |
| a-5-9 | `docs/research/research-standards.md` | **この環境向けに基準を書き直す**: 「一次資料が読めない環境では、外部根拠だけで行動を変えない。自前測定で現状の欠陥を示せた場合にのみ、外部は**対処法の選択**に使ってよい」 |
| a-5-10 | `docs/research/element-taxonomy-2026-08.md` 冒頭 | 「**この版は v2 に置き換わった。値は `element-taxonomy-v2.md` を読むこと**」の1行を追加(前版だけ読んだ人が誤った値を信じるのを止める) |

---

## [b] 確度A・要再レンダリング

**ここから先はピクセルと音が変わる。** 焼き直しが要るので、**まとめて1回で当てる。**

### b-0 先に L002 を基準まで直す(視聴データ不要・n=2 を作る作業)

第2ラウンドで**却下された決定の多くが「根拠が n=1(L001だけ)」**だった
(A4-13 立ち絵の判定 / A7-7 章の中の拍 / A5b-8 数値ラベル)。
`videos/L002-kinri-wakaremichi` は **155ユニット・図34%・19件不合格・立ち絵の判定に53件違反**で残っている。

**L002 を通せば、上記3件の A/B が「A/B ではなく規則化」に格上げできる。**
これは (b) の中で唯一、**視聴データを1件も必要としない**投資である。

### b-1 `production/shortlib.py` — 音(まとめて1回)

| # | 行 | 変更 | 効果(実測) |
|---|---|---|---|
| b-1-1 | 570 | `volume=0.15` → **`0.50`** | 現行は声比 **-42.3dB** で事実上無音。0.30 では発話中 -29.4dB、0.40 でも -26.9dB で狙いの帯(-20〜-25dB)を外し、**帯に入る最小値が 0.50** |
| b-1-2 | 571 | `sidechaincompress` を threshold **0.015→0.03** / ratio **6→2** / attack **8→20** / release **300→400**。`makeup=1` は ffmpeg 既定と同値なので削ってよい | ダック量 15.6dB → 8.7dB。現行の15.6dBは音楽3〜8dB・放送10〜15dB の**どちらの帯にも入っていない** |
| b-1-3 | 412 | `chords[bi % 4]` → **`chords[bi % len(chords)]`**、**かつ** `BGM_VARIANTS` の各進行を4和音→8和音 | **この2行はセット。片方だけでは黙って効かない**。現状 L001 は4小節11.16秒を35.9回反復し、変化は5箇所・計3.0秒(全尺の0.75%) |
| b-1-4 | 509-513 | `if peak > 1.0: track = track / peak` を **`np.clip` かソフトリミッタ**に | 全トラックに掛かるスカラー除算なので、**1箇所SEが重なるだけで動画全体のSE音量が台本次第で下がる**。既存動画は間隔22秒で未発生なので**再レンダリング不要**の唯一の音修正 |
| b-1-5 | `synth_se_track` の `don` | **2〜6次の連続倍音**を加算(案B')。案B(4+7次)を対抗案として残す | B。**最初の1本を焼いてから耳で決める**(この環境では判定できない) |
| b-1-6 | `_load_se_file` | 先頭無音の**トリム**(`impact` は0.109秒)。**上限0.3秒**を付ける(将来ゆっくり立ち上がる素材を入れたときにアタックを削らないため) | A。効果は「揃う」だけと明記 |
| b-1-7 | 話者別 | pitch を話者ごとに分ける / めたんの speed 較正 / true-peak / `aresample` | R1 [voice] B1-2・B1-6・B1-5(A) |

> **`pause_scale` 1.1→1.25 は入れない**(R1 [voice] B1-4-pause-scale は A/B送り)。
> 尺への寄与+1.5%で、1.25 という値に根拠が無い。

### b-2 `production/shortlib.py` — 色とコントラスト

| # | 変更 | 効果(実測) |
|---|---|---|
| b-2-1 | GRID `#2c2c2a`→`#454440` / BASELINE `#383835`→`#54534e` / MUTED `#898781`→`#9a9891`。**背景の明度は変えない**(47-50行) | パネル面比 GRID 1.06→1.35(現状ほぼ不可視)/ BASELINE 1.12→1.71 / MUTED 3.66→**4.55**(行47のコメントが自ら課した4:1 を現状は割っている)。提案の `#999790` は 4.4976 で目標を割るので **`#9a9891`** |
| b-2-2 | 章チップの色順を **黄→紫→赤→青→緑** に。順序は `videos/*/render.py` に散らさず **`shortlib.BAND_COLORS` に集約** | Viénot 1999 の自前実装で、現行は**P型で第2章紫と第3章青が距離0.0161(判別不能)**。提案順は 0.7453 で**全120順列中1位**。色を1つも変えず既存 ACCENTS との対応も壊さない |
| b-2-3 | MUTED_BAR の明度 / ghost棒 / compare2 の主役色 / highlight の必須化 | R1 [figure] A5-2 系(A) |

### b-3 `production/shortlib.py` — 字幕

| # | 変更 | 効果(実測) |
|---|---|---|
| b-3-1 | **`wrap_plain()`(783行)を案Dに差し替える**: 文節境界 ∪ 句読点直後 から、専門語・4字以上の連続名詞の内側を除いた候補を作り、`max(左の字数, 右の字数)` を最小化する1点で2分する(同点なら中央寄り)。`forbidden()` を含む | **matplotlib 実測幅**で: 縦型の自動縮小 **221本(47%)→147本(31%)**、最小縮小率 **0.727→0.750**、行長差 平均 3.75→2.43(縦)/ 7.50→2.90(横)、文節途中 2→0、専門語分割 0→0。**739字幕中162本(22%)の見た目が変わる** |
| b-3-2 | 縁取り / 出現演出 / 2行厳守 | R1 [subtitle] A3-7・A3-14-1・A3-5(A) |

> **`SUB_WRAP` の値(縦12 / 横26)は変えない。** 掃引したが効かなかった(A3-18)。
> **b-3-1 と a-4-1 は同じ回に入れる**(案Dで最悪ケースが0.750に上がるので、幅係数を直しても落ちなくなる)。

### b-4 `production/scenes_long.py` — 図(**バグ修正が3件含まれる**)

| # | 型 | 変更 | 効果(実測) |
|---|---|---|---|
| b-4-1 | timeline | stagger `0.50` → **`min(0.50, 1.2/max(1,n-1))`** | 意図しない薄さ9か所(4ユニット)が消える。**n≥6 で6箱目以降が描かれない不具合も消える**。n≤3 は無変更 |
| b-4-2 | table | stagger `0.35` → **`min(0.35, 1.2/max(1,rows-1))`** | 同4か所。**rows≥8 で8行目が描かれない不具合も消える**。4行以下は無変更 |
| b-4-3 | barsN | stagger `0.28` → **`min(0.28, 1.4/max(1,N-1))`** | N≤6 は無変更。**N≥10 で10本目が描かれない不具合が消える** |
| b-4-4 | compare2 | 棒配置を **`slot=0.300/m; bw=min(0.110, slot*0.74); bx=x-0.150+slot*(k+0.5)`** に | m=1,2 は無変更。**m=3 の重なり67px・枠外77px が消える** |
| b-4-5 | compare2 | `note_l/note_r` の y `0.212` → **`0.236`** | **字幕帯への潜り 14か所 / 10シーン / 31ユニット(L001 23・L002 8)が消える** |
| b-4-6 | timeline | note の y `0.232` → **`0.238`** | 帯との余白 +10.9px → **+17.4px** |
| b-4-7 | timeline | 箱の中身に **`max_w = 箱幅*0.92`**(縮小が起きたら NG) | はみ出し4か所を検出できる |
| b-4-8 | band | GOLD/EMPH の境目に **SURFACE `#1a1a19` の縦線 4px** | 境界コントラスト **1.65 → 5.25**(線とEMPHは 8.65) |
| b-4-9 | 横断 | 数値ラベルの**持ち場 clamp** / 数値を見出しより大きくする | R1 [figure] A5-4 系(B) |

> **数値ラベルの絶対値(pt)は変えない**(AB-F1)。測れたのは「いまの値に基準が無い」ことだけで、
> **図中ラベルの下限を書いた資料はスニペットにすら1件も出てこなかった。**

### b-5 立ち絵

| # | ファイル | 変更 |
|---|---|---|
| b-5-1 | **`production/zunda.py:93`** | `while t < 120:` → **動画長を渡して回す**。いまは **120秒で瞬きが止まる**ので、400.7秒の L001 は**残り70%が無表情**になっている |
| b-5-2 | `production/shortlib.py` | 位置(左端・右端)/ 向き / 非話者の減光 α0.72 / 名前プレート / 表情の種類 | R1 [tachie] A4-2・A4-3・A4-7・A4-10・A4-11(A) |
| b-5-3 | `production/metan.py` | `talk_bob_px` を talking の真偽から **mouth 段階連動**へ(mouth==2→6.0px / 1→3.0px / 0→0.0px)。**本命のパーツ分割素材はユーザーの入手待ち**(配布ページと同梱readmeの確認が要る。この環境から seiga.nicovideo.jp / booth.pm / wikiwiki.jp はすべて遮断) |

### b-6 サムネイル(a-1-9 とセットで)

| # | 変更 |
|---|---|
| b-6-1 | ずんだもんを **`[0.601, 0.010, 0.384, 0.780]`・`flip=False`** に(幅は素材 800×915=0.8743 に合わせた値。右配置なら反転を外すのが正しい) |
| b-6-2 | 文字を **x 0.035〜0.590 に閉じ込め `ha="left"`**。現状は `#fab219` の画素が x 204〜1716px(0.106〜0.894)に広がり、**ずんだもんの顎とめたんの頭部を横切っている**。帯の右端0.590と立ち絵左端0.601の隙間は21px |
| b-6-3 | 注記の色を MUTED `#898781`(4.85:1)→ **INK_2 `#c3c2b7`(9.72:1)**。30ptの小さい字なので上げる側でよい |

### b-7 転換(L001 の台本側)

| # | 変更 |
|---|---|
| b-7-1 | **章札の直前4ユニット(#12/#43/#65/#82)に `pad=0.5`** を追加(止め 6→10箇所)。総尺への影響は **+1.4秒**。設計書(`longform-design.md` 110-127行)に「章札の直前 0.5s」と書いてあるのに実装されていないだけで、議論の余地が無い |
| b-7-2 | **unit11 に `se="don"`**(冒頭ブロック→第1章の43.2秒地点) |
| b-7-3 | **`BANDS` に締めの帯を追加**(添字の直書きをやめ、色を冒頭と別にする) |
| b-7-4 | **SE を 18箇所 → 10箇所以下**に絞る。残す優先順位は「冒頭の答え / 各章の出口の判定 / 章札 / 締め」。`impact` は4→2以下 |
| b-7-5 | **#112 の直前に pad 0.6**(章の出口の止め) |
| b-7-6 | 最後20秒のユニットを **`chara="none"`** に(終了画面の枠を空ける) |
| b-7-7 | 章札 #12/#65/#82 を「判定語か数値を含み、手順語で終わらない」形に書き直す |
| b-7-8 | 全体量の予告に**締めを入れる** |
| b-7-9 | 字幕の4桁以上の金額を**カンマ付き**に(21箇所)。**単独で当てると `check_long` が不合格になる**(字数の数え方の正規表現に**半角カンマを足すのが同時修正**) |

### b-8 焼き直し

**b-1〜b-7 を全部当ててから、L001 を1回だけ焼き直す。**
`figure2.md` が「#1〜#8 を入れたら `videos/L001-nisa-son` は再レンダリングが要る」と明記している
(オチの箱=`san2` の5年目が、納品済み mp4 の中で **1.57:1** のまま入っている)。

**焼く前に必ず**:
1. `python3 production/preview_long.py <出力先>` でレイアウトを目視
2. `python3 production/check_long.py videos/L001-nisa-son`(a-4 の11本を入れた後の版)
3. VOICEVOX `/audio_query` で総尺を先に推定する(2〜3時間かけて焼いた後に落ちるのを避ける)

### b-9 保留(裁定 0-2 の結論待ち)

`SPEED_SCALE` 1.15 → 1.00。**採るなら4か所を同時に直す**:
`production/shortlib.py:281` `LONG_SPEED_SCALE` / `production/check_long.py:51`
`SEC_PER_CHAR_LONG = 0.152 * (1.3 / 1.15)` → `(1.3 / 1.00)` / `check_video.py` の推定尺 /
`longform-design.md` §4。**`check_long.py:51` を直し忘れると推定尺が約13%短く出て、
下限ゲートを実際より通りやすく見せる**(危険な側)。

---

## [c] A/Bテスト待ち(約40本)— まず仕分ける

### c-0 現実:設計された40本の大半は回らない

`kikaku.md` AB-0 の自前計算。実測14本の視聴率から群内SDをプールすると **SD = 8.41pt**:

| 検出したい差 | 各群 | 合計 |
|---|---|---|
| 5pt | 46本 | **92本** |
| 10pt | 12本 | 24本 |
| **15pt** | **6本** | **12本** |

**このチャンネルの本数では「15ポイント以上動く変更」しか検証できない。**
冒頭の言い回し・語彙・色といった細部は**原理的に自チャンネルでは判定できない**。

### c-1 先にやる順(安い順)

| 順 | ID | なぜ先か | 必要本数 |
|---|---|---|---|
| 1 | **AB-CH-3**(立ち絵の判定の前提整備) | **A/Bではない。視聴データ不要。** L002 の53件を直して2本揃うかを見るだけ。→ b-0 と同一作業 | 0本 |
| 2 | **AB-D8S-b**(表紙の伏せ字 vs 答え) | **追加制作なしで比較できる。** S027〜S031 の5本が既に伏せ字なので、公開後に S010〜S023 と比べるだけ | 0本 |
| 3 | **AB-F2**(barsN の既定の強調) | **1本の動画の中で 19対19 に割れる**(L002 に barsN が38本ある)。動画をまたがない | 1本 |
| 4 | **AB-2**(損得が起きるのが全員か / 条件つきか) | **唯一 AUC 1.000 が出ている軸**で、外れたときの損害が最も大きい。`kikaku.md` §E1-5 の**事前登録17本でほぼ賄える** | 各群6本・計12本 |
| 5 | **AB-SE-1**(don の倍音) | 次に出す長尺2本+縦型8本に新版を使い、旧版群と比べる。**苦情が出たら即戻す** | 新版10本 |
| 6 | AB-CH-1 / AB-BEAT-1 / AB-TR-1 | いずれも**長尺1本の中の区間比較**。ただし**視聴回数1,000回に届かないと判定しない** | 長尺1本ずつ |

### c-2 やらないと決めるもの

| ID | 理由 |
|---|---|
| **AB-D7**(終了画面 10秒 vs 20秒) | 1群36本。長尺のみでは**判定不能**と `meta-long.md` が自ら結論 |
| **AB-D2**(概要欄の冒頭) | 概要欄の展開率が取れないので**判定不能**。内部矛盾の解消として実施済み扱い |
| **D5L**(長尺の投稿時刻) | 差10pt に1群36本=約17ヶ月。**A/Bを設計しない**。代わりに Studio の「視聴者がYouTubeを利用している時間帯」を**1回見る** |
| **AB-D5S**(ショートの投稿時刻) | 片群27本・計54本。**先に Studio の同じ画面を見れば不要になる可能性が高い** |

### c-3 守ること

- **1回に変えるのは1要素**(`posting-plan.md` §6-4)。上を同時に走らせるとどれも判定できない
- **視聴回数1,000回に届かない動画では判定しない**(維持グラフのノイズが継ぎ目の差より大きい)
- **判定できなかったものは「効果なし」ではなく「未判定」と書く**
- **A/Bで決まるまで `check_long.py` にゲートを足さない**(A7-7 の拍ゲートと A4-13 のビルダー判定は、
  どちらも「測れた事実」から「まだ測れていない規則」へ飛んで却下された)

---

## 2. ユーザーに1回だけ頼むこと(この環境では取れないもの)

| # | 頼むこと | 何が前に進むか |
|---|---|---|
| 1 | Studio の「**視聴者がYouTubeを利用している時間帯**」を1枚 | D5L / D5S / AB-D5S(**計54本のA/Bが不要になる**) |
| 2 | **CTR(インプレッションのクリック率)** | D1L / A9-5(サムネの顔)。**タイトルとサムネを評価する正しい指標がこれで、いま1件も持っていない** |
| 3 | S001〜S009 の**実際の公開時刻** | `SCHEDULE` と `posting-plan.md` §2 が食い違い、**14本中9本の時刻が復元できない** |
| 4 | めたんのパーツ分割素材(配布ページと同梱readme) | A4-12 の本命。**この環境からは配布元に一切到達できない** |
| 5 | L001 公開後の**章別維持率** | AB-3(章数)/ AB-F1 / AB-BEAT-1。長尺の実績が**1本も無い**のが D と C の最大の制約 |

作成 2026-08-22 / 根拠: `element-taxonomy-v2.md` / `00-gaps-v2.md` /
`00-changes-round1.md`(117件)/ 第2ラウンド10ファイル。
実装状態の抜き取り13件は、本文書の作成時にこのコンテナで実行して確認した(確度A)。
