# C5 数字の扱い + C6 締め + C7 尺

調査日: 2026-08-21(検証: 2026-08-22)/ 対象: L001(唯一の焼き上がった長尺)
+ **mp4 のあるショート28本**(S001〜S023・S027〜S031。S024〜S026 は未制作)+ L002(未焼き)

> **検証記録(2026-08-22 / 反証担当 C5・C6・C7)**
> 26回の測定のうち **19回は同じ手順で完全に再現できた**(尺・章の尺・止め・話速・
> 登場位置8点・相関表3種・カンマ58箇所・CTA 16/20・占有率4フレーム・ショート28本統計)。
> **再現できなかった/誤っていた測定を7件、本文中に「【訂正】」として直した。**
> うち行動に効くのは3件:
> (1) **C5-6 の「21/30本」は 20/20本**(script_meta.json は20個しか無い)、
> (2) **C7-2 の「8分未満の WARN は無い」は誤り**(`check_video.py` に2箇所ある。走らせて確認)、
> (3) **C5-1 の「ショートの鎖の長さ 9〜14」は実測 2〜14**(下端を切り落としていた)。
> さらに **調査員が見落とした副作用を3件**「【追加】」として足した。
> **決定を1つ変えた**: C5-1 の「鎖の長さ ≤ 40」は較正の根拠が無いので
> 不合格条件から **WARN に落とした**(「章あたり初出数値 ≤ 10」は L001=7 / L002=14 の
> 中点にあるので不合格条件のまま)。
> **C7-2 には適用範囲の限定を条件として付けた**(新作限定にしないと、
> C7-1 の「L001 はそのまま出す」と矛盾して公開済みの唯一の長尺が恒久的に赤くなる)。
> C5-5 と C7-1 には **同時に直さないとゲートが壊れる箇所**があり、本文に書いた。

## この文書の証拠の質について(先に書く)

第1ラウンドの反省として、**この環境で何が読めて何が読めないか**を先に明示する。

- **外部サイトの本文は読めない**。実測: `dornsife.usc.edu`(Zhang & Schwarz の PDF)は
  `EGRESS_BLOCKED`。`support.google.com` も本文は取れない。
  **したがって本文書に「一次資料を読んだ」という記述は1つも無い。**
  検索結果のタイトル・スニペットだけを見た箇所は、そう書いてある。
- **代わりに測った**: L001 の mp4・113本の wav・抽出フレーム・VOICEVOX エンジン(localhost:50021)・
  `check_*.py` の実行・公開29本の台本統計。再現手順は各項目に書いた。
- 確度の定義: **A = 自分で測った** / **B = 独立3件以上のスニペット一致** / **C = それ未満**。
  確度Cで現状を変えていない(CLAUDE.md の規則)。

## 測定の土台(全項目が参照する数値)

| 測定 | 値 | 測り方 |
|---|---|---|
| L001 の尺 | **400.704秒(6:40.7)** | `ffprobe -show_entries format=duration` |
| ユニット数 | 113 | `render.py` の `UNITS`(`Unit(` の出現数と一致) |
| ナレーション実尺 / 無音 | 381.504秒 / **19.200秒(4.8%)** | `output/work/seg_NNN.wav` と `seg_NNN_pad.wav` を `wave` で全件読み、合計。pad 合計は mp4 の尺と 0.000秒 一致 |
| 話速 | **349字/分 = 466モーラ/分** | 字は `check_long.py` と同じ数え方(2328字)。モーラは VOICEVOX `/audio_query` の `accent_phrases[].moras` を全113行ぶん合計(3109モーラ) |
| 章の尺 | 冒頭43.2 / 1章106.5 / 2章74.3 / 3章60.2 / 4章116.5 秒 | 上の pad 累積で「では第N章」の行の開始秒を出した。`output/chapters.txt` の 0:43 / 2:29 / 3:43 / 4:44 と一致 |
| 止め(pad≥0.5s) | 5箇所(35.7 / 141.3 / 215.6 / 277.1 / 371.3 秒)。他108本は全部 0.15s | 同上 |
| 機械ゲート | `check_long.py` **ALL PASS** / `check_ikko.py` **鎖1本・宙に浮いた数字0個** | 実行した |
| 図の割合 | **63%(71/113)** | `check_long.py` と同じ判定(`FIGURE_KINDS`)で数え直した。**下の C7-2 の表にあった 66% は誤り** |

> **検証(再現できた)**: 上の表は 400.704 / 113 / 381.504 / 19.200 / 349字/分 /
> 章 43.2・106.5・74.3・60.2・116.5 / 止め 5箇所(35.67・141.30・215.64・277.07・371.29、他108本は 0.15)
> まで**すべて小数第3位まで一致**した。`check_long.py` の ALL PASS、
> `check_ikko.py` の「鎖1本・0個」も再実行して一致。**土台は信用してよい。**

---

## C5-1 鎖の本数(現状1本)

- **現状(測定値)**:
  `python3 production/check_ikko.py --detail` を実行(**走るのは30本**。
  S024/S025/S026 は `render.py` が無いので対象外。「全31ディレクトリ」は誤り)。
  - L001 = **鎖1本 / 宙に浮いた数字0個**。鎖の**長さは24数値**
    (20万円 → 40630円 → … → 8126円 → 100万円 → 80万円 → 8)
  - 全30本のうち 鎖1本=26本、鎖2本=4本(S013 / S016 / S021 / S022)。閾値 `MAX_CHAINS=2`
    → **再現できた**(4本の内訳まで一致)
  - **鎖の長さ**【訂正】: ショート **2〜14(中央値8・平均8.0)** / L001 **24** / L002 **47**。
    28本の実測値は
    `2,2,3,3,4,5,5,6,6,7,7,7,8,8,8,9,9,9,10,10,11,11,11,11,11,13,13,14`。
    **元の「9〜14」は上位半分だけを見た値で、下端を14本ぶん切り落としている。**
    分離の話としては結論は変わらない(L001=24 は最長のショートの1.7倍、L002=47 は3.4倍)が、
    「ショートは9〜14に収まっている」という**閾値の較正根拠としては使えない**
  - 章あたりの初出数値【訂正】: `check_ikko.py` 自身のトークナイザ(`NUM_RUN` + `to_value`)で
    章境界ごとに数え直すと **冒頭3 / 1章7 / 2章5 / 3章2 / 4章7(最大7)**。合計24で、
    **鎖の長さ24と一致する**(宙に浮いた数字が0個なので当然そうなる)。
    **元の「3/9/8/4/8(最大8)」は再現しなかった**(合計32になり、鎖の長さと合わない)。
    ゲートに使うなら、ゲート本体と同じトークナイザで数えるべきである
- **ここで見つけた欠陥(確度A)**: `check_ikko.py` は**グループの個数**しか見ていない。
  だから **L002 の「47個が1本につながった鎖」は「鎖1本」で合格になる**。
  ゲートの docstring に書かれた較正(合格作1〜2本 / S013だけ4本)は**ショートで取ったもの**で、
  長さの上限は誰も決めていない。**長尺では個数の判定が空振りする。**
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持 | 鎖1本、長さ無制限 | 現行 | 追加の作業ゼロ | L002 の47連鎖が素通りする(実測) |
| b 鎖の長さに上限 | 長尺は鎖の長さ40超 → **WARN**(検証側で不合格から降格) | L001=24 / L002=47 で分かれる | 実測で分離できる指標のひとつ | 較正が n=2。**ショートの分布 2〜14 からは40を導けない** |
| c 章あたりの初出数値に上限 | 章あたり10個超で不合格 | L001 の最大は**1章/4章の7個**(訂正後) | 「1章=ショート1本ぶんの数値負荷」という既存の設計語と一致 | 章の切り方でごまかせる |
| d 鎖を2本まで許す | 長尺は問いが複数あるので緩める | — | 企画の自由度 | `check_ikko` の設計思想(1動画1問い)と衝突。**採らない** |

- **比較**: L001 は c を満たす(**最大7** ≤ 10)し b も満たす(24 ≤ 40)。
  L002 は b で落ち(47 > 40)、**c でも落ちる**。
  【追加・元の文書が「偏りは未測定」と書いていた箇所を測った(確度A)】
  L002 の章あたり初出数値を、`check_ikko.py` と同じトークナイザで章境界ごとに数えると
  **6 / 14 / 6 / 8 / 10 / 3 / 1(冒頭+6章)。最大は1章の14個**で、
  **閾値10を超える**(平均7.8という元の推測より偏っていた)。
  つまり **b と c は「いま通っているものを落とさず、L002型の膨張だけを落とす」**。
- **【追加】c のほうが b より較正が良い。** 測ってみると位置関係がはっきり違う:

| | L001(合格させたい) | L002(落としたい) | 閾値 | 余裕 |
|---|---|---|---|---|
| b 鎖の長さ | 24 | 47 | 40 | 上に16・下に7。**24と47の間のどこでもよく、40である理由が無い** |
| **c 章あたり初出** | **7** | **14** | **10** | 上に3・下に4。**ほぼ中点** |

  b の40は2点の間に置いただけの線で、**ショートの分布(2〜14)からは導けない**。
  c の10は L001(7)と L002(14)のほぼ中点にあり、
  **「1章はショート1本ぶんの数値負荷まで」という既存の設計語と数字が合う**。
  **c を主、b を従(あるいは b は WARN)にするほうが、較正の理屈が通る。**
- **決定**: **鎖の本数は維持(1本)。ゲートに c を不合格条件として追加(変更)、
  b は WARN として追加(変更)**。
  `check_long.py` に「長尺: **章あたり初出数値 ≤ 10 → 不合格**」
  「長尺: 鎖の長さ > 40 → **WARN**」を足す。
  L001 は無修正で通る(最大7・長さ24)。これは**退行よけ**であって、L001 を直す変更ではない。
  【検証側で変更】元は b も c も不合格条件にしていたが、
  **b の閾値40はショートの実測分布(2〜14)からも L001/L002 の2点からも導けない**
  一方、**c の10は L001(7)と L002(14)のほぼ中点にある**。
  較正の根拠が違うものを同じ強さで置くべきではないので、**b を WARN に落とした。**
  また、この2つは同じトークナイザ(`check_ikko.py` の `NUM_RUN` + `to_value`)で
  数えることを明記する — 別の数え方をすると値が合わない(C5-1 の【訂正】参照)。
- **根拠**: L002 の47連鎖が現行ゲートを素通りすることを実行して確認した。
  c の閾値10は、**L001 の最大7 と L002 の最大14 の間**に置いた
  (repo の `02-calibration.md` が取った較正法と同じやり方 = 合格作を落とさず、却下作を落とす)。
  b の閾値40には同じ較正が無いので、WARN に留める。
- **確度**: **A**(素通りの事実・各数値は自前測定。検証側で再実行して一致)。
  **ただし閾値の較正は n=2 である**と明記する。
  さらに訂正後の実測では、ショートの鎖の長さは 2〜14 と幅が広く、
  **「40」という上限はショートの分布からは一切導けない**。L001(24)と L002(47)の
  2点の中間に置いただけの線である。この点は元の文書より弱く書き直した。

---

## C5-2 1文の初出数値(現状1個まで)

- **現状(測定値)**: 113文を正規表現で数値トークンに分解し、初出かどうかを追跡した。
  - 1文あたりの**初出**数値: **0個=77文 / 1個=33文 / 2個=3文**。**3個以上は0文**
    - 【訂正】検証側で数え直すと **0個=84 / 1個=27 / 2個=2**(合計113)。
      数値トークンの総数のほうは 0個=43 / 1個=54 / 2個=15 / 3個=1 で、
      **0個=43 が完全一致**しているので分解の仕方はほぼ同じ。ずれているのは
      「同じ数値とみなす範囲」(`15万9370円` を1トークンとするか2つに割るか等)。
      **分布の細部は数え方に依存するので、この3つの数字を根拠に使ってはいけない。**
      両方の数え方で一致しているのは **「3個以上は0文」** と
      **「1文1初出はほぼ完全に守られている(111〜112/113)」** の2点だけで、
      決定(維持)はその2点だけで足りる
  - 2個の3文: #22「15万9370円」(15万+9370円で2トークンに割れただけ・実質1個)、
    #46「1年目に、40万円」、#101「2026年8月」(時点表記)
  - **実質的に「1文1初出」は113文中112文で守られている(99%)**
  - 参考: 1文あたりの数値トークン総数(既出を含む)は 0個=43 / 1個=53 / 2個=16 / 3個=1
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持(1文1初出) | 既出の再掲は何個でも可 | 現行。実測99%遵守 | 守れている。追加コストなし | 既出の再掲が1文3個まで起きている(#59) |
| b 総数も1文2個まで | 既出も含めて制限 | — | 文が短くなる | #59「2年目の利益40万円に、81260円かかる」は**割ると鎖が切れる**。副作用が大きい |
| c 制限を緩める(2個まで) | 尺を詰めるため | — | 尺短縮 | 守れているものを緩める理由が無い |
- **比較**: b を当てると落ちるのは1文だけ(#59)。その1文は「2年目・40万円・81260円」で、
  3つとも既出(2年目=#47、40万円=#46、81260円は#59が初出)。**初出は1個**。
  つまり b は**1文しか捕まえられず、その1文は割ると因果が切れる**。効果 < 副作用。
- **決定**: **維持**(1文1初出。既出の再掲は制限しない)。
- **根拠**: 現行運用が99%守られており(実測)、追加の制限で捕まる文が1本だけで、
  その1本は分割すると鎖が切れる。**測って、変える理由が無いと分かった**ケース。
- **確度**: **A**(全113文を機械的に数えた)。ただし上の【訂正】のとおり、
  **分布の細部はトークナイザ依存**で再現しなかった。A と呼べるのは
  「3個以上は0文」「1文1初出がほぼ守られている」の2点。

---

## C5-3 数値の粒度(現状 円単位まで出す。40,630円)

- **現状(測定値)**:
  - 端数を持つ数値の読み上げは **26回・合計50.5秒 = 尺の12.6%**
    (40630円×14 / 81260円×3 / 20315円×3 / 8126円×1 / 15万9370円×1 / 20.315%×3
    / **0.20315×1**)
    - 【訂正】元は **25回・48.6秒** だった。**#107「小さいほうに、0.20315をかけるのよ」の
      `0.20315` を数え落としている**(VOICEVOX 実測で 1.920秒 / 14モーラ、
      読みは `レ'エテン/ニイゼ'ロ/サンイ'チ/ゴ'オ`)。
      これは**この動画でいちばん耳で追いにくい数値**なので、落としてはいけない側だった。
      内訳の各回数(14/3/3/1/1/3)は**すべて再現できた**
  - VOICEVOX(speaker=3, speedScale=1.15)で実際に合成して長さを測った:
    `40630円` = **1.877秒 / 14モーラ**、`約4万円` = **1.173秒 / 8モーラ**(差 0.70秒)
  - **全部を概数に置き換えると 50.5秒 → 約29.7秒。約20.8秒(尺の5.2%)短くなる**
    (元の「48.6→28.6秒 / −19.9秒 / −5.0%」に 0.20315 の1回ぶんを足した値。
    ただし**置き換え後の言い回しを何にするかで変わる推定値**であって、
    実測なのは「40630円=1.877秒 / 約4万円=1.173秒 / 差 0.704秒」の1組だけである。
    この0.704秒は検証側でも同じ値を再現した)
  - 概数表現(約/ほど/ぐらい/およそ)を含む文は **113文中1文だけ**(#81「小さいほうの2割ほど」)
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持: 全部円単位 | 40,630円 | 現行。`verify.py` の出力と一致 | 検算可能・「測って出した」感 | 25回×約1.9秒。耳で追えているかは未検証 |
| b 全部概数 | 約4万円 | 検索スニペット: 予防行動の研究では**丸めた数のほうが行動につながった**という報告 | 19.9秒短縮・記憶しやすい | 「独自計算」という差別化(strategy.md §8)が薄まる |
| c 初出だけ正確値、再掲は概数 | 1回目「40,630円」→ 以降「約4万円」 | — | 正確さと軽さの両取り。14回中13回を軽くできる | 同じものを2つの呼び名で呼ぶと鎖が切れて見える危険 |
| d 画面は正確値、口は概数 | 画面「40,630円」/ 声「約4万円」 | repo の既存規則「画面は道標、文はナレーション」 | 耳は軽く、目に正確値が残る | 画面と声が食い違う。ラジオテスト(音だけで通じるか)に落ちる |

- **比較(同じ指標で)**:

| 案 | 端数読み上げの合計秒 | 尺への影響 | 「独自計算」の見え方 |
|---|---|---|---|
| a | 48.6秒 | ±0 | 強い |
| b | 28.6秒 | −19.9秒(−5.0%) | 弱い |
| c | 約31.4秒(初出6回のみ正確値) | −17.2秒(−4.3%) | 中 |
| d | 28.6秒(声のみ) | −19.9秒 | 強い(画面に残る) |

- **決定**: **A/Bテスト送り(現状は維持)**。
- **根拠**: **外部の定石が割れている。** 検索で出てきた方向は正反対だった。
  - 「精密な数は測定を連想させ、信用される」(Zhang & Schwarz 系、広告のsharp/round研究)
  - 「丸めた数のほうが予防行動につながった」(健康行動の研究)
  - さらに引っかかる但し書きがスニペットにあった。ただし**元の書き方は言い過ぎだった**。
    - 元: 「伝え手が人間でない場合、精密さの効果は消える」→ うちは合成音声だから効かない
    - 【訂正】検証側で引き直したスニペットの言い方は
      **「communicative intent を欠く自動処理(automated procedure)の結果として
      提示された場合には、精密さは効かない」**である。境界条件は
      **「機械の声かどうか」ではなく「その数に伝えたい意図があると受け取れるかどうか」**。
      うちの数値は**人間が書いた台本**を合成音声が読み上げているので、
      この境界条件にそのまま当てはまるとは言えない。
    - あわせて出てきた別の境界条件: **出し手の信用度が低いと精密さは効かない**
      (「企業の研究責任者」では効き、「意見サイト」では効かなかった)。
      これはむしろ**チャンネルの信用を先に作れば精密値が効く側**の話で、
      元の文書が引いた向きとは逆に働く。
    - どちらにせよ**本文は読めていない**(`dornsife.usc.edu` の PDF は `EGRESS_BLOCKED` を再確認)。
      **効果の向きが確定しないという結論は変わらないので、決定(A/Bテスト送り)は維持する。**
  **これらは全部スニペットで、本文は読めていない**(PDFは `EGRESS_BLOCKED` を実測)。
  効果の向きも大きさも自分で確かめられない。**確度Cなので変えてはいけない。**
- **確度**: **C**(外部知見)。ただし**時間コスト(約20.8秒・5.2%)は確度A**
  (ただし置き換え文言に依存する推定であることは上記のとおり)。

---

## C5-4 概数と正確値の使い分け

- **現状(測定値)**: **使い分けの規則は事実上存在しない。**
  113文のうち概数表現は1文(0.9%)。残りは全部、円単位の正確値か、
  もともと切りのよい仮定値(20万円・40万円)である。
  `docs/research/` を `鎖|数値|数字|初出` で検索しても、粒度の規則を書いた行は無かった。
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状(規則なし) | 書き手の勘 | 現行 | — | 次の書き手が再現できない |
| b 役割で分ける | **仮定値は切りのよい数(20万円)/ 計算結果は正確値(40,630円)** | L001 が結果的にこうなっている(実測: 仮定値は全部万円単位、結果は全部円単位) | 既にそうなっているので追加コスト0。「自分が入れた数」と「出てきた数」が耳で区別できる | 明文化しても検証はできていない |
| c 桁で分ける | 万円以上は概数、万円未満は正確値 | — | 単純 | 40,630円が「約4万円」になり、この動画の答えが消える |

- **比較**: b は**すでに起きている構造を言語化するだけ**で、L001 の1文も変わらない。
  実測で確認した: 仮定値として置かれた数(20万円・40万円・30万円・10万円・4万円・12万円・
  100万円・80万円)は**8個すべて万円単位**。計算結果として出た数(40630・81260・20315・8126・
  159370)は**5個すべて円単位**。例外は0個。
- **決定**: **変更(規則を明文化)**。
  「**入力(仮定)は切りのよい数。出力(計算結果)は円単位の正確値。混ぜない**」を
  `docs/research/longform-design.md` と `sakubun-gensoku.md` に書く。数値そのものは変えない。
- **根拠**: L001 の13個の数値すべてがこの規則に一致していることを実測で確認した(例外0)。
  **検証側でも独立に再現できた**: 字幕に出る金額トークンを全部拾うと
  切りのよい万円が `4万/10万/12万/20万/30万/40万/80万/100万` の**8個**、
  円単位が `8126/20315/40630/81260/(15万)9370` の**5個**、
  **両者が混ざった表記(例: 4万630円)は0個**。
  暗黙のうちに守られている規則を、次の書き手が再現できるように書き出すだけの変更である。
  **粒度そのもの(C5-3)は確度Cなので変えない。ここで変えるのは文書だけ。**
- **確度**: **A**(13個の分類は自分で数えた)。

---

## C5-5 単位の表記(円/万円の切り替え)

- **現状(測定値)**: **画面と字幕で表記が食い違っている。** これは推測ではなく、
  `ffmpeg -ss 14.5` で抜いた**同一フレーム**で確認した:
  - 図のラベル: **`税 40,630円`**(カンマあり)
  - 同じフレームの字幕: **`そのもうけには、ふつう40630円の税金がかかるの。`**(カンマなし)
  - 373秒の表フレームでも同じ(表`40,630円` / 字幕`40630円`)
  - 棒グラフのラベルは **`益20万` `損20万`**(円が落ちている)。ナレーションは「20万円」
  - `render.py` 内でカンマ付きなのは `40,630円 / 81,260円 / 差 20,315円 / 得 40,630円 /
    損 40,630円 / 税 40,630円 / 税 81,260円` の7文字列。字幕側は0
  - 全リポジトリ集計: カンマ無しの4桁以上「円」表記は **58箇所 / 10本**。
    **L001 が21箇所で最多**
    - 【検証】**完全に再現できた。** 数え方は「`render.py` の `UNITS[].subtitle` の中で、
      直前が数字・カンマ・万千百でない4桁以上の数字+円」。この定義で
      **58箇所 / 10本、L001=21** がぴたりと出る(L001 で `15万9370円` の `9370円` を
      除外すると21、含めると22)。次点は S018/S020 の6箇所。
      L002 は**この規則では0箇所**(金額が全部「万円」形)なので、
      元の「L002を除く」という但し書きは不要だった
  - `verify.py` の出力は全部カンマ付き(`40,630円`)。**内側の正はカンマ付きである**
- **「なぜカンマを外したのか」を潰した(確度A)**: VOICEVOX `/audio_query` の `kana` を比較した。

| 表記 | 読み | モーラ |
|---|---|---|
| `40630円` | ヨンマ'ン/ロッピャク'/サンジュウ'エン | 14 |
| `40,630円` | ヨンマ'ン/ロッピャク'/サンジュウ'エン | 14 |
| `159,370円` | ジュウ'/ゴマ'ン/キュウセ'ン/サ'ンビャク/ナナジュウ'エン | 19 |
| `15万9370円` | (同上) | 19 |

  **カンマを入れても読みは1モーラも変わらない。TTS 上の理由は存在しない。**
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持 | 画面カンマあり / 字幕なし | 現行 | 変更なし | 同一フレーム内で同じ数が2通りに見える(実測) |
| b 字幕もカンマ付きに統一 | `40,630円` | `verify.py` の出力と一致。TTSコスト0(実測) | 画面と字幕が一致。桁が読み取りやすい | 字幕が1文字長くなる(40630円→40,630円で+1字) |
| c 画面のカンマを外す | `40630円` | — | 統一はされる | 5桁を区切り無しで読ませる。表の桁揃えが崩れる |
| d 全部「万円」に寄せる | `4万630円` | — | 日本語として自然 | 読みは14モーラで変わらず(実測)。答えの `40,630円` の見た目が弱る |

- **比較**: b のコストは字幕1文字ぶんの幅だけ。TTS は0(実測で kana 完全一致)。
  効果は **L001 の21箇所 + 他9本の37箇所 = 58箇所の不一致解消**。
  c は表の桁揃えを壊すので却下。d は読み上げ時間が同じ(1.877秒)で得が無い。
- 【追加・反対側の材料】 d(万円に寄せる)を「得が無い」と切ったのは**TTSの理由だけ**である。
  検索で出てきた日本語表記の議論では、**日本語は4桁ごとに単位が変わる(万・億・兆)ため、
  3桁カンマとは相性が悪く、4桁ごとに漢字の単位を入れるほうが読みやすい**という指摘がある
  (確度C・スニペット2件)。ただし
  **この動画の答えの見た目が `4万630円` になるのは弱い**という元の判断と、
  **`verify.py` の出力・図のラベル・概要欄がすべて既にカンマ形**という内部の事実のほうが強い。
  **b を採るのは「外部の定石に従うから」ではなく「内部の正(`verify.py`)に字幕を合わせるから」**
  である。この根拠の置き換えは重要で、外部知見(確度C)で決めた変更ではなくなる。
- **決定**: **変更 — 字幕の4桁以上の金額をカンマ付きに統一する**
  (`40630円` → `40,630円`)。**あわせて図のラベルの「万」表記に円を付ける**
  (`益20万` → `益20万円`)。対象は L001 の21箇所を含む全58箇所。
  機械ゲートに「字幕の4桁以上の数値+円にカンマが無ければ不合格」を足す。
- 【追加・この変更は単独ではゲートを壊す(確度A・実際に走らせて確認)】
  L001 のコピーを作り、21箇所を機械的にカンマ付きに直して全ゲートを走らせた。結果:

| ゲート | 結果 |
|---|---|
| `check_ikko` / `check_bunsho` / `check_goi` / `check_ryakugo` / `check_yomi` / `check_teinei` / `check_video` | 変化なし(通る) |
| **`check_long`** | **不合格1件「離脱の山の切れ目 — 45〜75秒に章の切れ目 ['45.2s']」** |

  原因は**カンマそのものではなく、字数の数え方**である。`check_long.py` は
  `len(re.sub(r"[。、【】]", "", u.tts_text()))` で字数を数えており、
  **半角カンマを1字として数える**。21個のカンマが21字ぶん尺の推定を伸ばし、
  章の切れ目の推定秒が 43.2 → 45.2 に動いて不合格線に入った。
  **実際の読み上げ時間は1モーラも変わらない**(kana完全一致を実測済み)ので、これは純粋な誤検知である。

  **同時に直すこと**: `production/check_long.py` の **81行目(`est`)と166行目(`chars`)**の
  `[。、【】]` を **`[。、,【】]`** にする。これを当てたうえで再実行すると
  **ALL PASS に戻ることを確認した**。`check_video.py` の推定尺も同じ理由で影響を受けるので、
  あわせて確認すること。
  **この同時修正が無いまま58箇所に手を入れると、長尺のゲートが理由なく赤くなる。**
- **根拠**: 同一フレームでの不一致をフレーム抽出で目視確認し、
  カンマ追加のTTSコストが0であることを VOICEVOX の kana 比較で確認した。
  **失うものが測定上ゼロで、直すものが58箇所ある。**
- **確度**: **A**(フレーム測定 + TTS実測。検証側で 14.5秒のフレームを再抽出し、
  図ラベル `税 40,630円` と字幕 `40630円` が**同一フレームに同時に出ている**こと、
  棒ラベルが `益20万` `損20万` で円が落ちていることを目視で再確認した。
  VOICEVOX の kana・モーラ数・合成長も `40630円` と `40,630円` で完全一致を再現)。
  ただし**どちらに寄せるか**(b か c か d か)は測定ではなく
  「内部の正(`verify.py`・図・概要欄)に合わせる」という判断である。

---

## C5-6 検算の見せ方(verify.pyの結果を画面に出すか)

- **現状(測定値)**:
  - `python3 videos/L001-nisa-son/verify.py` は12行の検算を出力し `ALL OK` で終わる(実行して確認)
  - **その結果は動画のどこにも出ていない。** 113ユニットのシーン定義を全部見たが検算の画面は無い
  - 代わりに**概要欄に**「・動画内の金額はすべて **verify.py** で再計算しています」と書いてある
  - この「verify.py」という**ファイル名を概要欄に書いている動画は 20/20本**
    - 【訂正】元は **21/30本**。`videos/` 直下の `script_meta.json` は**20個しか無い**
      (S001〜S010・S024〜S026 の13本には `script_meta.json` が存在しない)。
      その20個は**全部**に `verify.py` の文字列が入っている。
      つまり正しくは **20/20本(100%)**で、**直す対象は21本ではなく20本**。
      分母30は `check_ikko.py` が走る本数と取り違えたものと思われる
  - `render.py` の冒頭には `assert zei(200_000) == 40_630` 等の
    **5本**の数値 assert(49〜53行)があり、
    **レンダリング時に検算が走る仕組みにはなっている**(コードを読んで確認)
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持 | 画面に出さず、概要欄にファイル名 | 現行 | 変更なし | **視聴者に「verify.py」は意味を持たない**。独自性の主張が伝わらない |
| b 概要欄の文言だけ直す | 「動画内の金額は、別に組んだ計算プログラムで全件を再計算し、一致を確認しています」 | strategy.md §8「独自計算」を独自性要素として数える方針 | 意味が伝わる。動画は1フレームも変わらない | 検証はできない |
| c 画面に検算の1枚を入れる | 締めの前に「20万円 × 20.315% = 40,630円」の1枚 | 競合の「計算過程を見せる」型 | 「自分で計算した」が目に見える | repo の規則「図に文を貼らない(冗長性)」と衝突。尺が伸びる |
| d 概要欄に検算結果を全文貼る | verify.py の12行をそのまま | — | コストほぼ0 | 概要欄が長くなり、上位3行の勝負に不利 |

- **比較**: b は動画に一切触れずに 21本の概要欄を直せる。c は L001 でおよそ +4秒/1ユニット、
  かつ「図に文を貼らない」という既存規則との衝突を自分で作る。
- **決定**: **b は変更、c は A/Bテスト送り。**
  - **変更**: 概要欄の「verify.py で再計算しています」を、ファイル名を出さない日本語に置き換える(**20本**)
  - **A/Bテスト送り**: 検算の1枚を画面に入れるかどうか
- **根拠**: b は「視聴者にとって無意味な開発者用語が概要欄に**20本ぶん(=ある全部)**載っている」という
  **実測された事実**への対応で、動画側のリスクがゼロ。
  c は効果を測る手段がこの環境に無く(長尺の維持率データが1本も無い)、確度Cなので変えない。
- **確度**: **A**(**20本**の集計・`verify.py` の実行。検証側で
  `python3 videos/L001-nisa-son/verify.py` を再実行し、12行の検算 + `ALL OK` を確認)。
  c の効果は**C**。

---

## C6-1 締めの型(現状 ループ型+次動画の名指し)

- **現状(測定値)**:
  - L001 の最終行 = #112「売る損得の判断に、税金は持ちこまなくていいのだ。」(396.6秒〜)
  - 冒頭 #0「株で損したら、税金はどうなるのか?」との**文字の重なりは21%**
  - **締めが独立ブロックになっていない**。最後の20秒のフレーム(395秒)を抜いて確認したところ、
    左上の章チップは**まだ「第4章 自分の場合」**。
    `longform-design.md` §3 は「**締めは独立ブロック。章の中に埋めない**」と書いている。**違反している**
  - 公開13本で「冒頭と締めの文字の重なり」と「平均視聴率」の関係を計算した:
    **r = 0.543 / t = 2.15 / df = 11**(両側5%の臨界値は 2.201。**有意ではない**、p≈0.055)
  - しかも中身を見ると交絡している。**検証側で再現し、交絡は元の記述より強かった**:
    - 重なりを「冒頭行と最終行の文字集合の Jaccard」と定義すると **r=+0.542 / t=+2.14 / df=11**
      (元の r=0.543 / t=2.15 とほぼ一致。**両側5%の臨界値 2.201 に届かない**)。
      定義を「|A∩B| / |B|」にすると r=+0.570 / t=+2.30 で**符号は同じだが5%を超えてしまう**。
      **定義を1つ変えるだけで有意/非有意がひっくり返る指標**であり、この時点で使えない
    - 【訂正】交絡は3本ではなく **13本中8本**。重なりが厳密に0.00 なのは
      **S005・S001・S008 に加えて S004・S007・S002・S009・S003 相当の「【数字】。」型冒頭**で、
      共通点は「冒頭が裸の数字だから、締めと共有する文字が構造的に無い」こと。
      **13本中8本が同じ値(0)に張り付いている**ので、
      相関を動かしているのは実質**残り5本の並び**だけである。
      この指標は**冒頭の型**を測ってしまっていて、締めの型だけを測れていない
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状(ループ型+次の名指し) | 冒頭の問いに答えて閉じ、次を名指す | 現行 | 周回が起きうる(S015 が48秒尺で平均45秒) | 長尺で周回は起きにくい |
| b 決定を促して閉じる | 「◯◯を見てほしいのだ」 | ショート20本中18本がこの型(実測) | 「視聴後に決められること」(CLAUDE.md)と直結 | 長尺での効果は未測定 |
| c 締めを独立ブロックにする | 章チップを消し、締め専用の画面に切り替える | `longform-design.md` §3 が既に指定している | 終了画面の場所が空く(C6-4と同じ利益) | 実装が要る |
| d 次の動画への橋渡しで閉じる | 「この続きは◯◯で」 | 検索スニペット: 終了画面+関連動画でセッション視聴時間が伸びる | セッション視聴時間 | 満足せずに終わる感じが出る |

- **比較**: a〜d のうち、**この環境で数値比較できたのは a だけ**(上の r=0.543、有意でない)。
  長尺の維持率データは**1本も存在しない**(`demand-2026-08.md` / `hold-rate-2026-08.md` を検索して確認)。
  c だけは効果の話ではなく**自分の設計文書との不一致**なので、別扱いにできる。
- **決定**: **締めの型そのものは A/Bテスト送り(現状維持)。ただし c は変更。**
  - 変更: 最後の約20秒を**締めブロックとして章から出す**(章チップを消す)。
    `check_long.py` に「最後の20秒に章チップが残っていたら不合格」を足す
  - 【追加・実装の注意(確度A)】`check_long.py` の章の検出は
    **`u.scene` が `ch\d+` に完全一致するか**で行っている(`heads`)。
    一方、画面に出ている章チップは `render.py` の **`BANDS`**
    (L001 は `[(0,'今日の問い'), (12,'第1章 引けない損'), (43,…), (65,…), (82,'第4章 自分の場合')]`)が描いている。
    **この2つは別物**なので、「最後の20秒に章チップが残っていたら不合格」は
    `heads` ではなく **`BANDS` を見る判定として書かないと空振りする**。
    また、締めを非 `ch` シーンにしても `heads` は増えないので、
    **章の尺の計算では締めが4章に足されたままになる**(L001 の4章116.5秒には締めの約16秒が含まれている)。
    C7-2 の章の尺ゲートを締めるなら、ここも同時に決めないと数字が合わなくなる
  - A/Bテスト送り: ループ型 vs 決定促し型
- **根拠**: ループ度と視聴率の相関は t=2.15 で 5% に届かず、しかも指標が冒頭の型と交絡している。
  **確度Cなので締めの型は変えない。** 一方 c は `longform-design.md` §3 に既に書かれた規則の
  未実装であり、フレーム抽出で違反を確認した(確度A)。
- **確度**: 締めの型 = **C**(n=13・有意でない・**定義次第で有意/非有意が入れ替わる**・
  13本中8本が0に張り付く交絡あり) / 章チップの残存 = **A**
  (検証側で 395秒のフレームを再抽出し、左上のチップが `第4章 自分の場合` のままであることを目視確認)。

---

## C6-2 免責の位置(現状 最後20秒を避ける)

- **現状(測定値)**: 免責・時点表記は2文だけで、位置は
  - #101 `354.0秒`(終了まで **46.7秒**)「ちなみにこの制度は、2026年8月時点の内容なの。」
  - #102 `358.0秒`(終了まで **42.7秒**)「制度は毎年変わるから、見る時期に応じて確かめてね。」
  - 合計 **7.1秒**(ナレーションのみ。止め(pad)込みなら 7.36秒)。
    `check_long.py` の「終了画面の枠(最後20秒)」は**通っている**
  - ただし直後の #103(361.4秒)が「では3つの場合を、表にまとめるわよ」= **まとめ表の入口**。
    つまり免責は**山場の直前に7.1秒割り込んでいる**
  - 画面には全編を通じて右上に `2026年8月時点・仮定の金額での計算` のバッジが常時出ている
    (抽出した5枚すべてで確認)。**時点表記は画面で常時担保されている**
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状(締めの手前・音声7.1秒) | 42.7〜46.7秒前 | 現行 | ゲートを通る | まとめ表への助走を切る |
| b 冒頭に移す | 0〜40秒に置く | — | 山場を切らない | `longform-design.md` §2「定義を冒頭に置かない」と衝突。**採らない** |
| c 章の継ぎ目に移す | 4章の入口(284秒)に置く | 継ぎ目は既に話題が切れている | 山場を切らない。ゲートも通る | 4章の入口は「自分の場合の出し方」の予告で、そこも山場 |
| d 音声から外し画面バッジだけにする | 常時バッジ+概要欄 | バッジは実測で全編表示 | 7.1秒の尺が浮く | **コンプライアンス要件(CLAUDE.md §5「時点表記を入れる」)を音声で満たさなくなる**。採らない |

- **比較**: a と c の差は「まとめ表の直前を切るか、4章の入口を切るか」の7.1秒。
  どちらが維持率に効くかを測る手段が無い(長尺の維持率データ0本)。
- **決定**: **維持**。
- **根拠**: ゲートを通っていること・画面バッジで時点表記が常時担保されていることは実測。
  移動先の優劣を決める測定手段がこの環境に無い。**動かす根拠が無いので動かさない。**
  ただし C6-4 で見つかった「最後20秒に立ち絵が乗っている」問題のほうが、
  終了画面の実害としては大きい(そちらは変更する)。
- **確度**: **A**(位置は自前測定。検証側で #101=354.00秒(終了まで46.70秒)・
  #102=357.96秒(同42.74秒)・#103=361.36秒 を再現。バッジの常時表示も
  抽出した全フレーム(14.5 / 373 / 385 / 390 / 395 / 399秒)で確認)。移動の是非は**測れなかった**。

---

## C6-3 CTA(現状 コメントの問いかけ)

- **現状(測定値)**: **前提が事実と違っていた。**
  - **本編ナレーションに口頭CTAは1本も無い。** 公開30本+L001/L002 の全ナレーションを
    「コメント」で全文検索して**ヒット0**
  - CTA は `script_meta.json` の `cta`(概要欄の末尾)と固定コメントにだけ存在する
  - 20本の `cta` を並べた結果:
    - **「コメントで教えてください」で終わるのが 16/20本**
    - **欠けているのが L001「あなたは、NISAと課税口座の両方を使っていますか?」・
      L002・S012・S027 の4本**。長尺2本が両方とも欠けている
    - 【検証】**16/20 と欠けている4本の内訳まで完全に再現できた。**
      `script_meta.json` は20個で、`cta` が空のものは0個、
      「コメントで教えて」で終わらないのが L001 / L002 / S012 / S027 の4本
    - 【訂正】4本を一律に「Yes/Noで閉じる」と書いているが、実際に Yes/No なのは
      **L001・L002・S012 の3本**。S027 は
      「あなたなら何歳からもらうのがおすすめですか?理由も聞きたいです。」で、
      **開いた問い + 理由の要求**になっている。**S027 は型としては上位側**で、
      欠けているのは末尾の定型句だけである
  - L001 の CTA は **Yes/No で閉じる問い**。上位の型(S015「住民税はいくら引かれていますか?」=
    数字を答えさせる / S013「変動と固定、どっちを選びますか?」= 選ばせる)と形が違う
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持 | 概要欄のみ・Yes/No の問い | 現行 | — | 他18本と揃っていない。答えが1語で終わる |
| b 概要欄CTAを他本と揃える | 「あなたのNISA口座、去年は損と利益どちらでしたか?コメントで教えてください。」 | 16/20本の既存型 | 揃う。開いた問いになる | 効果は未検証 |
| c 本編に口頭CTAを入れる | 締めで「コメントで教えてほしいのだ」 | 検索スニペット: 終了画面には口頭CTAを併せるのが定石 | 目と耳の両方 | **30本の実績が全部「口頭CTA無し」で取られている**。効果の切り分けができない |
| d CTAを外す | 問いかけを置かない | — | 尺が浮く | 初動のコメント数はアルゴリズム上の信号(確度C) |

- **比較**: b は**既に16本で使っている型に L001 を合わせるだけ**で、新しい賭けが無い。
  c は新要素の追加で、効果を測る前例が自チャンネルに0本。
- **決定**:
  - **変更: L001/L002/S012/S027 の概要欄CTAを、他16本と同じ「…?コメントで教えてください。」型にする。
    あわせて L001 の問いを Yes/No から「どちらでしたか」型に開く**
  - **A/Bテスト送り: 本編に口頭CTAを入れるかどうか**
- **根拠**: 「16/20本がこの型」は実測。揃えるだけの変更で、新しい仮定を持ち込まない。
  口頭CTA の効果は自チャンネルに前例が0本で、外部知見はスニペットのみ(確度C)。
- **確度**: 現状の測定 = **A**(検証側で全 `render.py` の字幕を
  「コメント」「チャンネル登録」「高評価」で全文検索し、**ヒット0**を再現。
  概要欄CTAの 16/20 も再現) / 口頭CTAの効果 = **C**。
  【検証】口頭CTAについて検索し直したが、出てくるのは
  **マーケティング会社のブログばかり**(nobledesktop / zebracat / lemonlight / vidiq / hypedx 等)で、
  内容は「音声CTAと画面CTAを併用するほうが効く」で揃っている。
  だが `research-standards.md` の「相互コピペのベンダーブログ群は1本と数える」に従えば
  **これは独立1本**であり、**Bには届かない。確度C・A/Bテスト送りのままが正しい。**

---

## C6-4 次の動画の誘導(現状 ショート版を名指し)

- **現状(測定値)**: **名指しているのに、指し先が存在しない。**
  - #110 `387.9秒`(終了まで12.8秒)「税金の20.315%は、**概要欄のショート版**でも見られるのだ。」
  - #111 `393.3秒`「ショート版なら、利益と手取りの図だけをさっと見られるわ。」
  - **`script_meta.json` の `description` を検索した結果: URL は0件、「ショート」の語は0件、
    ショートのIDへの言及も0件。** 概要欄にショート版へのリンクは無い
  - 画面(395秒のフレーム)には「1分で見たいとき / 税金の20.315% / ※忙しい人向け」のカードが出るが、
    **どの動画かは名前で書かれていない**
  - `check_long.py` の「次に見るものの名指し」は**通っている**(正規表現が「概要欄」に当たるため)。
    **ゲートが名指しの実在を検証していない**
  - 終了画面の枠(最後20秒)の占有率を4フレームで測った(背景輝度37からの逸脱面積):
    全体14.1〜14.8%。象限別に **左下16.3〜18.0% / 右下16.0〜17.0% / 右上7.0〜8.5%**。
    左下はずんだもんの立ち絵、右下は四国めたんの立ち絵。**終了画面の要素が置かれる下側が両方とも埋まっている**
    - 【検証】**完全に再現できた。** 385/390/395/399秒の4フレームで
      背景輝度の最頻値が **37**(全フレームで一致)、判定を **|L−37| > 25** としたとき
      全体 14.1/14.8/14.6/14.4%、左下 16.3〜18.0%、右下 16.0〜17.0%、右上 7.0〜8.5% と
      **表の数字がそのまま出る**。元の文書に閾値が書かれていなかったので、ここに残す
    - 【追加・元の文書が落としている象限(確度A)】**左上も 15.1〜18.0% 埋まっている。**
      中身は**章チップ + カードの見出し**で、立ち絵ではない。
      したがって**立ち絵を消しても左上は 11.0〜12.3% までしか下がらない**。
      YouTube の終了画面の要素は上側にも置けるので、
      **「下側だけが埋まっている」という元の記述は不正確**で、
      空いているのは実質**右上だけ**である。
      左上を空けるには C6-1 の「締めの20秒で章チップを消す」が要る
      — **C6-1c と C6-4d は同じ問題の別の面**であり、両方やって初めて枠が空く
  - 外部仕様(確度B): 終了画面は**動画の最後5〜20秒**に置け、要素は**最大4つ**、
    **動画は25秒以上**が必要。検索で複数の独立ソース(YouTubeヘルプのタイトル、制作会社系ブログ複数)が一致
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持 | 「概要欄のショート版」と言う / リンク無し | 現行 | — | **言った先が無い。視聴者が概要欄を見て裏切られる** |
| b 概要欄にリンクを足す | 説明文の先頭にショート版のURLを1行 | — | 言葉と実体が一致する | 概要欄の上位行を1行使う |
| c 動画名で名指す | 「『利益171万円、税金でいくら消える?』で見られる」 | `check_long.py` の意図(「名前と、見ると何が分かるかを言う」) | 終了画面のカードと一致させられる | ショート側のタイトルに依存 |
| d 最後20秒の立ち絵を消す | `chara="none"` | repo の既存規則「図が主役のユニットは立ち絵を消す」の延長 | 終了画面の4要素が置ける | 締めが寂しくなる |

- **比較**: b+c+d は競合しない。コストはそれぞれ、概要欄1行 / 台本2文の書き換え /
  `render.py` の締め4ユニットに `chara="none"` を足すだけ。
  d の効果は測定済み。【検証】立ち絵の領域
  (左 x=60〜400 / y=380〜900、右 x=1560〜1920 / y=350〜900)をマスクして測り直すと、
  **左下 16.3〜18.0% → 6.4〜7.7% / 右下 16.0〜17.0% → 4.6〜5.4%**。
  元の「7〜8%台に落ちる」はおおむね正しく、**右下はさらに下がる**。
  ただし残るのは字幕帯の文字なので、これ以上は下がらない。
- **決定**: **変更(3点とも)**。
  1. 概要欄にショート版のリンクを入れる(あるいは、無いなら音声から「概要欄の」を外す)
  2. 名指しを「概要欄の」から**動画のタイトル**に変える
  3. 最後20秒のユニットを `chara="none"` にして終了画面の枠を空ける
  4. `check_long.py` の「次に見るものの名指し」を、**`script_meta.json` の description に
     その名前かURLが実在するかまで確認する**判定に変える
- **根拠**: 「言っているのに概要欄に無い」は description を機械検索して確認した(URL 0件・「ショート」0件)。
  終了画面の占有率はフレームの画素を数えた。終了画面の仕様(5〜20秒・最大4要素)は
  検索スニペット3件以上の一致。**本文は読めていない**(support.google.com は取得できない)。
- **確度**: 不一致と占有率 = **A**(検証側で `script_meta.json` の `description` を
  再検索し、**URL 0件 / 「ショート」0件 / ショートのID 0件**を再現。
  占有率も4フレームで再現し、閾値と立ち絵マスクの効果を追加測定した) /
  終了画面の仕様 = **B**(検証側でも検索し直し、
  「最後の5〜20秒」「最大4要素」「25秒以上の動画」で
  日本語の独立ソース複数が一致することを再確認)。

---

## C7-1 長尺の尺(現状6分41秒)

- **現状(測定値)**: **400.704秒(6:40.7)**。8分(480.0秒)まで **79.3秒(+19.8%)足りない**。

**必須測定(1): 全数値の登場位置**(pad累積で算出。上の「測定の土台」の方法)

| 数値 | 初出 | そこまでの経過 |
|---|---|---|
| 20万円 | #1 | **3.06秒** |
| **40,630円(この動画の答え)** | #4 | **13.37秒** |
| 4つの章(全体量の予告) | #10 | 35.67秒 |
| 20.315% | #13 | 46.59秒 |
| 15万9370円 | #22 | 81.13秒 |
| 81,260円 | #59 | 202.50秒 |
| 20,315円 | #70 | 242.73秒 |
| 8,126円 | #88 | 306.10秒 |

  最初の数値 3.06秒(ゲート15秒以内)・最初の図 (ゲート20秒以内)・答えの金額 13.37秒。
  **冒頭のゲートは余裕を持って通っている。**
  【検証】**この8行の秒はすべて小数第2位まで再現できた**(pad込みの累積で算出)。

**必須測定(2): 8分の損得試算**

- ミッドロール広告は **8分(8:00)以上**が条件。7:59 では付かない(確度**B**:
  YouTubeヘルプのページタイトル + vidiq + socialmediatoday/digiday + 日本語制作会社ブログ複数が一致。
  **本文は読めていない**)。2020年に10分→8分に下がって以降、変更の報告は見つからなかった。
  【検証】検証側でも検索し直し、**2026年8月時点でも8分のまま**という記述で
  vidiq / digiday / socialmediatoday / smk / fluxnote / deepdatawithmivaa が一致した。
  「7:59では付かない、8:00なら付く」という書き方まで一致する。**B は妥当。**
- 収益側の試算(中間点の残存率を S とし、プレロール1枠は常時、ミッドロール1枠が増えるとする):

| 中間点の残存率 S | 広告表示/再生 | 収益倍率 | 損益分岐(再生数がここまで減っても元が取れる) |
|---|---|---|---|
| 20% | 1.20 | 1.20x | −16.7% |
| 30% | 1.30 | 1.30x | −23.1% |
| **40%** | **1.40** | **1.40x** | **−28.6%** |
| 50% | 1.50 | 1.50x | −33.3% |

- 維持率側のコスト(平均視聴**秒**が変わらないと仮定した場合の、平均視聴**率**の見かけの低下):

| 平均視聴秒 | 400.7秒での率 | 480秒での率 | 差 |
|---|---|---|---|
| 160秒 | 39.9% | 33.3% | **−6.6pt** |
| 200秒 | 49.9% | 41.7% | **−8.2pt** |
| 240秒 | 59.9% | 50.0% | **−9.9pt** |

- **自チャンネルの実測が、この試算のどちらを支持するか(必須測定3)**:
  `demand-2026-08.md` 追記4の14本(のべ1,200〜1,900回/本)で相関を計算した。

| 関係 | ピアソン r | スピアマン ρ | t(df=12) | 決定係数 |
|---|---|---|---|---|
| **尺 vs 平均視聴率** | **−0.302** | −0.239 | −1.10(有意でない) | R²=0.091 |
| **尺 vs 平均視聴秒** | **−0.061** | −0.093 | −0.21(有意でない) | R²=0.004 |
| 平均視聴秒 vs 平均視聴率 | **+0.968** | +0.964 | — | **R²=0.936** |

  50秒以下7本の平均視聴率 65.1% / 51秒以上7本 62.7%(差 2.4pt)。
  → **尺は視聴率の分散の9%しか説明しない(有意でない)。説明するのは平均視聴秒(94%)。**
  → **尺を伸ばしても、見られる秒数は増えも減りもしなかった**(r=−0.06、実質ゼロ)。
  ※ ただしこれは**ショートの話**であり、長尺に移せるかは分からない(確度C)。
  ※ 元の数値はユーザーから提供されたアナリティクスを `demand-2026-08.md` に転記したもの。
    **相関の計算は自分でやったが、元データを自分で再測定することはできない。**

- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持(6:41) | 内容が終わったら出す | `longform-design.md` §1「8分に届かせるために薄めない」 | 薄めない | ミッドロール1枠を捨てる |
| b 話速を落として8分に届かせる | `SPEED_SCALE` 1.15 → 約0.94 | — | **文章を1文字も足さずに届く** | 290字/分はゲートの下限ちょうど。余白ゼロ |
| c 話速を標準に戻す + 短い章を埋める | `SPEED_SCALE` 1.15 → 1.00、2章と3章を設計目標まで伸ばす | `longform-design.md` §3「1章100〜130秒」に**既に違反している**のを直すだけ | 薄めではなく設計違反の是正 | 台本の追加が要る |
| d 内容を足して9〜10分にする | 章を1つ足す | 競合(リベ大等)は10〜30分 | ミッドロール2枠 | L002 でやって失敗している(下の C7-2) |

- **比較(実測に基づく)**: VOICEVOX で実際に合成して、`speedScale` と長さの関係を測った
  (L001 の3文を各値で合成し wav のフレーム数を測定):

| speedScale | 合成した3文の長さ | 1.15を1としたときの比 | 理論値(1.15/sp) |
|---|---|---|---|
| 1.30 | 12.043秒 | 0.8834 | 0.8846 |
| **1.15(現行)** | 13.632秒 | 1.0000 | 1.0000 |
| **1.00** | 15.595秒 | **1.1440** | 1.1500 |
| 0.95 | 16.363秒 | 1.2003 | 1.2105 |
| 0.90 | 17.397秒 | 1.2762 | 1.2778 |

  【検証】検証側でも VOICEVOX(speaker=3)で3文を各値で合成し直した。
  **絶対秒は文の選び方で変わるが、比は 1%以内で再現した**
  (1.30→0.8868 / 1.00→**1.1481** / 0.95→1.2104 / 0.90→1.2726)。
  この比は**理論値 1.15/sp とほぼ同じ**なので、
  「`speedScale` は尺にほぼ厳密に反比例する」と扱ってよい(確度A)。

  これを L001 全体に当てると(ナレーション381.50秒だけが伸び、無音19.20秒は伸びない):

| speedScale | 全体の尺 | 話速 | ゲート(290〜360字/分) | 8分に対して |
|---|---|---|---|---|
| 1.15(現行) | 400.7秒(6:41) | 349字/分 | 通る(上限寄り) | −79.3秒 |
| **1.00** | **455.6秒(7:36)** | **307字/分** | **通る(帯の中央寄り)** | −24.4秒 |
| **0.95** | **481.0秒(8:01)** | **290字/分** | **下限ちょうど** | **±0** |

  【訂正】元は「約0.94 / 約481秒」と書いてあったが、これは理論値からの外挿。
  **実測の比を当てると 0.95 でちょうど 481.0秒 / 290字/分**になる(0.94ではない)。

  そして章の尺(speedScale 1.00 換算):冒頭49.1 / 1章121.1 / **2章84.4** / **3章68.5** / 4章132.6 秒。
  【検証】**この5つの値も 455.6秒という合計も、そのまま再現できた**
  (ナレーションだけを1.144倍し、無音19.2秒は伸ばさない計算)。
  **`longform-design.md` §3 が指定する「1章100〜130秒」に、2章と3章が届いていない。**
  現行の1.15でも 2章74.3秒 / **3章60.2秒** で、**3章はゲートの不合格線(60秒未満)まで0.2秒**である。

  → **8分に届かせるために足す79.3秒は、「薄めるための水増し」ではなく
     「設計目標に届いていない2つの章を埋める作業」で丸ごと吸収できる。**
     2章と3章を設計帯の中央(115秒)まで伸ばすと 532.8秒(8:52)になり、8分を通り越す。
     8:20(500秒)を狙うなら、speedScale 1.00 のうえで **+44.4秒 ≒ 226字 ≒ 11ユニット**を
     2章・3章に入れればよい(1ユニット平均20.6字・4.03秒の実測から算出)。

- **決定**: **変更 — 長尺の目標尺を「内容で決まる」から「500秒(8:20)前後」に置く。
  ただし届かせ方を限定する**:
  1. `SPEED_SCALE` を **1.15 → 1.00**(話速 349 → 307字/分。ゲート帯の中央に入る)
     - 【追加・同時に直さないと壊れる箇所(確度A・コードを読んで確認)】
       `production/check_long.py` の51行に
       **`SEC_PER_CHAR_LONG = 0.152 * (1.3 / 1.15)`** と **1.15 が直に書いてある**。
       これは「音声がまだ無い台本」の尺を字数から推定する係数で、
       `SPEED_SCALE` を 1.00 にしたら **`0.152 * (1.3 / 1.00)`** に直さないと、
       **新しく書いた台本の推定尺が約13%短く出る**。
       章の尺の下限(60秒 / C7-2の案では90秒)と話速の上限を
       **実際より通りやすく見せる**方向にずれるので、危険な側である。
       同じ理由で `production/shortlib.py` の `LONG_SPEED_SCALE = 1.15`(281行)と
       `check_video.py` の推定尺も見ること
     - 【追加・設計文書との衝突(確度A)】`docs/research/longform-design.md` §4 は
       **「長尺は標準の上限(約330〜345字/分)に置く」**と明記している。
       **307字/分はこの帯の外**なので、この決定を採るなら §4 も同時に書き換える必要がある。
       なお §4 の対応表は「`1.15 → 311字/分`」と書いているが、
       **L001 の実測は 349字/分**で、**この対応表自体が現物と合っていない**
       (ショート用の較正を持ち込んだまま直っていないと思われる)。
       検索でも日本語ナレーションの標準帯は **300〜350字/分**(NHKのニュース読みで約300、
       速い人で350)で一致しており、**307 は帯の中**。
       §4 の「上限に置く」という指定のほうが根拠が薄い
  2. 足りない分は **2章・3章を設計目標(100〜130秒)まで埋める**ことだけで埋める
  3. **章を増やして埋めることを禁じる**(L002 の失敗の再現になる)
  4. **L001 自身は現状(6:41)のまま出す。** これは A/Bテストの対照群にする
- **根拠**:
  - ミッドロール1枠は 8:00 が条件(確度B)。得は 1+S 倍で、S=0.4 なら再生数が28.6%落ちても元が取れる
  - **足す79.3秒が水増しにならないことを、章の尺の実測で示せる**(2章・3章が設計違反、確度A)
  - 話速の変換係数は VOICEVOX で実際に合成して測った(確度A。検証側でも比を再現)
  - **維持率への影響は測れない**(長尺のアナリティクスが0本)。だから維持率を理由にしていない。
    CLAUDE.md の「確度Cで行動を変えない」に従い、**変更の根拠を確度Bと確度Aだけで組んだ**
- **確度**: 尺・章の尺・話速変換 = **A**(検証側で登場位置8点・相関表3種・
  speedScale比・章の尺の1.00換算をすべて再現) / ミッドロールの8分条件 = **B**(再検索で確認) /
  8分化が維持率に与える影響 = **測定不能(C)。だから根拠に使っていない**
  【検証・相関表について】14本の r / ρ / t / R² は
  **−0.302 / −0.239 / −1.10 / 0.091**、**−0.061 / −0.093 / −0.21 / 0.004**、
  **+0.968 / +0.964 / 0.936**、および 65.1% vs 62.7%(差2.4pt)まで
  **すべて小数第3位まで一致した**。ただし元データがユーザー提供の転記である点は
  元の文書のとおりで、そこは検証できていない。

---

## C7-2 尺の下限を縛らない規則の是非

- **現状(測定値)**: `production/check_long.py` を全文読んだ。**総尺の下限も上限も存在しない。**
  縛っているのは間接的な2つだけ:
  - 章の尺 `60 <= span <= 150`(メッセージには「100〜130sが目安」と書いてあるが、**判定は60〜150**)
  - 章の数 `len(heads) <= 5`
  - → 機械的に許される総尺は **およそ4:40〜13:30**。実質的に何でも通る
  - 【訂正・重要】ただし **`check_long.py` だけを読んで「下限はどこにも無い」と結論したのは誤り。**
    `production/check_video.py` には **8分未満の WARN が2箇所ある**
    (154行: 推定尺 < 480 / 226行: 実尺 < 480)。実際に走らせると
    **`[WARN] 尺 — 6.7分(8分未満)`** が出る(検証側で実行して確認)。
    つまり `longform-design.md` §1 の
    **「いま8分の下限は WARN であって不合格ではない」は正しい記述**であり、
    下の選択肢 d「下限を WARN で戻す」は**すでに実装済み**である
- **この規則が置かれた経緯と、その後どうなったか(実測)**:
  `longform-design.md` §1 に、下限ゲートを入れた結果 L002 が「尺の理由だけで +89ユニット足された」と
  書かれている。**その L002 が今どうなっているかを実行して確かめた**:

| | L001 | L002 |
|---|---|---|
| 尺 | 400.7秒(6:41・**実測**) | 519.9秒(8:39・推定) |
| ユニット数 | 113 | **155** |
| 章 | 4 | **6** |
| `check_long.py` | **ALL PASS** | **不合格19件** + WARN 6件 |
| 図の割合 | **63%**(訂正。元は66%) | **34%** |
| 図なしの最長連続 | 25秒以下(合格) | **62.8秒** |
| 止め(pad≥0.5s) | 5箇所 | **0箇所** |
| 鎖の長さ | 24 | **47** |

  **8分を超えている唯一の長尺が、19件落ちている水増し版である。** これが下限ゲートの成果物。
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a 現状維持(下限なし) | 総尺は縛らない | 現行 | 水増しが起きない | 6:41 で出てミッドロールを捨てる |
| b 下限を復活させる | 「8分未満は不合格」 | ミッドロール条件 | 8分に届く | **L002 で実際に失敗した方法**。155ユニット・図34%がその結果 |
| c 下限は置かず、章の尺を締める | `60 <= span <= 150` → `90 <= span <= 140` | 設計文書が既に「100〜130秒」と書いている | **薄めずに尺が伸びる**。水増しは章を増やさないと作れないが章数は5で頭打ち | 現行の L001 が落ちる(2章74.3秒・3章60.2秒) |
| ~~d 下限を WARN で戻す~~ | 8分未満は警告のみ | — | — | **【訂正】すでに `check_video.py` に実装済み。選択肢ではない** |

- **比較**: b と c はどちらも尺を伸ばすが、**伸び方が違う**。
  b は「総尺」を目標にするので、**どこに足しても通る**(=いちばん楽な水増しが通る)。
  c は「章の尺」を目標にするので、**足せる場所が章の中に限られる**。
  章数の上限が5なので、c で作れる最大は 5×140+冒頭+締め ≒ 12分強。
  水増ししようにも、章の中に足すには章の問いに答える材料が要る。
  **c は水増しの逃げ道を構造的に塞いでいる。**
- **決定**: **総尺の下限は置かない(維持)。章の尺のゲートを `60〜150` → `90〜140` に締める(変更)。**
  L001 の2章(74.3秒)・3章(60.2秒)はこれで落ちる。**落とすのが目的**である。
- 【追加・適用範囲を限定しないと通らない(確度A)】この案には条件を付けないと採れない。
  1. **C7-1 は「L001 は現状のまま出す。A/Bテストの対照群にする」と決めている。**
     一方この案は L001 を不合格にする。**同じ文書の中で矛盾している。**
     CLAUDE.md のワークフローは「台本を書いたら焼く前に `check_long.py` を通す」なので、
     **公開済みの L001 が恒久的に赤いままになる**。
     赤が常態になったゲートは読まれなくなる — これは `check_ikko.py` の較正
     (`02-calibration.md`)が「**合格作を落とさない**」を原則にしたのと同じ理由である
  2. したがって採るなら **「新しく書く長尺にだけ適用する」**と明記し、
     既存作は対象外にする(`check_long.py` に除外リストを持たせるか、
     L001 を `--legacy` で走らせる)。**この限定が無ければ却下。**
  3. さらに `speedScale` を 1.00 にしても(C7-1)、章の尺は
     121.1 / 84.4 / 68.5 / 132.6 で **2章と3章はまだ90に届かない**。
     **話速を下げるだけでは通らず、中身を足すことが必須**である
     — これは元の文書の主張どおりで、検証側の計算でも一致した
  4. 【追加】C6-1c で締めを章から出すと、`check_long.py` の `heads` は
     `ch\d+` シーンしか見ないので**章の尺の数え方が変わらない**。
     L001 の4章116.5秒には締めの約16秒が入ったままである。
     **90〜140 の帯を決める前に「締めを章の尺に含めるか」を先に決めること。**
     含めない数え方にすると4章は約100秒になり、帯の下側に寄る
- **根拠**: 下限ゲートの失敗は L002 として実物が残っており、19件の不合格を実行して確認した(確度A)。
  【検証】**L002 の比較表は再現できた**: 519.9秒 / 155ユニット / 6章 /
  **不合格19件 + WARN 6件** / 図34% / 図なしの最長連続 62.8秒 / 止め0箇所 / 鎖の長さ47。
  **L001 の図の割合だけが 66% ではなく 63%**(71/113)だった。
  一方、章の尺の目標(100〜130秒)は `longform-design.md` §3 に既に書かれていて、
  **ゲートだけが 60〜150 に緩められていた**。文書とゲートの不一致を、文書の側に寄せる。
- **確度**: **A**(両方の動画でゲートを実行した。検証側でも再実行して一致)。
  ※ ただし「90〜140が正しい」という外部の裏付けは無い。設計文書の 100〜130 に幅を持たせただけである。
  ※ **適用範囲の限定(上記2)を付けない限り、この変更は採れない。**

---

## C7-3 ユニット数と尺の関係(現状113ユニット)

- **現状(測定値)**: 全動画の `script.md` のナレーション行数と mp4 の尺から算出した。

| | 尺 | ユニット | 秒/ユニット |
|---|---|---|---|
| **L001** | 400.7秒 | **113** | **3.55** |
| ショート28本 | 45.8〜56.1秒 | 中央値**16.5**(14〜19) | 中央値 **3.08**(2.66〜3.41) |
| L002(未焼き) | 519.9秒(推定) | **155** | 3.35(推定) |

  L001 の1ユニットはショートより **0.47秒(15%)長い**。話速を下げた(1.3→1.15)ぶんと一致する。
  【検証】**ショート28本の統計は完全に再現できた**(尺 45.8〜56.1 / ユニット 14〜19 /
  秒per ユニット 2.66〜3.41・中央値3.08)。中央値のユニット数だけ **16 → 16.5**(28本の偶数中央)。
  ユニットの内訳: ナレーション 381.5秒 + 無音 19.2秒。無音は5箇所が0.6秒、108箇所が0.15秒。
  **無音は全体の4.8%しかない。**
- **選択肢**:

| 案 | 内容 | 根拠・採用例 | 長所 | 短所 |
|---|---|---|---|---|
| a ユニット数を目標にしない(現状) | 尺はユニット数の結果 | 現行 | 水増しの動機が生まれない | 見積もりが立たない |
| b ユニット数を目標にする | 「8分なら135ユニット」 | 3.55秒/ユニットから逆算 | 企画時に見積もれる | **L002 がまさにこれで +89ユニットされた** |
| c 章あたりのユニット数を目安にする | 1章 25〜35ユニット | L001 実測: 冒頭12 / 1章31 / 2章22 / 3章17 / 4章31 | 章の尺ゲート(C7-2)と同じ単位で管理できる | 章の尺を測れば足りるので冗長 |
| d 無音を増やして尺を作る | pad を 0.15 → 0.3 に | — | 台本を変えずに +16.9秒 | **中身が増えないのに尺だけ増える = 水増しそのもの** |

- **比較**: d を実際に計算すると、108箇所の pad を 0.15→0.30 にして **+16.2秒**。
  8分に必要な79.3秒の20%しか埋まらず、しかも1文字も情報が増えない。**論外**。
  b は 500秒 ÷ 3.55秒 = **141ユニット**という数字を作ってしまい、L002 の再現を招く。
- **決定**: **維持(ユニット数は目標にしない)**。ただし**見積もり用の換算値として
  「長尺は約3.55秒/ユニット、8:20なら約141ユニット」を文書に残す**(目標ではなく物差しとして)。
- 【訂正・C7-1 との内部矛盾】この 141ユニットは **`speedScale` 1.15 のときの 3.55秒/ユニット**で
  割った値である。**C7-1 は 1.00 への変更を決めている**ので、両方を採るなら
  1ユニットは **4.03秒**(455.6秒 ÷ 113)になり、**8:20(500秒)は約124ユニット**になる。
  **141 と 124 が同じ文書に同居している状態は直さなければならない。**
  換算値は「**速度を決めてから割る**」ものなので、文書に残すなら
  「`speedScale` 1.15 なら 3.55秒/ユニット、1.00 なら 4.03秒/ユニット」の**両方**を書き、
  ユニット数の目安は書かないほうがよい(書けば目標になる、というのがこの項目の結論そのものである)。
- **根拠**: 3.55秒/ユニットは実測。b を目標にした前例(L002)が155ユニット・図34%・19件不合格として
  リポジトリに残っている。**同じ道具を目標にすると同じ結果になる**ことを、実物で確認できる。
- **確度**: **A**(mp4 のあるショート28本 + L001 を測定。検証側で再現)。

---

## 測れなかったこと(正直に書く)

1. **長尺の視聴維持率が1本も無い。** L001 は焼き上がっているが、
   `demand-2026-08.md` / `hold-rate-2026-08.md` を検索してもアナリティクスの記載は無かった。
   したがって「8分にすると維持率がどうなるか」「締めの型が長尺で効くか」は**この時点では原理的に測れない**。
   本文書で維持率を根拠にした変更は1つも無い。
2. **一次資料を1本も読めていない。** Zhang & Schwarz の PDF は `EGRESS_BLOCKED`(実測)。
   YouTube ヘルプも本文は取れない。B と書いた箇所は**すべて検索結果のスニペット3件以上の一致**であり、
   それ以上の意味は無い。
3. **数値の粒度(C5-3)は外部の知見が正反対に割れている。** 「精密=信用される」と
   「丸い=行動が変わる」の両方がスニペットにあり、しかも「伝え手が人間でないと精密さの効果は消える」
   という但し書きがある(うちは合成音声である)。**どちらが正しいか判断する材料が無いので、
   A/Bテストに送った。**
4. **視聴率データの元は自分で測っていない。** 14本の平均視聴率はユーザー提供の値を
   `demand-2026-08.md` に転記したもの。相関の計算(r・t・R²)は自分でやったが、
   元の数値を再測定することはできない。
5. 【検証側の追記】**再現できなかった測定を7件、本文中に「【訂正】」として直した。**
   内訳: C5-1 の「ショートの鎖の長さ 9〜14」(実測2〜14)/ C5-1 の章あたり初出数値
   (3/9/8/4/8 → 3/7/5/2/7)/ C5-2 の1文あたり分布(77/33/3 → 84/27/2)/
   C5-3 の端数読み上げ回数(25回 → 26回)/ C5-6 の「21/30本」(→ 20/20本)/
   C7-2 の「8分未満のWARNは無い」(→ `check_video.py` に2箇所ある)/
   C7-2 の L001 の図の割合(66% → 63%)。
   **決定が覆ったものは1件も無い**が、C5-6 は対象本数が変わり、
   C7-2 は選択肢dが消えた。
6. 【検証側の追記】**「変えると壊れる」箇所を3件見つけた**(いずれも実際に走らせて確認):
   - **C5-5**(字幕のカンマ)を単独で当てると `check_long.py` が不合格になる。
     字数カウントの正規表現に半角カンマを足すのが同時修正
   - **C7-1**(`SPEED_SCALE` 1.15→1.00)は `check_long.py` 51行の
     `SEC_PER_CHAR_LONG` に 1.15 が直に書かれているので、そこも直さないと推定尺が13%短く出る。
     さらに `longform-design.md` §4 の「330〜345字/分に置く」と衝突する
   - **C7-2**(章の尺 90〜140)は C7-1 の「L001 は現状のまま出す」と矛盾する。
     新作限定にしないと、公開済みの唯一の長尺が恒久的に不合格になる
7. 【検証側の追記】**測り方を書いていなかった測定に、閾値を書き足した。**
   終了画面の占有率は「背景輝度37からの逸脱」とだけ書かれていて再現できなかったが、
   **`|L − 37| > 25`** とすると表の数字がそのまま出る。この閾値を本文に追記した。
   同様に C5-5 の「58箇所」は「直前が数字・カンマ・万千百でない4桁以上の数字+円」という
   数え方で完全一致する。**閾値と数え方を書かない測定は、確度Aを名乗れない。**
