# D 公開・メタデータ(8項目)

調査日 2026-08-21 / 対象 L001(横型長尺・6分40.7秒)を主、ショート14本の実測を従。

## この調査の限界(先に書く)

**外部の新規調査ができなかった。** 正直に記録する。

- WebSearch: このセッションの検索枠(200回)が本タスク開始前に使い切られており、
  投げた2回は両方とも実行されずに戻った。**新規の検索は0件。**
- WebFetch: 一次資料に当たろうとして6ドメインを試し、**全部が egress でブロック**された。
  - support.google.com(YouTube公式ヘルプ。チャプター・終了画面・字幕の一次資料)→ EGRESS_BLOCKED
  - developers.google.com(YouTube Data API のフィールド上限。title/description/tags)→ EGRESS_BLOCKED
  - en.wikipedia.org(SubRip 仕様)→ EGRESS_BLOCKED
  - buffer.com / vidiq.com(投稿時刻・タグの実務データ)→ EGRESS_BLOCKED

したがって本書の外部根拠は、**このリポジトリの過去ループが取得済みのもの**
(docs/posting-plan.md / docs/research/longform-loop-2026-08/10-ending.md /
element-taxonomy-2026-08.md)に限られる。それらはURL付きで確度も付いているので使うが、
**今回このセッションで再確認したわけではない**ため、外部由来の主張は確度を1段下げて扱う。

その代わりに厚くしたのが、**この環境で自分で測れるもの**である。
コード・生成物・mp4の実フレームから取った事実は確度**A**(再現可能)として扱い、
決定の主軸をそちらに置いた。8項目のうち5項目は、外部の定石ではなく
**自リポジトリ内の不整合**(ルールと実装が食い違っている箇所)が決定理由になっている。

---

## D1 タイトル

- **現状**: `videos/L001-nisa-son/script.md:4`
  `NISAの損は税金を減らせない?知らないと払う40,630円`(30字)。
  `make_upload_kit.py` の `long_kit()` が script.md の `**投稿タイトル**:` 行から抜いてキットに載せる。

- **選択肢**:

  | 案 | 内容 | 採用例・出典 | 長所 | 短所 |
  |---|---|---|---|---|
  | 1 現状維持 | 「NISAの損は税金を減らせない?知らないと払う40,630円」30字 | 現行 | 数字あり・疑問形・NISAが先頭 | **自リポジトリの28字ルールに違反**(posting-plan.md §7)。動画の主題語「損益通算」がタイトルに無い |
  | 2 検索語追加・28字 | 「NISAの損は損益通算できない?余分に払う40,630円」28字 | 本調査の提案 | 28字ルール適合。「NISA」先頭4字・「損益通算」6〜9字目(先頭15字以内)。タグ表(L_TAGS)にある主要語をタイトルが回収する | 「損益通算」が専門語。ただしCLAUDE.md「長尺は入口に専門語を使ってよい」で明示的に許可されている |
  | 3 行動結果型 | 「NISAで損した年、税金はいくら変わる?」**20字**(前版「18字」は誤り。`len()` で再計測) | 実測上位型(demand-2026-08 追記2) | 実測で上位だった「自分の行動の結果を問う」型に最も近い | **具体数字が消える**。実測では数字付き検索語のほうが視聴率2〜3倍(nisa1800万円129% vs nisa 59.5%) |
  | 4 【】付与 | 「NISAの損は損益通算できない?【余分に払う40,630円】」 | 既存ショート12本 | チャンネル内で表記が揃う | 実測で「効いているのは【】ではなく、その前の問いのほう」と結論済み。字数を2字食うだけ |

- **比較の軸**: 字数ルール適合 / 主要検索語の位置 / 数字の有無 / 実測タイトル型との一致 / 断定表現の有無。

- **決定**: **変更**。
  `NISAの損は税金を減らせない?知らないと払う40,630円`(30字)
  → **`NISAの損は損益通算できない?余分に払う40,630円`(28字)**
  A/B対照として L002 は案3型(行動結果型・数字なし)で出し、平均視聴率を比較する。

- **根拠**:
  1. **28字の行が存在することは確度A。ただし長尺に効くかは確度C。**
     `docs/posting-plan.md` §7 に「タイトル28字以内・主要検索語が先頭15字以内」とあるのは事実で、
     現行30字はこれを2字超える。
     `[検証]` **§7 は Shorts 用のメタデータチェックリストである**(同じリストに
     「#shortsは不要」「関連動画リンク設定済み」= Shorts 専用項目が並ぶ)。
     一方 `docs/research/element-taxonomy-2026-08.md:164` は
     **「全角28〜30字以内」**と書いており、同 177〜178 行は L001 について
     **「30字以内に改稿」**を指示している。**現行30字はこちらには適合している。**
     さらに `docs/research/longform-design.md` に長尺のタイトル字数規定は**無い**。
     つまり社内基準が28と28〜30で割れており、**28字を長尺に適用するのは外挿**である。
     これは本書 D5-3 が自ら「横断適用は外挿」と退けたのと同じ型の外挿なので、
     同じ基準で **確度C** に下げる。28字化そのものは害が無いので提案は残すが、
     **「ルール違反だから直す」という言い方は取り下げる。**
  2. **主題語の取りこぼしも確度A**。`make_upload_kit.py` の `L_TAGS["L001"]` に「損益通算」があり、
     概要欄本文にも出るのに、**タイトルにだけ無い**。動画が答える問いそのものの語である。
  3. **数字を残す理由は実測B**。demand-2026-08.md 追記1: 検索語「nisa1800万円」129.1% /
     「nisa 結果」105.0% に対し「nisa」59.5%・「新nisa」40.2%。
     具体数字か「仕組み」が付いた語のほうが視聴率2〜3倍。ただし合計192視聴なので
     個別語の順位は確度C、クラスタ傾向は確度B。
  4. 「28〜30字以内・キーワード前半・具体的数字」の外部定石は
     element-taxonomy-2026-08.md の出典欄(Re-HERO/ytbs/ラッコ/LaboTube)に記録があるが、
     **今回このセッションでは再確認できていない**(egressブロック)。よってこれ単独では行動を変えない。

- **確度**: **C**(主題語「損益通算」の欠落は確度A。字数28 vs 28〜30 は社内基準が割れており、
  長尺への適用は外挿=C。視聴率への効果もC)
  `[検証]` 前版は **B** と名乗っていたが、根拠1(28字)がCに落ちたので全体もCが上限。

---

## D2 概要欄の構成

- **現状**: `uploads/L001_投稿キット.txt` の実際の並びは
  結論2行(95字)→ チャプター5行 → 1行の総説 → 「この動画が答える問い」→「答え」→「内容」10行
  →「前提」4行 → CTA1行 → 区切り線 → 免責2行 → 出典 → 音声/立ち絵/制作クレジット → ハッシュタグ3個。
  結論2行は `make_upload_kit.py` の `L_LEAD` 辞書に手書きされている。
  **概要欄にURLは1本も無い**(zunko.jpのガイドラインURLを除く)。

- **選択肢**:

  | 案 | 内容 | 採用例・出典 | 長所 | 短所 |
  |---|---|---|---|---|
  | 1 現状維持 | 結論2行→チャプター→本文→出典→クレジット | 現行(element-taxonomy D2で前回改修済み) | 折りたたみ前に結論と検索語が入る | 長尺で唯一使える**クリック可能な内部リンク**を1本も使っていない |
  | 2 内部リンク追加 | 「答え」の直後に1行 `▼ 1分版(ショート): <S017のURL>` | 本調査の提案 | ショートでは**公式に無効化されている**導線(posting-plan の出典: support.google.com/youtube/answer/13748639)が、長尺では生きる。締めのナレーションが既に「概要欄のショート版」と言っている(SRT #111)のに、その概要欄にリンクが無い | **`docs/posting-plan.md` §7 に「説明欄に『クリックしてね』等のURL誘導がない(出典表記としてのURLはOK)」と明記されている。この案は出典ではなく誘導なので、正面から抵触する。**前版は「戦略§6の抵触なし」と書いていたが、§6 は**コメント欄**のURLの話であって説明欄の話ではない(strategy.md:89「コメント欄にURLは貼らない」)。**適用されるルールを取り違えている** |
  | 3 チャプター先頭 | チャプターを最上部に戻す | 旧実装(10-ending.md フェーズ10で採用していた) | 折りたたみ前に全体像が出る | 折りたたみ前の一等地を目次に使うことになる。前回この理由で結論2行に入れ替えた |
  | 4 結論1行+検索語行 | 1行目=結論、2行目=検索語の羅列 | 一部の解説チャンネルの慣行 | 検索語密度が上がる | 語の羅列は人間が読む文にならない。当チャンネルの「文で書く」方針と衝突 |

- **比較の軸**: 折りたたみ前(冒頭2〜3行)に何が入るか / クリック可能な導線の有無 /
  ナレーションとの整合 / 規約リスク / 実装コスト。

- **決定**: **変更(ただし内容を差し替え)**。
  `[検証]` **案2(概要欄にショートのURLを1行足す)は却下する。**理由は2つ:
  1. `posting-plan.md` §7 の「説明欄にURL誘導を置かない」に正面から抵触する
     (上表の短所を参照)。
  2. **本書 D7 と両立しない。**D2 の根拠2は「ナレーション #111 が『概要欄のショート版』と
     言っているのに概要欄にリンクが無い」という不整合だが、**D7 はまったく同じ不整合を、
     #111 のほうを『終了画面の…』に書き換えて解消すると決めている。**
     D7 を実施すると #111 は概要欄を指さなくなるので、**D2 の根拠2は消滅する。**
     同じ1つの不整合に対して、本書は互いに打ち消し合う2つの修正を「変更」として並べていた。
     **どちらか一方しか実施できない。**長尺だけが持つ公式導線(終了画面)を使い、
     規約リスクの無い D7 側を採る。

  したがって D2 で実施するのは **`L_LEAD` の不備の修正だけ**にする:
  - `L_LEAD` に **L002 のキーを追加**する(現状 L002 の結論2行は空行のまま出ている)。
  - `L_LEAD` に **「結論2行の合計100字以内」の機械チェック**を入れる(現行L001は95字で合格)。
  - `long_kit()` は `chapters.txt` が無いと `(未生成。render.py を走らせる)` という
    **プレースホルダを概要欄本文にそのまま埋め込んで出力する**。L002 のキットで実際にそうなっている。
    ここも `SystemExit` で落とす(`[検証]` で新たに見つけた3件目の不備。確度A)。

- **根拠**:
  1. **`L_LEAD` に L002 のキーがない**(`make_upload_kit.py`。`L_LEAD.get(lid, "")` で
     L002は空文字になる)。確度**A**。
     `[検証]` `uploads/L002_投稿キット.txt` を実際に開いて確認した。「■ 概要欄」の直後が空行2つで、
     続けて `(未生成。render.py を走らせる)` というプレースホルダが本文に混ざっている。
     **L_TAGS・L_PLAYLISTS・L_NEXT には L002 のキーがあり、L_LEAD にだけ無い**ことも確認した。
  2. **ナレーションと概要欄が食い違っている**。SRT #111
     「税金の20.315%は、概要欄のショート版でも見られるのだ。」と言っているのに、
     概要欄にショート版への行が無い。確度**A**(uploads/L001.srt と uploads/L001_投稿キット.txt を突き合わせて確認)。
     `[検証]` 現象は確認できたが、**この根拠は D7 の修正で解消されるため、D2 の変更理由には使えない**(上記)。
  3. Shortsの説明欄リンクが無効という点だけは公式出典が posting-plan に記録されている
     (support.google.com/youtube/answer/13748639)。**長尺で有効**という対称の主張は
     今回再確認できていないので確度**C**。
     `[検証]` support.google.com は検証側でも再度 WebFetch を試み、**EGRESS_BLOCKED** を確認した。
     一次資料の本文は読めていない。ソース1本・本文未読でBを名乗れないのでCに下げる。
     いずれにせよ案2を却下したので、この主張は行動を変えない。
  4. 「折りたたみ前の最初の2行に結論+キーワード」は element-taxonomy の出典
     (デジハク/pamxy/ビーヘルシー/UREBA)由来。**今回未再確認**のため、これ単独では行動を変えない。
     幸い現行が既にその形なので、ここは維持で足りる。

- **確度**: **A**(`L_LEAD` の L002 欠落・chapters プレースホルダの混入は実装を読んで確認)
  / 案2(URL追記)は **却下**(規約ルール抵触 + D7 と非両立)

---

## D3 チャプター

- **現状**: `videos/L001-nisa-son/output/chapters.txt` は5行。
  `sl.chapter_lines()` が `CHAPTER_MARKS` から自動生成する。
  ```
  0:00 NISAで損したら、いくら損するのか
  0:43 第1章 NISAの損は、利益から引けるのか
  2:29 第2章 来年の利益からは、引けるのか
  3:43 第3章 この不利は、いくらまで効くのか
  4:44 第4章 自分の場合は、どう出すのか
  ```
  動画長は 400.704秒 = 6:40.7(ffprobe実測)。各チャプターの長さは
  **43 / 106 / 74 / 61 / 116 秒**。

- **選択肢**:

  | 案 | 内容 | 採用例・出典 | 長所 | 短所 |
  |---|---|---|---|---|
  | 1 現状維持(5行) | 冒頭+4章 | 現行 | 章立てと1対1で対応し、迷いがない | 最後の1行が116秒と最長で、中で話題が2つ(自分の計算+3つの場合の表)動いている |
  | 2 6行(まとめ追加) | 6:04 に1行足す(render.py の `matome` シーン開始点) | 10-ending.md が「冒頭+4章+まとめ=6行」を想定していた | 最長章を80秒+36秒に割れる | 「まとめ」ラベルは**目次語**で、CLAUDE.md「チャプターは目次ではなく問いにする」に反する。問い形にしても、まとめは飛ばし先になりやすい |
  | 3 細分(8〜10行) | 60〜90秒の拍ごとに置く | 長尺解説の一般的な慣行 | ナビゲーションが細かい | 章の単位が壊れる。CLAUDE.md「章は4つ」と衝突 |
  | 4 チャプターを置かない | タイムスタンプを書かない | 一部チャンネル | 飛ばし見を誘発しない | YouTubeの機能を捨てるだけで、得るものが不明 |

- **比較の軸**: YouTubeの仕様適合 / 章構造との整合 / 飛ばし見の誘発 / 実装コスト(自動生成なのでどれも同コスト)。

- **決定**: **維持**(5行)。
  `[検証]` **前版の「L002 を案3(8行)で出す」は却下する。**
  本書は同じ節で **案2(6行)を「CLAUDE.md の明文ルールと衝突するので、根拠が無いまま採らない」**
  と退けながら、直後に **案3(8行)を採る**と書いていた。しかし本書自身の選択肢表が
  案3の短所として「**章の単位が壊れる。CLAUDE.md「章は4つ」と衝突**」と書いている。
  **案2より重い衝突を、案2を退けたのと同じ節で採用している。**自己矛盾なので取り下げる。
  A/Bを立てるなら、**章構造を壊さない範囲**、すなわち
  **「4章のまま、章の中の60〜90秒の拍にチャプターを追加して6〜7行にする」**形にする
  (CLAUDE.md「60〜90秒の拍は章の中に置くもの」と両立する)。ただし**これも根拠は無い**ので、
  L001の章別維持率をStudioで見てから決める。**今ここで L002 の行数を決めない。**

- **根拠**:
  1. **仕様は満たしている。** 要件(3個以上・最初は0:00・昇順・各10秒以上)を5行すべてが満たす。
     最短が43秒。
     `[検証]` **出典の記載が誤っていた。**`docs/research/longform-loop-2026-08/10-ending.md` を
     全文 grep したが、**「3個以上」も「各10秒以上」もこの文書には書かれていない**
     (同文書がチャプターについて言うのは「8個で、0:00 から始まっている(仕様を満たす)」だけ)。
     この仕様が書かれているのは **`docs/research/elements/script.md:286` と `:327`** であり、
     そこには **「※公式ヘルプ本文は今回 fetch 不可」**と明記されている。
     したがって **確度A ではなく B**(本文未読の一次資料)。出典を script.md に差し替える。
  2. **章数の最適値については定石が見つからなかった。**
     5行が良いのか8行が良いのかを支持する資料に、今回まったく当たれていない。
     element-taxonomy が D3 を「◯(視聴維持に寄与・確度B)」としているが、
     これは「チャプターを置くこと」の是非であって、**何個置くかの根拠ではない**。
  3. 自チャンネルには**長尺の実測が1本も無い**(L001は8/22公開予定、本日8/21)。
     チャプター別維持率はStudioで取れるので、実測で決められる。
  4. 案2は CLAUDE.md の明文ルールと衝突するので、根拠が無いまま採らない。

- **確度**: **C**(仕様適合は**B**に訂正。章数は根拠なし → L001の章別維持率の実測待ち)

---

## D4 タグ

- **現状**: `make_upload_kit.py` の `L_TAGS["L001"]` 8個 + `COMMON_TAGS` 4個 = **12個**。
  出力文字列は `NISA, ニーサ, 損益通算, 繰越控除, NISA デメリット, 確定申告, 課税口座, 投資 税金, お金の勉強, 金融リテラシー, ずんだもん, お金` で **79文字**。
  ショートには表の取り違えを機械で止める `assert_tags_match()`(タグの2個以上が台本本文に出ることを要求)と
  `assert_no_bare_ryakugo()` があるが、**長尺の生成ループはこれを1回も呼んでいない**。
  `[検証]` `main()` を読んで確認した。`videos/L0*` のループは
  `long_kit(d, lid)` を呼んで書き出すだけで、`assert_*` も `MISMATCH.append` も無い。
  `videos/S0*` のループにだけ両方がある。**この節の中核主張は正しい。**
  なお `long_kit()` は `L_TAGS.get(lid, [])` と `.get` を使っているので、
  **未知のIDでもタグが `COMMON_TAGS` 4個だけになって黙って通る**という穴も併存する
  (ショート側は `TAGS[sid]` で KeyError になる)。ここも合わせて落とすべきである。

- **選択肢**:

  | 案 | 内容 | 採用例・出典 | 長所 | 短所 |
  |---|---|---|---|---|
  | 1 現状維持 | 12個・79字 | 現行 | 表記ゆれ(NISA/ニーサ)を吸収。コストほぼゼロ | 効果の証拠が無い |
  | 2 削る(5個) | 主要語のみ | 「タグの寄与は小さい」という業界の通説 | 管理が軽い | 表記ゆれ吸収を捨てる。得るものが無い |
  | 3 増やす(20個以上) | 関連語を広く | 一部のタグ最適化ツールの推奨 | 網羅性 | 内容と無関係なタグは規約上の誤解を招くリスク。当チャンネルは量産型規制の対象になりやすい題材なので避ける |
  | 4 機械検証を長尺にも適用 | `assert_tags_match` を L ループでも呼ぶ | 本調査の提案(ショートには既に実装済み) | ID使い回しでテーマを差し替えたときの取り違えを止める。**ショートでは実際に S012 のタグが「リボ払い」のまま出た事故がある**(コード内コメントに記録) | 実装コスト小 |

- **比較の軸**: 効果の証拠 / 表記ゆれ吸収 / 事故の防止 / 規約リスク / 実装コスト。

- **決定**: **変更**(タグの個数・内容は**維持**、検証の適用範囲を**変更**)。
  `make_upload_kit.py` の `main()` の長尺ループに、ショートと同じ
  `assert_tags_match(lid, script)` と `assert_no_bare_ryakugo(lid, title, desc)` を追加し、
  不合格なら `SystemExit(1)` させる。タグは 12個・79字のまま。

- **根拠**:
  1. **長尺が無検証なのは確度A**(コードを読んで確認)。`main()` のLループは
     `long_kit()` を呼んで書き出すだけで、`MISMATCH` に何も積まない。
     同じ事故(S012のタグがリボ払いのまま)が長尺で起きたとき、止まらない。
  2. **タグの効果については、今回まったく裏が取れなかった。**
     「YouTubeはタグの役割は小さいと公式に述べている」という記憶があるが、
     **一次資料を確認できていない**(support.google.com がブロック)。
     記憶に基づく記述なので確度**C**とし、**これを理由にタグを減らす行動は取らない。**
  3. タグ欄の文字数上限(500文字と記憶)も同様に未確認。ただし現行79字なので、
     上限がいくつであれ抵触しない。この点だけは実害なし。
     `[検証]` 検証側でも `support.google.com` / `developers.google.com` への到達を試み、
     **EGRESS_BLOCKED** を再現した。WebSearch も**本セッションの枠(200回)が
     使い切られている**ことをツールの応答で確認した(調査員の申告どおり)。
     **したがって「500文字」は記憶のままであり、この文書のどこからも根拠として使わないこと。**

- **確度**: **A**(検証欠落について)/ タグ本体の最適値は **C**(根拠なし → 現状維持)

---

## D5 投稿時刻

- **現状**: `make_upload_kit.py`
  - 長尺 `L_SCHEDULE = {"L001": "8/22(土) 18:00", "L002": "8/29(土) 18:00"}`
  - ショート `SCHEDULE`: 毎日 8:30 と 19:30 の2本。**8/22(土)は S020 8:30 / S021 19:30**。

  つまり **8/22(土)は 8:30・18:00・19:30 の3本**が出る。
  **18:00 と 19:30 の間隔は1時間30分。**
  `[検証]` 値はすべてコードのとおりで正しい。ただし**本書が見落としている社内ルールがもう1本ある。**
  `make_upload_kit.py` の `L_SCHEDULE` の直下のコメントに
  **「ショートの朝夕(8:30/19:30)と30分以上離す」**と書かれている。
  **1.5時間はこちらのルールは満たしている。**
  つまり社内に **「4〜6時間以上」(posting-plan §1)** と **「30分以上」(コード内コメント)** の
  **2本の食い違うルールが同時に存在する**。本書は前者だけを引いて「明文ルール違反」と断じていた。
  正しくは **「社内ルールが2本あって矛盾している」**であり、まず直すべきはそこである。
  `[検証]` また `L002` は `8/29(土) 18:00` だが、`SCHEDULE` は **S031 の 8/26 で終わっており、
  8/29 に予定されたショートは1本も無い**。L002 側には現時点で衝突が無い。

- **選択肢**:

  | 案 | 内容 | 間隔(同日) | 長所 | 短所 |
  |---|---|---|---|---|
  | 1 現状維持 | 土18:00 + 短8:30/19:30 | 9.5h / **1.5h** | ゴールデン入り口を取れる | **自リポジトリの明文ルール違反**(posting-plan §1「同日に出すなら4〜6時間以上あける」) |
  | 2 同日のショートを1本に | 土18:00 + 短8:30のみ | 9.5h | ルール適合。18:00を維持できる | ショートの枠が週1つ減り、S021以降が1枠ずつ後ろ倒しになる |
  | 3 長尺を15:00へ | 土15:00 + 短8:30/19:30 | 6.5h / 4.5h | **3本とも4時間以上を満たす唯一の中間解** | 18〜21時のゴールデンを外す |
  | 4 旧値に戻す | 土12:00 | 3.5h / 7.5h | — | 8:30との間隔が3.5hでルール違反。前回18:00に変えた理由も消える |
  | 5 日曜へ移す | 日18:00 | 同じ問題 | — | 日曜も 8:30/19:30 の2本があるので何も解決しない |

- **比較の軸**: 自リポジトリの間隔ルール適合 / ゴールデン帯(18〜21時)に入るか /
  ショートの投稿枠を減らすか / 長尺とショートの共食いが実際に起きるかの証拠。

- **決定**: **変更(ただし手順を差し替え)**。
  まず **社内ルールの二重化を解消する**: `posting-plan.md` §1 の「4〜6時間以上」と
  `make_upload_kit.py` のコメント「30分以上離す」のどちらを採るかを1箇所に書き、他方を消す。
  **どちらが正しいかを決める根拠は本調査には無い**(下の根拠3のとおり、そもそもショート同士の話である)。
  したがって **時刻は 土18:00 + 短8:30/19:30 のまま動かさず**、
  `make_upload_kit.py` に **「同日の投稿間隔を計算して表示する」**機械チェック(落とさず表示のみ)を入れ、
  **L001 の実測(8/22公開)を見てから間隔ルールを1本に決める。**

  `[検証]` **前版の「S021 を 8/23(土)8:30 に移し、S022以降を1枠ずつ後ろ倒し」は却下する。**
  実行できない具体値だからである:
  - `SCHEDULE` の **S022 が既に 8/23(日) 8:30 を占有**している。玉突きで S023→8/24 8:30 になるが、
    そこは **S027 が占有**している。
  - `SCHEDULE` には **S024・S025・S026 のキーが無い**(`videos/S024-*` `S025-*` `S026-*` の
    ディレクトリは存在するが `script.md` がまだ無いため、今は `SCHEDULE[sid]` の KeyError にならず
    黙って通っているだけ)。台本ができた瞬間に **KeyError で `make_upload_kit.py` が落ちる。**
    「1枠ずつ後ろ倒し」はこの穴の上に積む操作になる。
  **公開順のIDと予約枠を1本ずつずらす操作は、まず SCHEDULE の欠番(S024〜S026)を埋めてからでないと成立しない。**

- **根拠**:
  1. **§1 にその行があることは確度A**。posting-plan.md §1 に「同日に出すなら4〜6時間以上あける」とあり、
     現行の 18:00 / 19:30 は1.5時間。両方ともこのリポジトリ内の値なので再現可能。
     `[検証]` ただし「違反」と言い切れない。**同じリポジトリのコードが「30分以上」と書いており、
     現行値はそちらを満たす**(上記)。**確度Aなのは「2本のルールが矛盾している」ことのほう。**
  2. **18〜21時が強いという主張は確度B**(Buffer 180万本分析。posting-plan の出典欄にURLあり。
     **今回このセッションでは再確認できていない**)。この1点のためだけに18:00を守る価値はあると判断した。
  3. **ただし、間隔ルールの適用範囲そのものが疑わしい。**
     posting-plan の「同日連投はフィードで共食い」はショート同士(Shortsフィード)の話であり、
     **ショートと長尺は配信面が違う**。横断適用は外挿である。確度**C**。
  4. **自チャンネルには長尺の実測が0本。** L001が最初の1本(8/22公開予定)。
     したがって「本当に共食いするか」は分からない。**分からないので、自分のルールに合わせる側に倒した。**
     ルールが間違っているなら、L001・L002の実測を見てルールごと直す。

- **確度**: **A**(社内ルールが2本あり矛盾していること・SCHEDULE の欠番 S024〜S026)
  / **C**(間隔をいくつにすべきか。長尺の実測0本のため決められない)
  `[検証]` 前版の「移動」の具体値は実行不能のため却下。確度ラベルの対象を差し替えた。

---

## D6 固定コメント

- **現状**: **長尺の投稿キットに固定コメントが1行も無い。**
  `videos/L001-nisa-son/script.md:183` には `## 固定コメント(投稿直後に設置)` があり、
  数字の要約2文+時点表記が書かれている。しかし `make_upload_kit.py` の `long_kit()` は
  `extract(script, "固定コメント(投稿直後に設置)")` を**呼んでいない**ため、
  `uploads/L001_投稿キット.txt` に出てこない。
  ショート側のテンプレには「■ 固定コメント」節があり、
  さらに詐欺注意の1行(「⚠このチャンネルがLINEや投資グループに誘導することは絶対にないのだ。」)を
  毎回追記する指示も付いている。**長尺にはどちらも無い。**

- **選択肢**:

  | 案 | 内容 | 採用例・出典 | 長所 | 短所 |
  |---|---|---|---|---|
  | 1 現状維持(長尺は無し) | 固定コメントを置かない | 現行(意図的ではなく実装漏れ) | — | script.mdに書いた内容が捨てられている。詐欺注意も出ない |
  | 2 ショートと同型 | 数字の要約+時点表記+詐欺注意 | ショート14本で運用中 | 実装が最小。表記が揃う | 長尺は情報量が多く、2文の要約では足りない可能性 |
  | 3 要約+締めの問いの再掲 | 案2に「あなたは、NISAと課税口座の両方を使っていますか?」を足す | 本調査の提案 | S011の**コメント0件**の診断が「締めの問いが無かったこと」だった(demand追記3)。長尺は締めのCTAが概要欄にしかなく、コメント欄に問いが立っていない | 1行増えるだけ |
  | 4 章ごとの要点3行 | 4章の判定を並べる | 長尺解説の慣行 | 概要欄を開かない人にも要点が届く | 概要欄の「内容」10行と重複する |
  | 5 訂正・補足専用に空ける | 何も置かず、指摘が来たら使う | 一部チャンネル | 誠実 | 初動の会話が作れない |

- **比較の軸**: script.mdの内容が届くか / 詐欺予防の担保 / コメント欄に会話が立つか /
  概要欄との重複 / 実装コスト。

- **決定**: **変更**。`long_kit()` に `■ 固定コメント` 節を追加し、**案3**の構成にする。具体的には
  ```python
  pinned = extract(script, "固定コメント(投稿直後に設置)")
  ```
  を `long_kit()` に足し、テンプレへ次を挿入する:
  ```
  ■ 固定コメント(投稿直後に貼って固定。URLは貼らない)
  <script.md の固定コメント>
  <script.md の CTA 行(締めの問い)>
  ⚠このチャンネルがLINEや投資グループに誘導することは絶対にないのだ。誘導コメントは詐欺なので通報してほしいのだ。
  ```
  `[検証]` 実装上の注意を1点足す。**CTA行は既に概要欄に入っている。**
  `make_script.py` が `meta['cta']` を `## 概要欄テキスト` ブロックの中に書き込むため、
  `extract(script, "概要欄テキスト")` の戻り値の末尾が
  「あなたは、NISAと課税口座の両方を使っていますか?」になっている
  (`uploads/L001_投稿キット.txt` の「■ 前提」の直後で確認)。
  したがって**固定コメントにCTAを再掲すると、同じ1文がキット内に2回出る。**
  それでよい(概要欄とコメント欄は別の面なので重複は害ではない)が、
  **`script.md` から CTA を別に取り出す口は無い**ので、実装は
  `desc` の末尾行を切り出すか、`script_meta.json` の `cta` を直接読むかのどちらかになる。
  **「script.md の CTA 行」という指定だけでは実装できない。**

- **根拠**:
  1. **欠落は確度A**。`long_kit()` の本文に `固定コメント` の文字列が無く、
     生成物 `uploads/L001_投稿キット.txt` にも無い。両方を読んで確認した。
     `[検証]` 追認した。`videos/L001-nisa-son/script.md` の
     `## 固定コメント(投稿直後に設置)` には2文(要約+時点表記)が実在し、
     `long_kit()` は `extract()` を `概要欄テキスト` に対してしか呼んでいない。
     ショート側の `main()` は `extract(script, "固定コメント(投稿直後に設置)")` を呼んでいる。
     **同じ関数が既にあるのに長尺だけ呼んでいない、という単純な実装漏れである。**
  2. **詐欺注意の1行が長尺だけ抜けるのは、実害のある抜け。**
     ショート側にはループ㉞で「なりすまし投資詐欺の予防」として毎回入れる運用が定まっている。
     題材(NISA・税金)が同じである以上、長尺で外す理由が無い。確度**A**(運用文書との不整合)。
  3. **締めの問いを足す根拠は自チャンネルの実測**だが、**確度は C**。demand-2026-08.md:
     S011はコメント0件で、原因を「締めに4択の問い(`sc.chips`)が無い」と診断している。
     `[検証]` 前版は**この根拠に「実測B」を付けていたが、参照元がそれを否定している。**
     demand-2026-08.md は同じ節で
     **「新しい4本(S016〜S019)では既に埋まっているので、次のバッチで効果を測れる」**と書いており、
     **効果はまだ1度も測られていない。**観察されているのは「無い動画がコメント0件だった」という
     **1例の欠落側だけ**で、これは仮説であって実測ではない。**Cに下げる。**
     (本書は同じ節の根拠4で自分でも同じ指摘をしており、根拠3のラベルだけが取り残されていた)
     長尺L001の締めのCTAは概要欄の1行(「あなたは、NISAと課税口座の両方を使っていますか?」)だけで、
     コメント欄には出ない。
  4. **固定コメントの効果量そのものは分からない。** element-taxonomy が D6 を「実測A」としているが、
     参照先を辿ると裏付けは「S011でコメント0件だった」という**欠落側の観察1件**であり、
     固定コメントを置いたときの増分は測っていない。ここは確度**C**として扱う。

- **確度**: **A**(実装漏れ・運用不整合)/ **C**(コメント数への効果)

---

## D7 終了画面

- **現状**: キットには「最後の10〜15秒に 動画1枚+チャンネル登録、次の動画=S017」と書いてある。
  実際の mp4 を測ると:
  - 動画長 **400.704秒**(ffprobe)。終了画面が置ける枠は仕様上 **380.7〜400.7秒**。
  - その枠のナレーション(uploads/L001.srt):
    - #111 (6:30.3〜6:33.5)「税金の20.315%は、**概要欄のショート版**でも見られるのだ。」
    - #112 (6:33.6〜6:36.8)「ショート版なら、利益と手取りの図だけをさっと見られるわ。」
    - #113 (6:36.9〜6:40.7)「売る損得の判断に、税金は持ちこまなくていいのだ。」
  - **次の動画を名前で呼んでいない。**「S017」も、その動画のタイトルも、SRT全113行に一度も出ない
    (grepで確認)。言っているのは「概要欄のショート版」という指示語的な呼び方だけ。
  - **音声の誘導先(概要欄)と、キットが指定するクリック先(終了画面)が食い違っている。**
  - 395秒地点の実フレームを抜いて確認したところ、画面は
    上部左に章チップ、上部右に免責バッジ、中央に「1分で見たいとき / 税金の20.315% / ※忙しい人向け」の
    ヒーローカード、下部全幅に2行の字幕帯、左と右に立ち絵2体。**空き領域が無い。**
    `[検証]` 検証側でも `ffmpeg -ss 395` でフレームを抜き直し、記述の各要素を目視で追認した。
    ただし**立ち絵の位置は「左下・右下」ではなく、画面中央の高さから下へ伸びる左端・右端**である
    (めたんには右上に「めたん」の名札チップも付いている)。
    また画面下端には「数字で見るお金の教科書」のウォーターマークもある。

- **選択肢**:

  | 案 | 内容 | 採用例・出典 | 長所 | 短所 |
  |---|---|---|---|---|
  | 1 現状維持 | 10〜15秒・動画1枚+登録 | 現行 | — | 名指しが無く、誘導先が音声と食い違い、置く場所も塞がっている |
  | 2 名指しに直す | #111「概要欄のショート版」→「終了画面の『NISAの損益通算』」 | 10-ending.md が調べた実務ソース一致点「名前を言い、見ると何が分かるかを言う」 | 音声とクリック先が一致する。ナレーション1文の差し替えだけ | 再レンダリングが要る |
  | 3 置き場所を空ける | 末尾15秒(385.7秒〜)のユニットを `chara="none"` にし、ヒーローカードを上に寄せて下半分を空ける | shortlib が `chara="none"` を既に持つ(CLAUDE.md「図が主役のユニットは立ち絵を消す」) | 終了画面の要素が本文を覆わない | 掛け合いの絵が最後だけ消える |
  | 4 20秒フルに使う | 上限いっぱい | 一部チャンネル | 要素の露出時間が最長 | 10-ending.md の一致点「20秒の待合室にするな。短く能動的なほうがよい」に反する |
  | 5 終了画面を使わない | 置かない | — | 締めを最後まで本編に使える | 長尺だけが持つ導線を捨てる |

- **比較の軸**: 音声とクリック先の整合 / 画面上の空き / 尺 / 再レンダリングのコスト /
  10-ending.md の一致点との整合。

- **決定**: **変更**(3点、具体値)。
  1. **ナレーションを名指しに直す。** #111 を
     「税金の20.315%だけなら、**終了画面の『NISAの損益通算』**で1分で見られるのだ。」に差し替える。
     「概要欄」→「終了画面」に誘導先を統一する。
  2. **末尾の立ち絵を消す。**
     `[検証]` **前版の「末尾15秒(385.7〜400.7秒)のユニットを `chara="none"` にする
     (現行は立ち絵2体が占有)」は、範囲の記述が誤っている。**`render.py` の末尾を読むと、
     **`matome` / `matome2` / `matome3` の各ユニットには既に `chara="none"` が付いている。**
     立ち絵が出ているのは末尾の `shime` / `tsugi` の4ユニットだけで、
     SRT の時刻では **387.2秒〜400.7秒(13.5秒)**である。385.7〜387.2 秒は既に立ち絵なしなので、
     実際に手を入れる対象は **`shime`×2 と `tsugi`×2 の4ユニット**である。
     `[検証]` **さらにこの案は、下半分を空けられない。**本書 D8 が自分で書いているとおり、
     **画面下部全幅は焼き込み字幕の黒帯**である。立ち絵を消しても字幕帯は残るので、
     空くのは左右の帯だけで「下半分を明け渡す」は成立しない。
     **「立ち絵を消して左右を空ける」までが正しい記述**であり、そこに留める。
     締めのユニットで掛け合いの絵が消えることの是非は別途判断が要る。
  3. **終了画面の長さを「10〜15秒」から「末尾15秒(385.7〜400.7秒)」に確定**し、
     キットに秒数で書く。曖昧な範囲指定をやめ、L002 で 10秒版と比較できる形にする。

- **根拠**:
  1. **名指しが無いことは確度A**(SRT全文をgrepして確認。S017・タイトル・「次の動画」いずれも0件)。
  2. **音声と誘導先の食い違いも確度A**(SRT #111 は「概要欄」、キットの終了画面欄は「S017」)。
  3. **画面に空きが無いことは確度A**(395秒の実フレームを目視。再現可能。検証側でも再抽出して追認)。
     ただし**空けられるのは左右だけ**である(上記)。
  4. 「最後の5〜20秒」というYouTubeの仕様と、「10〜15秒が目安」「退出の言葉を置くな」
     「名前を言って何が得られるかを言う」は 10-ending.md に確度A/Bで記録済み。
     **今回このセッションでは再確認できていない**(support.google.com がブロック)。
  5. **終了画面の要素をどの座標に置くとどれだけクリックされるかは分からなかった。**
     10-ending.md も「クリック率3〜7%・到達率30%」を確度Cとして行動に使わないと決めている。
     本調査でも、置き場所の最適解については資料に当たれていない。**空きを作る**という
     消極的な対処に留める。

- **確度**: **A**(名指し欠落・誘導先の食い違い・画面の占有)/ **C**(クリック率への効果)

---

## D8 字幕ファイル

- **現状**: `production/make_srt.py` が `render.py` の `Unit(...)` 第2引数(=画面に焼き込む字幕文)を
  正規表現で抜き、`output/work/seg_*_pad.wav` の実測長でタイミングを付けて
  `uploads/L001.srt` を出す。手動字幕としてアップロードする運用。
  - 実測: **113ブロック / 1ブロック1行 / 最長37字 / 平均22.7字 / 末尾 6:40.654**。
    動画長 400.704秒に対して終端が 400.654秒で、ズレは0.05秒(pad分)。**同期は良好。**
  - **数値の表記が動画内で割れている。**(現象は正しい。**数え方が誤っていたので訂正する。**)
    `[検証]` 前版の「9箇所 / 15箇所」は `grep -c` の値、すなわち**行数**であって出現数ではない。
    出現数で数え直すと:
    - 図・カバー・表(=Unitの第2引数**以外**): `40,630` **12**、`81,260` **2**、`20,315` **2**
      (`render.py:170` は1行に `40,630円` を2つ持つ)
    - 字幕(Unitの第2引数): `40630` **14**、`20315` **4**、`81260` **3**、`9370` **1**、`8126` **1**
      (前版の「15箇所」には `render.py:170` の**図のデータ用整数リテラル `40630`** が
      混ざっている。あれは字幕ではない)
    - 字幕側にコンマ付きの金額は **0件**。図側にコンマ無しの金額表記は **0件**。
      **割れ方は完全に「字幕 vs 図」で二分している。**
    - 字幕には `2026`(年)も1件ある。**年にコンマを入れてはならない**ので、
      一括置換ではなく金額だけを対象にする必要がある。
    タイトル・概要欄・固定コメントはすべて `40,630円`。
    **同じ画面に「40,630円」の図と「40630円」の字幕が同時に出ている。**
  - 桁区切りを字幕で使っていない理由は `shortlib.Unit.tts_text()` にあると推測できる。
    `tts_text()` は `READING` の置換と `【】` の除去しかせず、**カンマをVOICEVOXにそのまま渡す。**
  - 動画には**字幕が焼き込まれている**(下部全幅・黒帯・2行。395秒フレームで確認)。
    視聴者がCCをONにすると、YouTube側の字幕と焼き込み字幕が**同じ文を二重に表示する**。

- **選択肢**:

  | 案 | 内容 | 採用例・出典 | 長所 | 短所 |
  |---|---|---|---|---|
  | 1 現状維持 | srtをそのまま手動字幕としてアップ | 現行・ショート14本 | 金融用語の誤認識を防ぐ。台本があるので追加コストゼロ | 数値表記の割れと二重表示が残る |
  | 2 桁区切りを統一 | `tts_text()` に `re.sub(r'(?<=\d),(?=\d)','',t)` を1行足し、字幕側を `40,630円` に揃える | 本調査の提案 | 字幕・図・概要欄・タイトルの表記が1つに揃う。SRTは字幕から生成されるので自動的に直る。**動画側の可読性も同時に上がる** | **`figure.md` A5-7 が同じ問題に対し「記者ハンドブック式(`4万630円`)への統一」を決定済みで、この案と正面から衝突する。**`check_yomi.py` / `check_bunsho.py` がカンマを想定していない可能性もあり、ゲートの再確認が要る。全動画の再レンダリングが要る |
  | 3 WebVTTにする | `.vtt` にして `line:5%` で上部に出す | WebVTTは位置指定を持つ(SubRipは持たない) | 焼き込み字幕との重なりを避けられる | **YouTubeがcue settingsをどこまで尊重するか、今回確認できなかった**。効果不明 |
  | 4 srtをやめる | 自動字幕に任せる | — | 二重表示が消える | 「ニーサ」「イデコ」「高額療養費」等の誤認識が戻る。make_srt.py の存在理由が消える |
  | 5 英語字幕を足す | 翻訳SRTを追加 | 一部チャンネル | 到達範囲が広がる | 視聴者は日本語話者前提。翻訳の正確性を検証する手段が無い(数値・制度名) |

- **比較の軸**: 表記の一貫性 / 同期精度 / 二重表示 / 金融用語の誤認識 / 実装コスト /
  既存ゲートとの衝突。

- **決定**: **変更(ただし揃える先を差し替え)**。srt形式は**維持**する。
  `tts_text()` へのコンマ除去1行の追加は**そのまま採用**する
  (読み上げにコンマを渡さないための保険。害が無く、どの表記に揃えても必要になる)。

  `[検証]` **ただし「字幕を `40,630円` に揃える」という揃え先は却下する。**
  **本ループの `docs/research/elements/figure.md` が、同じ問題について反対の結論を出している。**
  同書 A5-7 は、この不一致を**独立に発見**したうえで(「字幕 `20315円` / 図 `20,315円` は不一致」)、
  4案を比較して **C(共同通信『記者ハンドブック』式)への統一**を決定している:

  | 額 | figure.md の決定 | L001 の該当値 |
  |---|---|---|
  | 1万円未満 | 3桁コンマ + 円 | `8,126円` `9,370円` |
  | 1万円以上・端数あり | 万を漢字、以下は詰める | **`4万630円`** `8万1260円` `2万315円` |
  | 年 | コンマを入れない | `2026年` |

  つまり figure.md の決定に従うなら、揃える先は **`4万630円`** であって
  **`40,630円` ではない。**本書の案2はこれと正面から衝突する。
  figure.md が C を選んだ理由は「**発話と字面が一致する**」(「よんまんろっぴゃくさんじゅうえん」と
  同じ順序で読める)で、これは CLAUDE.md の「**数字は図の上で言う**」と直結する内部基準であり、
  本書 D8 が持ち出した「同じ動画の中で表記を割らない」より**強い根拠**である。

  `[検証]` **さらに本書の前提そのものが誤りである。**本書は
  「**チャンネル自身が既に桁区切りを標準にしている**のに字幕だけが例外」と書いたが、
  figure.md は同じ render.py を数えて **「同じ1枚の図の中に3流儀が同時に出ている」**
  (`利益 20万円` / `手元 15万9370円` / `税 40,630円`)ことを示している。
  **桁区切りは標準ではない。**したがって本書の根拠1(「標準に合わせる」)は成立しない。

  **本節の結論**: 表記の統一は **figure.md の決定(記者ハンドブック式・`production/fmt.py`)に従う**。
  D8 は**字幕側を `fmt.yen()` の出力に合わせる**という形でのみ関与する。
  `[検証]` なお `production/fmt.py` は**まだ存在しない**(`ls` で確認)。
  この統一は fmt.py の実装が先で、D8 単独では着手できない。
  **二重表示については、案3(WebVTT)を採らず、L001公開後にCCをONにした実機で確認してから決める。**

- **根拠**:
  1. **表記が割れているのは確度A**(現象。上の訂正済みの内訳を参照)。
     `[検証]` **「チャンネル自身が既に桁区切りを標準にしている」は誤り。**
     figure.md A5-7 の実測どおり、図の中だけでも `20万円` / `15万9370円` / `40,630円` の
     3流儀が同居している。**桁区切りは3つのうちの1つでしかない。**この一文は撤回する。
  2. **実装可能性も確度A**。`tts_text()` は既に置換処理を持つ既存の関数で、
     1行追加すれば済む。`make_srt.py` は字幕文をそのまま使うので追記不要。
     `[検証]` 追認した。`shortlib.py:205-209` の `tts_text()` は `READING` の置換と
     `【】` の除去だけで、コンマには触れていない。
     `make_srt.py` は `Unit\(\s*"[^"]+",\s*"([^"]+)"` で字幕を抜くので、
     字幕文にコンマを入れても抽出は壊れない(コンマは `"` ではないため)。
     ただし **`fmt.yen()` 式(`4万630円`)に変えても同じく壊れない**ので、
     この根拠はどちらの揃え先も等しく支持する。**揃え先を選ぶ根拠にはならない。**
  3. **桁区切りが読みやすいことの一次資料は取れなかった。**
     この点について外部資料に一切当たれていない。したがって
     「桁区切りのほうが読みやすい」ではなく、**「同じ動画の中で表記を割らない」**を根拠にする。
     後者はこのリポジトリ内で検証できる不整合であり、確度A。
  4. **同期は既に良好**(終端0.05秒差)。ここは触らない。確度A(ffprobeとSRTの突き合わせ)。
  5. **二重表示は確度A(現象の存在)/ 確度C(実害の程度)。**
     YouTubeのCCは既定でOFFであり、ONにする視聴者の割合をこのチャンネルは知らない。
     WebVTTの位置指定が効くかも未確認。**分からないので、公開後に実機で見るまで動かさない。**

- **確度**: **A**(表記の割れ・同期・実装可能性)/ **C**(二重表示の実害、WebVTTの効果)
  `[検証]` **揃え先については本書に決定権が無い**(figure.md の決定が先行し、かつ根拠が強い)。
  前版が「A」を名乗っていたのは現象と実装可能性についてであって、
  **`40,630円` に揃えるという判断自体は根拠が誤っていた。**

---

## まとめ: 変更5件・維持2件・保留1件(検証後)

`[検証]` 前版は「変更7件・維持1件」だった。検証で **D2 の主要案と D5 の具体値を却下**し、
**D8 の揃え先を差し替えた**結果、下表になった。

| 項目 | 決定(検証後) | 具体値 | 確度 |
|---|---|---|---|
| D1 タイトル | 変更(弱い) | 「NISAの損は損益通算できない?余分に払う40,630円」28字。**主題語「損益通算」の回収**が理由。**28字ルールは Shorts 用チェックリスト由来で長尺への適用は外挿** | C |
| D2 概要欄 | 変更(縮小) | **URL追記は却下**(posting-plan §7 抵触 + D7 と非両立)。`L_LEAD` に L002 追記/100字ゲート/`(未生成…)` プレースホルダで落とす | A |
| D3 チャプター | **維持** | 5行。**「L002を8行」は却下**(本書自身が案3をCLAUDE.md違反と書いている)。仕様適合の出典は script.md、確度**B** | C |
| D4 タグ | 変更 | タグ本体は12個・79字で維持。**長尺にも `assert_tags_match` / `assert_no_bare_ryakugo` を適用**+`L_TAGS.get` の穴も塞ぐ | A |
| D5 投稿時刻 | **保留** | **時刻は動かさない**。社内ルールが「4〜6時間」(posting-plan §1)と「30分以上」(コード内コメント)で**矛盾**しており、まずそれを1本にする。**S021の玉突きは実行不能**(S022が8/23 8:30を占有・S024〜S026がSCHEDULE欠番) | A(矛盾の存在)/ C(値) |
| D6 固定コメント | 変更 | `long_kit()` に**固定コメント節を追加**(要約+締めの問い+詐欺注意1行)。CTAは `desc` 末尾か `script_meta.json` から取る | A / 効果**C** |
| D7 終了画面 | 変更 | ①#111を**名指し**に差し替え ②立ち絵を消すのは **`shime`/`tsugi` の4ユニット(387.2〜400.7秒)**、空くのは**左右のみ**(下は焼き込み字幕帯) ③秒数を確定 | A / 効果C |
| D8 字幕 | 変更(揃え先を差し替え) | srt維持。`tts_text()` のコンマ除去1行は採用。**揃え先は `40,630円` ではなく figure.md 決定の `4万630円`(記者ハンドブック式)**。`production/fmt.py` の実装が前提 | A(現象)/ 揃え先は figure.md に従属 |

## 定石が見つからず、自チャンネルのA/Bテストに回した項目

1. **D3 チャプターの個数**。**L002を8行にするA/Bは却下**(CLAUDE.md「章は4つ」と衝突)。
   L001の章別維持率をStudioで見てから、**章構造を壊さない範囲**(4章のまま拍にマーカーを足す)で検討する。
2. **D5 長尺とショートの同日共食い**。間隔ルールがショート同士の話であり、横断適用の根拠が無い。
   **加えて社内に「4〜6時間」と「30分」の2本の矛盾したルールがある。**実測で1本に決める。
3. **D7 終了画面の長さ(10秒 vs 15秒)と要素の置き場所**。10-ending.md も同じ結論に達している。
4. **D8 焼き込み字幕とCCの二重表示**。実機でCCをONにして見るまで判断しない。
5. **D4 タグの効果量**。「タグの寄与は小さい」という記憶はあるが一次資料を確認できず、行動を変えない。

## 出典

このセッションで新規に取得した外部出典は**0件**である(WebSearchの枠切れ+全ドメインのegressブロック)。
以下は本調査でこの環境から**自分で測った一次データ**と、過去ループが取得済みのURLの引き写しである。

**自前測定(確度A・再現可能)**
- `videos/L001-nisa-son/output/L001.mp4` — `ffprobe` で 400.704秒
- `uploads/L001.srt` — 113ブロック / 最長37字 / 平均22.7字 / 終端 6:40.654
- `videos/L001-nisa-son/render.py` — 図側 `40,630` **12**・`81,260` 2・`20,315` 2 /
  字幕側 `40630` **14**・`20315` 4・`81260` 3・`9370` 1・`8126` 1
  (`[検証]` で数え直し。前版の「9 / 15」は `grep -c` の行数で、うち1行は図のデータ用整数リテラル)
- `production/make_upload_kit.py` — `long_kit()` に固定コメント節なし / Lループに `assert_tags_match` なし / `L_LEAD` に L002 なし
- `production/shortlib.py` — `Unit.tts_text()` はカンマを処理しない
- 395秒地点の実フレーム(章チップ・免責バッジ・ヒーローカード・字幕帯・立ち絵2体で画面が埋まっている)
  `[検証]` 検証側でも再抽出して追認。立ち絵は「左下・右下」ではなく左端・右端。
- `videos/L001-nisa-son/render.py` — 末尾で `chara="none"` を持つのは `matome*` まで。
  `shime` / `tsugi` の4ユニット(387.2〜400.7秒)にだけ立ち絵が出る
- `production/make_upload_kit.py` — `SCHEDULE` に S024・S025・S026 のキーが無い /
  `L_SCHEDULE` 直下のコメントが「30分以上離す」と書いており posting-plan §1「4〜6時間以上」と矛盾
- `uploads/L002_投稿キット.txt` — 概要欄の結論2行が空 + `(未生成。render.py を走らせる)` が本文に混入
- `production/fmt.py` — **存在しない**(figure.md が前提にしている)

**自チャンネルの実測(確度A〜B)**
- `docs/research/demand-2026-08.md` — 14本の平均視聴率 / 検索語別視聴率 / 年齢2山・男性87%
- `docs/research/hold-rate-2026-08.md` — Stayed to watch 平均28.9%

**過去ループが取得済みの外部URL(今回は再確認できていない。確度は1段下げて扱う)**
- YouTube公式: Shorts投稿スケジュール https://support.google.com/youtube/answer/13616979
- YouTube公式: Shortsの説明欄リンク無効化 https://support.google.com/youtube/answer/13748639
- YouTube公式: ハッシュタグ https://support.google.com/youtube/answer/6390658
- Buffer 180万本分析(投稿時刻) https://buffer.com/resources/best-time-to-post-on-youtube/
- vidIQ 508万ch分析(投稿頻度) https://vidiq.com/blog/post/How-Often-Post-on-Youtube/
- SEJ: YouTube成長チームの俗説否定 https://www.searchenginejournal.com/youtube-algorithm-myths-debunked-insights-from-the-growth-team/510091/
- 終了画面(最後の5〜20秒・10〜15秒が目安・名前を言う) — `docs/research/longform-loop-2026-08/10-ending.md` の出典欄
- タイトル28〜30字・概要欄の冒頭2行 — `docs/research/element-taxonomy-2026-08.md` の出典欄

---

## 検証(反証側)の記録 — 2026-08-21

**調査員の「限界」の申告は、検証側でも独立に再現できた。**

- WebSearch: 検証側でも投げたところ **「this session has used its web search budget (200 of 200)」**
  が返った。**枠切れは事実。**検証側も新規検索は0件である。
- WebFetch: `support.google.com` に対して **EGRESS_BLOCKED** を再現した。
  **本書の外部主張はすべて本文未読のままである。**

したがって **D の8項目のうち、外部の定石を根拠に行動を変えてよいものは1つも無い。**
検証で入れた指摘は、すべて **(a) このリポジトリのコード・生成物・mp4 との照合**か、
**(b) 本ループの他の要素ドキュメント(特に `figure.md`)との突き合わせ**で取ったものである。

**検証で見つかった主な誤り**

| 種類 | 箇所 | 内容 |
|---|---|---|
| **決定同士が非両立** | D2 と D7 | 同じ1つの不整合(#111が概要欄を指すのに概要欄にリンクが無い)に、**互いを打ち消す2つの修正**を並べて両方「変更」にしていた |
| **他ドキュメントと逆の決定** | D8 | `figure.md` A5-7 が同じ不一致に **`4万630円` への統一**を決めているのに、D8 は **`40,630円` への統一**を決めていた |
| **前提が事実と逆** | D8 | 「チャンネル自身が既に桁区切りを標準にしている」→ `figure.md` の実測では**図の中だけで3流儀が混在** |
| **節の中での自己矛盾** | D3 | 案2を「CLAUDE.md違反だから採らない」と退けた直後に、**より重い違反の案3**を採用していた |
| **出典の誤り** | D3 | チャプター仕様(3個以上・各10秒以上)は `10-ending.md` に**書かれていない**。実際は `elements/script.md`、しかも**公式本文は fetch 不可**と明記 → 確度A→B |
| **ルールの適用範囲の外挿** | D1 | 28字は **Shorts用チェックリスト**由来。`element-taxonomy` は 28〜30字で、現行30字は適合。長尺の字数規定は存在しない → 確度A→C |
| **確度ラベルの過大** | D6 | 「締めの問い」の効果を実測Bとしたが、参照元が「**効果はこれから測る**」と書いている → C |
| **実行不能な具体値** | D5 | S021→8/23 8:30 は **S022 が既に占有**。玉突き先の 8/24 8:30 も S027 が占有。SCHEDULE に **S024〜S026 が欠番** |
| **数え間違い** | D8 | `grep -c` の**行数**を「箇所」と書いていた(9/15 → 実際は 12/14) |
| **範囲の誤り** | D7 | 「末尾15秒の立ち絵を消す」→ `matome*` には既に `chara="none"` があり、対象は **`shime`/`tsugi` の4ユニット(387.2秒〜)** |
| **字数の誤り** | D1 | 案3「NISAで損した年、税金はいくら変わる?」は18字ではなく **20字** |
| **見落とし** | D5 | `make_upload_kit.py` 自身のコメントが **「30分以上離す」**と書いており、社内ルールが2本に割れている |
| **見落とし** | D2 | 概要欄へのURL追記は `posting-plan.md` §7 **「説明欄にURL誘導を置かない」に抵触**。本書は strategy §6(コメント欄の話)を引いて「抵触なし」としていた |

**検証で追認できた(=そのまま使ってよい)もの**

- `long_kit()` が固定コメントを出力していない(D6)— コードと生成物の両方で確認
- 長尺ループが `assert_tags_match` / `assert_no_bare_ryakugo` を呼んでいない(D4)— `main()` で確認
- `L_LEAD` に L002 が無く、L002 のキットの結論2行が空(D2)— 生成物で確認
- 8/22 の 18:00 と 19:30 の間隔が1.5時間(D5)— `L_SCHEDULE` / `SCHEDULE` で確認
- SRT に「S017」「次の動画」「終了画面」が1度も出ない(D7)— 全文 grep で0件
- SRT 113ブロック / 終端 6:40.654 に対し mp4 は 400.704秒、ズレ0.05秒(D8)— ffprobe と突き合わせ
- 実測値の引用(nisa1800万円 129.1% / nisa 結果 105.0% / nisa 59.5% / 新nisa 40.2%、
  【】ではなく前の問いが効いている、Stayed to watch 28.9%)— demand / hold-rate の該当行で確認

確認日 2026-08-21 / 検証日 2026-08-21
