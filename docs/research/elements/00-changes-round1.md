# 要素別Deep Research 第1ラウンド — 確定した変更 117件(実装計画)

検証員(敵対的レビュー)を通過したものだけを載せる。却下102件は各 elements/*.md の本文に理由つきで残した。
確度A=自前測定で再現済み / B=検索スニペット独立3件以上 / C=それ未満(**Cは実装せずA/Bテスト送り**)。


## 確度A(自前測定で再現済み。実装する)

### [audio] B2-1
- **変更**: assemble() の volume=0.15 → volume=0.50(B2-2 とセット)
- **根拠**: 測定を独立に再現した。BGM生 mean=-25.2dB、volume=0.15 適用後 -41.7dB、現行ダッキング後 mean=-57.3dB/peak=-29.7dB、声(mean -15.0dB)との差 -42.3dB。ffmpeg で同じフィルタ列を組み直して -57.3/-29.7 が一致。声比-42dB は事実上無音で、synth_bgm を回す処理そのものが無駄という論証は外部ソースに一切依存しない(確度A)。0.50 を選ぶ算術も確認: 0.30 では発話中 -29.4dB、0.40 でも -26.9dB で狙いの帯(-20〜-25dB)を外し、帯に入る最小値が 0.50。さらに変更後の最終ミックスを通しで焼いて I=-14.2/TP=-1.0/LRA=2.4 と現行同一、BGMの1〜4kHz成分はナレーションより29.6dB低いままであることを新たに測り、明瞭度を削らないことを確認した

### [audio] B2-2
- **変更**: sidechaincompress を threshold=0.015→0.03 / ratio=6→2 / attack=8→20 / release=300→400
- **根拠**: ffmpeg -h filter=sidechaincompress を再実行し既定値(threshold 0.125 / ratio 2 / attack 20 / release 250 / makeup 1 / knee 2.82843 / detection rms / mix 1)を確認。実測ダック量も再現した: 現行 15.6dB(-41.7→-57.3)、変更後 8.7dB(-31.2→-39.9)、volume だけ 0.50 にした場合は -46.8dB で声比 -31.8dB。決定の根拠は「引用された2つの帯(音楽3〜8dB / 放送10〜15dB)のどちらにも現行15.6dBが入っていない」という点にあり、これは外部ソースの真偽に依存しない。ただし目標値8〜10dBの導出は相反ソースの中点であって独立一致ではないので確度は C+ に下げた。makeup=1 は ffmpeg 既定と同値で冗長

### [audio] B2-4a
- **変更**: BGM_VARIANTS の各進行を4和音→8和音、かつ shortlib.py:412 の chords[bi % 4] を chords[bi % len(chords)] に
- **根拠**: L001 は out_name='L001.mp4' → seed 540 → variant 0 = 86BPM を確認。bar=2.7907秒、4小節=11.16秒、400.704秒で35.9回の反復、変化は pad>=0.5 の5箇所・計3.0秒(全尺の0.75%)を render.py のパースで再現した。ただし bi % 4 がハードコードなので8和音化だけでは黙って効かない。この2行はセットで必須。反復が多すぎるという現状認識は確度A、「変えると維持率が上がる」は確度Cのまま

### [audio] B3-3
- **変更**: synth_se_track(shortlib.py:509-513)の『peak>1.0 のときだけ全体を正規化』を np.clip かソフトリミッタに
- **根拠**: コードを読んで確認した。全トラックに掛かるスカラー除算なので1箇所SEが重なるだけで動画全体のSE音量が台本次第の量だけ下がる。B3-3 で測った声比(pop -17.4 / don -12.3 / impact -12.1 / puchun -11.2、いずれも単体合成して再現)が保証されなくなる。外部ソースを一切必要とせずに実施できる唯一の修正。現行の全動画は間隔22秒で未発生なので既存動画の再レンダリングは不要

### [audio] B2-3
- **変更**: 曲調は維持(歌詞なし・80〜92BPM・低倍音パッド)
- **根拠**: 変更なしの決定なので低い確度で行動を変えることにならない。加えて『パッドがナレーション帯域を空けている』を新たに帯域測定で確認した(bgm 1〜4kHz mean -49.7dB vs narration -20.1dB = 29.6dB差)。コード読解の推測だったものが確度Aの実測になり、B2-1 で音量を上げる判断の裏付けにもなる

### [bg] A1-1-fg
- **変更**: GRID #2c2c2a→#454440(パネル面比 1.06→1.35)/ BASELINE #383835→#54534e(1.12→1.71)/ MUTED #898781→#9a9891(3.66→4.55)。背景の明度そのものは維持
- **根拠**: 根拠に使われた「旧背景 #21201d」は捏造だったが、実SURFACE #1a1a19 で再計算しても結論は変わらない。MUTED は 4.85→3.66 で shortlib.py 行47 のコメントが自ら課した4:1を割っており、GRID は 1.06 でほぼ不可視。コード実読と自前のWCAG相対輝度実装で再現済み。MUTED の提案値だけ #999790 が 4.4976 で目標4.5を割るため #9a9891 に訂正した

### [bg] A1-6-band-order
- **変更**: 章チップの割り当て順を 黄#fab219→紫#c39bff→赤#ff7a6b→青#5aa9ff→緑#3ecf8e に変更。順序は videos/*/render.py に散らさず shortlib.py の BAND_COLORS に集約
- **根拠**: Viénot 1999 を独立実装して完全再現。現行は P型で第2章紫と第3章青が距離0.0161(P型シミュレーションで #a0a0ff と #a2a2ff)で判別不能。提案順は0.7453、かつ全120順列の総当たりで1位。色を1つも変えず既存ACCENTSとの対応も壊さないため実質ノーコスト。6項目中で唯一、申告どおりの精度で検証を通った

### [cover] A8-1-chip-bug
- **変更**: make_thumbnail() の先頭で S.CURRENT_BAND = None を明示代入し、サムネへの章チップ焼き込みを止める
- **根拠**: thumbnail.png 左上(30,30)-(420,110)の最頻色は (62,207,142)=#3ecf8e(第4章の色)、動画の第0フレームは (248,176,23)=#fab219(今日の問い)。CURRENT_BAND は shortlib.py 658/712/1157行のモジュールグローバルで、make_thumbnail()(render.py 498行)は render_video() のあと(525行)に走るため最後のユニットの値が残る。実装とレンダ済み画像の両方から確認

### [cover] A9-3-note-color
- **変更**: cover() の注記の色を MUTED #898781(対背景4.85:1)→ INK_2 #c3c2b7(9.72:1)
- **根拠**: WCAG 2.1 相対輝度式で自前再計算。8色のうち MUTED だけが4.85でぎりぎり。30ptの小さい文字なので上げる側でよい。外部ソースに依存せず自前で閉じている

### [cover] A9-1-zunda-rect
- **変更**: ずんだもんを [0.601, 0.010, 0.384, 0.780] に置き、flip=False にする(検証で幅を0.370→0.384に修正)
- **根拠**: 幅は素材 800x915=0.8743 に rect 比を合わせた値(draw_zunda は aspect="auto"、zunda.py 49行)。flip=False は shortlib.py 1084行のコメント「素材は左向きなので、左下配置では反転して画面内側(右)を向かせる」から、右配置なら反転を外すのが正しいと確認

### [cover] A9-1-text-band
- **変更**: サムネの文字を x 0.035〜0.590 に閉じ込め、ha="left" にして立ち絵と1pxも重ねない
- **根拠**: thumbnail.png の #fab219 画素の x 範囲は 204〜1716px(0.106〜0.894)で、ずんだもん(x<0.305)の顎とめたん(x>0.727)の頭部を実際に横切っていることをピクセルで確認。帯の右端0.590と立ち絵左端0.601の隙間は21px

### [emphasis] A7-2
- **変更**: 章札の直前4ユニットに pad=0.5 を追加(止め 6→10箇所)。秒数0.6は変えない。LONG_STOP_PAD=0.5 も変えない。check_long.py のゲートを「pad>=0.5 が3箇所以上」→「6箇所以上、かつ章札の直前4箇所すべてに pad>=0.5」に強化。ただし総尺への影響は +2.0秒ではなく **+1.4秒**(検証員が訂正)
- **根拠**: 外部根拠に一切依存しない。longform-design.md の止めの表(110-127行)に「章札の直前 0.5s」と明記されているのに、L001 の章札4ユニット(#12/#43/#65/#82)の直前 pad は4つとも既定0.15のまま — パーサで全113ユニットを走査して確認した。設計書に書いてあることを実装していないだけで、議論の余地がない。check_long.py:156 の現行ゲートが「3箇所以上」と緩いために素通りしていたことも確認

### [emphasis] A7-3-1
- **変更**: SEの総数を 18箇所 → 10箇所以下(113ユニットの9%=10.2箇所)に絞る。残す優先順位は「冒頭の答え/各章の出口の判定/章札/締め」。impact.mp3 は4箇所→2箇所以下。check_long.py に「SEを持つユニットが全体の9%を超えたら不合格」のゲートを追加
- **根拠**: longform-design.md:176 が「SEを増やさない(9%が上限の目安)」と書いているのに L001 は pop 9 + don 5 + impact 4 = 18箇所 = 15.9%(grep で確定)。自リポのルール違反という内部事実だけで成立する。check_long.py には現状SEに関するゲートが1行も無いことも確認(grep 済み)。ただし「9%」という数字自体の出どころが longform-design.md に書かれていないのは研究員の指摘どおりで、そこはC

### [emphasis] A6-5-2
- **変更**: checklist() の非点灯行の枠線(edgecolor=BASELINE, linewidth=1.6)を廃止し、囲みを table と同じ「塗り」に統一する。実装は非点灯行で Rectangle を描かない(edgecolor="none" だけだと不可視 artist が毎フレーム5〜8個積まれる)
- **根拠**: 外部根拠(富士フイルム/伝わるデザイン)は本文未読だが、この変更は外部根拠を必要としない。scenes_long.py:366 の table は highlight 行を facecolor=EMPH/edgecolor="none"/alpha=0.14 の**塗り**で描き、:332 の checklist は全行に edgecolor 付き Rectangle を描いている = **自リポ内部の作法の不統一**がコードで直接確認できる。画面ごとに囲みの見た目が変わる理由が無い

### [emphasis] A7-4
- **変更**: chapter() に color 引数を足し、head「第N章 / 全4章」の色を EMPH 固定 → 渡された章色にする。render.py は BANDS の色をそのまま渡す。章の色数は5色のまま維持
- **根拠**: 実フレームとコードで「左上チップは章色(#ff7a6b 等)、中央の第N章は EMPH #fab219 固定」という理屈のない不統一を確認(shortlib.py:711 と scenes_long.py:67)。さらに検証員が WCAG 相対輝度で5色すべてを SURFACE=#1a1a19 に対して計算し、6.84〜9.49:1 で40pt なら AAA(4.5:1)を全色が超えることを確認 — 研究員は subtitle.md の「帯の地色に対する実測」を引いていたが参照先が違ったので、実測で裏を取り直した。色覚多様性はチップが章題テキストを含むため二重符号化済みで問題なし

### [figure] A5-1-3種以上撤回
- **変更**: 「図の型3種以上でマンネリ回避(実測A)」という前版の判定を撤回する
- **根拠**: 出典が見つかっていないうえ、自チャンネルの実測でも L001=6種 / L002=5種 と型数が違うのに維持率の比較データが無い。research-standards.md の「確度Cで行動を変えない」に照らして撤回が正しい

### [figure] A5-2-ghost棒
- **変更**: 文脈の棒をベタ塗りから「面だけ RGBA α0.30 + 不透明枠 linewidth 2.2」に変える。ただし alpha= 引数は渡さない
- **根拠**: 実装値から独立に再計算して、EMPH/MUTED_BAR が5系統中4系統で 3:1 未満(2.29〜2.92)、GOLD/MUTED_BAR は最良でも 1.90 であることを確認した。ゴースト化すれば枠 3.31:1(対背景)/主役の面 対 薄い面 4.59〜6.33:1 で5系統すべて通る。前版の書き方(Patch の alpha=0.30)だと枠も30%になり 1.37:1 で消えるため、書き方を修正した上で採用

### [figure] A5-2-MUTED_BAR明度
- **変更**: MUTED_BAR を #67655f → #6e6c65(背景比 2.99 → 3.31)。ただし A5-2-ghost棒 と同時にのみ適用
- **根拠**: 背景比は直るが、単独では default の EMPH/MUTED_BAR が 3.18→2.86 に落ちて唯一通っていた系統が落ちる。ゴースト化と抱き合わせなら害が無く枠の視認性が上がるため、条件付きで承認

### [figure] A5-2-compare2主役色
- **変更**: compare2 の呼び出し側が渡している GOLD を EMPH に替える
- **根拠**: GOLD/MUTED_BAR は 1.15〜1.90 で全系統が 3:1 に届かない。EMPH ならゴースト面に対して 4.59〜6.33。band には適用してはいけない(part と rest が同色になる)ので対象を compare2 に限定

### [figure] A5-2-band作り直し
- **変更**: band は part=EMPH(ベタ)/ rest=MUTED_BAR のゴーストに置き換える。GOLD→EMPH の単純置換はしない
- **根拠**: band は part=GOLD・rest=EMPH で隣り合っており EMPH/GOLD が 1.67〜2.06 と全系統で基準割れ(前版が見落とし)。単純置換すると2区画が同色になる。差し替え案なら part 対 rest が 3.35〜4.91、part 上の SURFACE 文字も 3.23→6.29 以上に上がり、2つの基準割れが同時に直る

### [figure] A5-2-highlight必須化
- **変更**: barsN(highlight=...) を必須引数にする
- **根拠**: コードで確認: highlight=None のとき全棒が EMPH になる。L001 の uchiwake が実際にその状態で、合図が1点に絞れていない

### [figure] A5-3-10pxWARN
- **変更**: barsN の最小の棒の描画高が 10px 未満なら WARN(高さの下限で誤魔化さない)
- **根拠**: L001 uchiwake の 0.315% の棒が 0.430×0.315/17×1080 = 8.6px であることを再計算して一致。自リポジトリの実測から作った基準で、量では通せない

### [figure] A5-3-compare2バグ修正
- **変更**: compare2 の棒位置を bx = x + 0.150*(k-(m-1)/2) に直し、棒幅を min(0.110, 0.150*0.62) にする
- **根拠**: 現行式は m=2 でしか正しくない。m=3 では棒間隔 0.075 に対し棒幅 0.110 固定で 0.035 重なり、左端の棒(x-0.205)が列の枠(x-0.165)からはみ出し、群の中心も x-0.075 にずれる。L001/L002 は m=2 のみで未露見だが、A5-1 で型を増やす前に直す必要がある(検証員の追加)

### [figure] A5-5-SE運用の明文化
- **変更**: SE は章の出口の判定ユニットと答えの数字の着地にだけ付ける(1本あたり4〜8回)。「anim+SE同期」という前版の現状記述を訂正
- **根拠**: 図ユニットの SE 付きが L001 11/71、L002 1/53、anim>0 が 39%/32% であることを数え直して一致。前版の記述は実態と違う

### [figure] A5-6-scene_kinds修正
- **変更**: scene_kinds() を __qualname__ ベースに直す
- **根拠**: 提案コードを実際に L001/L002 に当てて確認。L002 の be5/be10/be15/be20/be25 の5キー・10ユニットが ? → barsN に直り、図の割合が 53/155(34%)→ 63/155(41%)になる。L001 は完全一致で影響なし。scenes_common も同じクロージャ形で副作用なし

### [layout] A2-1-3
- **変更**: BADGE_XY (0.972, 0.940) → (0.950, 0.930)
- **根拠**: 実測でバッジ文字は x〜0.9720 / y 0.9258-0.9542(bbox pad 込みで右端 約0.978)。右端2.2%・上端4.6% は自分たちで宣言した SAFE 0.05 の外。移動後は右端5.0%・文字上端 約0.944 で 0.95 の内側に入る。get_window_extent で再現確認済み。

### [layout] A2-1-4
- **変更**: 章チップのアンカー (0.028, 0.945) → (0.050, 0.930)
- **根拠**: 実測でチップ文字は x 0.0280-0.1379 / y 0.9308-0.9592。上端 0.9592 は 0.95 の外。移動後は文字 y 約0.9158-0.9442 で内側に入る。shortlib.py:715 の fig.text(0.028, 0.945) をコードで確認。

### [layout] A2-1-6a
- **変更**: CHARA_RECTS["bl"] [0.010,0.200,0.192,0.391] → [0.030,0.200,0.192,0.391](bl のみ。br は据え置き)
- **根拠**: 実測でずんだもんの可視域は x 0.0115-0.1807。移動後は 0.0318-0.2005 で、左端がアクションセーフ 0.035 相当に入る。右側は PLOT_L=0.055 と重なる範囲が 0.126→0.146 に増えるが、衝突は3シーンのみで局所修正できる範囲(A2-3 参照)。実際に rect を差し替えて可視域を再計測して確認。

### [layout] A2-1-7
- **変更**: scenes_long.TITLE_Y 0.925 → 0.915(検証員が追加)
- **根拠**: A2-1 が新設する「figure の Text は y<0.95」ゲートを、現行の図タイトル17種(実測 y 0.920-0.959)が全部破る。L001 で実際に走らせて 19種の違反を確認(うち17種がタイトル)。これを直さないと提案ゲート自体が導入初日に不合格になる。調査員は見落としていた。

### [layout] A2-3-2
- **変更**: check_overlap.py に左の立ち絵も禁止領域として足す。ただし (a) 判定はシーン名ではなくユニットの chara で行う、(b) 領域は CHARA_RECTS の rect ではなく実際の可視域(bl x 0.0115-0.1807 / br x 0.7958-0.9594)を使う
- **根拠**: 穴の指摘自体は本物。実際に bl を足して L001 に走らせると 51件検出され、そのうち son(0:08 のヒーロー文字「株で20万円の損」)/ shime / kousiki の3件は真の衝突。ただし残る48件は chara="none" のユニットしか使わないシーンの偽陽性(94%)。原因は has_chara = key not in cover_keys という決め打ち。この2点を直さないと、通らないゲートが増えて無視されるだけになる。

### [layout] A2-4-3
- **変更**: 章チップと注記バッジは維持(変更しない)
- **根拠**: 現状維持なので確度に関係なく安全。バッジは打消し表示として戦略§6 のコンプラ要件に紐づいており、外す選択肢は元から無い。

### [layout] A2-4-4
- **変更**: ¥モチーフ7個と罫線12本の本数は変えない(A/B 送り)
- **根拠**: 「減らす根拠が無いから変えない」という判断は research-standards.md の「確度Cで行動を変えない」に忠実。調査員が定石の不在を正直に書いている数少ない箇所で、そのまま支持する。

### [meta] D4
- **変更**: make_upload_kit.py の main() の長尺ループに assert_tags_match(lid, script) と assert_no_bare_ryakugo(lid, title, desc) を追加し、不合格なら SystemExit(1)。タグは12個・79字のまま維持。加えて long_kit() の L_TAGS.get(lid, []) を辞書アクセスに変え、未知IDが COMMON_TAGS 4個だけで黙って通る穴も塞ぐ
- **根拠**: コードを読んで確認。videos/L0* のループは long_kit(d, lid) を呼んで書き出すだけで assert_* も MISMATCH.append も無い。videos/S0* のループにだけ両方がある。S012 のタグが旧テーマ「リボ払い」のまま出た事故がコード内コメントに記録されており、同じ事故が長尺で起きても止まらない。タグ本体の最適値は根拠が無い(枠切れ+egressブロックで一次資料未確認)ので触らない、という判断も妥当

### [meta] D6
- **変更**: long_kit() に pinned = extract(script, "固定コメント(投稿直後に設置)") を足し、■ 固定コメント節(要約 + 締めの問い + 詐欺注意1行)をテンプレに挿入する。ただし CTA は script.md から直接取れないため、desc 末尾行の切り出しか script_meta.json の cta 直読みで実装する
- **根拠**: long_kit() の本文に固定コメントの文字列が無く、uploads/L001_投稿キット.txt にも出ていないことをコードと生成物の両方で確認。videos/L001-nisa-son/script.md:183 には該当節が実在し内容が捨てられている。ショート側 main() は同じ extract() を既に呼んでおり、単純な実装漏れ。詐欺注意1行がショートだけにある運用不整合も、題材が同じ以上外す理由が無い。ただしコメント数への効果は demand-2026-08.md が『次のバッチで効果を測れる』と書いており未測定なので C

### [meta] D2
- **変更**: L_LEAD に L002 のキーを追加し、「結論2行の合計100字以内」の機械チェックを入れる。加えて chapters.txt 未生成時に long_kit() が『(未生成。render.py を走らせる)』を概要欄本文へ埋め込む挙動を SystemExit で落とす。概要欄へのショートURL追記は却下
- **根拠**: uploads/L002_投稿キット.txt を開いて確認。「■ 概要欄」直後が空行2つで、続けてプレースホルダが本文に混ざっている。L_TAGS・L_PLAYLISTS・L_NEXT には L002 があり L_LEAD にだけ無い。URL追記のほうは posting-plan §7『説明欄にURL誘導を置かない』に抵触し、かつ D7(#111 を終了画面の名指しに書き換える)を実施すると根拠が消滅するため両立しない

### [meta] D7
- **変更**: ①SRT #111 を「税金の20.315%だけなら、終了画面の『NISAの損益通算』で1分で見られるのだ。」に差し替えて誘導先を終了画面に統一。②立ち絵を消すのは shime/tsugi の4ユニット(387.2〜400.7秒)。空くのは画面左右のみで、下部全幅は焼き込み字幕帯なので明け渡せない。③終了画面の長さをキットに秒数で確定表記する
- **根拠**: SRT全113行を grep して S017・動画タイトル・「次の動画」がいずれも0件であること、#111 が「概要欄」と言う一方でキットの終了画面欄が「S017」を指していることを確認(名指し欠落・誘導先の食い違いは確度A)。395秒フレームを ffmpeg で再抽出して画面が埋まっていることも追認。ただし範囲は訂正が必要で、render.py の matome/matome2/matome3 には既に chara="none" があり、立ち絵が出るのは shime/tsugi のみ。クリック率への効果は資料に当たれておらず C

### [meta] D5
- **変更**: 投稿時刻は土18:00 + ショート8:30/19:30 のまま動かさない。まず posting-plan §1「4〜6時間以上」と make_upload_kit.py のコメント「30分以上離す」の矛盾を1本に解消する。間隔チェックは落とさず表示のみにし、L001 の実測を見てから値を決める。あわせて SCHEDULE の欠番 S024〜S026 を埋める
- **根拠**: L_SCHEDULE と SCHEDULE の値は確認でき、8/22 は 8:30/18:00/19:30 の3本で 18:00-19:30 は1.5時間。ただし社内ルールが2本あって矛盾しており「違反」と断じられない。さらに間隔ルールはショート同士(Shortsフィードの共食い)の話で長尺への横断適用は外挿。前版の具体値は実行不能(S022 が 8/23 8:30 を占有、玉突き先の 8/24 8:30 も S027 が占有、S024〜S026 は SCHEDULE 欠番で台本ができ次第 KeyError)

### [meta] D8
- **変更**: srt形式は維持。shortlib.Unit.tts_text() に桁区切り除去1行を追加する。字幕と図の表記統一は figure.md の決定(記者ハンドブック式・production/fmt.py)に従い、揃え先は 4万630円 / 8万1260円 / 2万315円 / 8,126円 / 9,370円、年(2026年)はコンマなし。40,630円 に揃える案は却下。二重表示は L001 公開後に実機で CC を ON にして確認するまで動かさない
- **根拠**: 表記が字幕 vs 図で完全に二分していることは確認(出現数は図側 40,630×12 等、字幕側 40630×14 等。前版の 9/15 は grep -c の行数で、うち1件は図のデータ用整数リテラル)。ただし揃え先は本書に決定権が無い。同じループの figure.md A5-7 が同一の不一致を独立に発見し、発話と字面の一致(CLAUDE.md「数字は図の上で言う」)を根拠に反対の結論を出している。本書の前提「チャンネル自身が既に桁区切りを標準にしている」も figure.md の実測(図の中だけで3流儀混在)に反する。同期は良好(mp4 400.704秒 vs SRT終端 400.654秒、差0.05秒)で触らない

### [script] C0-1(検証員が新設)
- **変更**: check_long.durations() の render_signature() 呼び出しに bands を渡す。長尺の秒を推定ではなく実測で判定できるようにする
- **根拠**: render_video は bands を署名に入れるが check_long は bands=None で計算するため、BANDS を使う長尺は署名が永久に不一致。署名3通りを計算して確認済み(bandsあり のみ stored と一致)。結果、check_long は長尺の秒を一度も実測しておらず、常に約5%長い推定で判定している。他の全ゲートの前提を壊しているので最優先

### [script] C0-2(検証員が新設)
- **変更**: SEC_PER_CHAR_LONG(0.17183 s/字)と longform-design.md §4 の較正表を測り直す。L001 実測は 0.16388 s/字
- **根拠**: 推定が実測より4.9%長い。下限側ゲート(章尺≥60秒・話速≥290字/分)にとって危険な向きで、短い章と速い話速を実際より安全に見せる。§4 の表(1.15→311字/分)も平均実効speed 1.214・実測349字/分と6〜11%ずれ、確度Aを名乗れない

### [script] C2-5
- **変更**: 締め4ユニット(実測16.2秒)を章4から切り出し独立ブロック shime にする。章4=100.3秒、概要欄チャプター6行
- **根拠**: longform-design.md §3『締めは独立ブロック。章の中に埋めない』と 03-structure.md が明示的に要求しており実装だけが追いついていない、という内部不整合。外部根拠不要。締め16.2秒はYouTubeのチャプター最小10秒(03-structure.mdに確度Aで記録)を満たす。ただし章4=100.3秒は設計下限まで0.3秒しかなく、初版の『設計値内に収まる』ほどの余裕は無い

### [script] C5-4
- **変更**: 積み上がる数字を最低3段(40,630→81,260→121,890)、同じ図の上で step を進めて出す
- **根拠**: 07-progress.md が自己診断H5への結論として『繰り返しているのが動かない数字だから』と決め、階段の実装を指示していた。実測で121,890円は0回(旧版1回からの後退)、81,260円3回、40,630円14回。scenes_long.barsN に step が実装済みで追加コストほぼゼロ

### [script] C6-1(縮小して採用)
- **変更**: 締めを独立ブロックにする。長さは実測16.2秒を基準にC6-5の参加の問い1ユニットを足して16〜20秒
- **根拠**: 独立化は内部不整合の解消でA。長さは 10-ending.md(終了画面は最後の5〜20秒=公式・確度A、実務目安10〜15秒、終わりの15〜20秒前から締めに入る)に合わせた。初版の30秒は却下

### [script] C6-4
- **変更**: check_hold.py の L 除外(56行目 vdir.name.startswith("L"))を解除し、H2(終盤の新情報)とH3(ループ)を長尺にも適用。H1 は A8-2 と衝突するため長尺では除外を明示
- **根拠**: check_hold の関数をそのまま当てて再現済み: H2=100/113=88%(必要60%以上)、H3の共通内容語={税金}。L001 は既に両方通るので台本の書き直し不要。除外の事実と通過は実測でA。ただし長尺での効果は未実測でC

### [script] C7-2
- **変更**: templates/plan.md に「尺の見込み」欄を作り、6分側/8分側のどちらを狙うかを企画時に宣言させる
- **根拠**: 8分下限を不合格にする案は 01-length.md に記録された失敗そのもの(自分で作ったゲートに自分で内容を薄められた)。無視する案は収益機能を毎回捨てる。判断を制作後ではなく企画時に移すのが両方の失敗を避ける位置

### [subtitle] A3-7
- **変更**: 字幕経路の stroke_fx(color, outline=outline) に fatten=0.0 を明示指定する(draw_rich_line 内。stroke_fx の既定値 2.0 は他の20箇所以上の呼び出し元のため据え置き)
- **根拠**: 自分で別実装により再計測して方向を確認した。40pt「確」でインク被覆 0.502 → 0.403、counter の最大面積 47px → 95px(2倍)。outline 比を 0.10/0.08/0.06/0.04/0.00 と5点で振っても被覆は 0.403〜0.412 でほぼ動かず、黒縁の太さは潰れの原因ではないことも再現。fatten の効き(+0.10)はウェイト 900→700 の効き(+0.05)の2倍で、潰れの主因が fatten であることは確実。ただし調査員の『白場 0個 → 4個』という数値は再現せず、しきい値依存かつ字によって逆転するため、根拠を被覆と counter 面積に置き換えた

### [subtitle] A3-11-gate
- **変更**: check_overlap.py:54 の band_top = S.SUBTITLE_Y + 0.030 を、実装と同じ S.SUBTITLE_Y + S.SUB_LINE_H * 0.80 に直す
- **根拠**: draw_subtitle() の実際の帯上端は 0.2052 なのにゲートは 0.180 として扱っており、27.2px の過小評価。この状態では『字幕帯に重なる図の要素』を検出できず、実際いま帯が立ち絵矩形の下端 5.6px を覆っているのに 0 件で通っている。実装とゲートの食い違いという確度Aの事実で、外部出典を要さない。調査員が A3-11 の手順に書いた『変更後に check_overlap.py を再実行すれば安全』が空振りする原因でもあるので、位置を動かす前にこちらを先に直す必要がある

### [subtitle] A3-14-1
- **変更**: render_video() の anim == 0 側の分岐でも先頭フレームに pop を渡す(持続は現状の値のまま。0.12秒への延長は行わない)
- **根拠**: AST 再走査で完全に再現した。L001 の anim 分布は 0.0 が59本・1.4 が53本・1.6 が1本で、anim=0 の 59/113(52.2%)は pop が一度も発火しない。これは設計判断ではなく実装の欠落で、確度A。ただし持続 0.12秒という値は確度C(調査員も認めている)なので、まず 0.05秒のまま全ユニットに掛けて1本焼き、目視で足りなければ伸ばす順序に縮小した。調査員の手順3(pop を縮小後サイズに掛ける)は却下(下記)

### [subtitle] A3-2-doc
- **変更**: element-taxonomy-2026-08.md A3-2 の『Bold(700)』を『Black(900)』に訂正する
- **根拠**: findfont で再現確認した。setup_fonts() は NotoSansCJK-Black.ttc(900)と NotoSansCJK-Bold.ttc(700)の両方を family 名 'Noto Sans CJK JP' で登録し、FontProperties(family='Noto Sans CJK JP', weight='black') は Black.ttc に解決される。字幕は必ず draw_rich_line(weight='black' 既定)を通るので、実際に出ているのは 900。rcParams['font.weight']='bold' は字幕には効いていない

### [subtitle] A3-9-doc
- **変更**: element-taxonomy-2026-08.md A3-9 の色コード『#f2f0e9』を『#f2f1e9』に訂正する
- **根拠**: shortlib.py:43 の INK は #f2f1e9。実フレーム(t=60s / t=200s)から抜いた字の RGB は (239.5, 238.5, 230.4) で、帯の実測地色に対するコントラストは 16.40:1。調査員の値(239.4, 238.5, 230.4 / 16.3:1)とも一致。純白 18.87:1 との差は実用上ゼロで、維持の判断も支持できる

### [subtitle] A3-3-doc
- **変更**: element-taxonomy-2026-08.md A3-3 の『40pt=画面高4.9%』と shortlib.py の use_landscape() docstring 内『少し大きめの40pt(4.9%)』を、どちらも 5.14% に訂正する
- **根拠**: 40pt × DPI 100 / 72 = 55.56px、1080px に対して 5.144%。調査員は element-taxonomy 側しか挙げていなかったが、同じ誤りが shortlib.py のコメントにもある。純粋な算術で確度A

### [subtitle] A3-5
- **変更**: 維持(2行上限・check_video.py:127 のゲート継続)
- **根拠**: 実装と実測の両方を再現した。check_video.py はフォーマットを判定して WRAP=26(横型)で wrap_plain の行数を数え、3行以上を落とす。L001 実測は 1行94本 / 2行19本 / 3行以上0本で調査員の値と完全一致。ゲートが効いており違反ゼロなので触る理由が無い(定石側の出典は egress 遮断で未検証だが、行動を変えない維持判断なので影響しない)

### [tachie] A4-5
- **変更**: zunda.py BlinkSchedule.__init__ の `while t < 120` を `while t < 1800` にする。間隔(平均3.5秒・幅2.0〜5.0秒)と閉じ0.13秒は維持
- **根拠**: コードを再実行して確認。L001 の実 seed は sum(ord(c) for c in "L001.mp4")=540 で、最後のまばたきは116.51秒。動画は400.704秒(ffprobe実測)なので29.1%地点で止まり、残り70.9%は無まばたき。eyes() は bt>t で break するため上限を伸ばしても1フレームあたりの走査量は増えず(t=400秒地点で約114回)、性能上の懸念もなし

### [tachie] A4-7
- **変更**: gen_zunda_parts.py の troubled と smug の eyes_closed を None → [("!目", "*UU")] にしてパーツ再生成(18→24枚)。happy/surprised は None のまま
- **根拠**: parts/ の実ファイルが18枚・*_closed.png が normal の3枚だけであることを確認。PSDのレイヤーを列挙して ("!目","*UU") の実在も確認済み。ただし効き幅は調査員の言う31%ではなく troubled 17+smug 1=18/113=16%(訂正済み)

### [tachie] A4-10
- **変更**: imshow の scalar alpha をやめ、arr=arr.copy(); arr[...,:3]=(arr[...,:3]*dim) の輝度倍率にする(値は0.72のまま、引数名 alpha→dim)。加えて _IMG_CACHE のキーを name|flip|dim にして減光済み配列もキャッシュする
- **根拠**: matplotlib 3.11.1 で scalar alpha が RGBA のアルファに乗算されることを最小例で実証し、橙背景の上で体内部の画素が [247,134,43] になる(=背景が透ける)ことを独立に再現。図の上で立ち絵が透けるのは定石の有無と無関係に不具合なので、外部ソース(「定石は暗くする」)が未検証でも変更は正当化される。キャッシュ追記は L001 が3230フレームあり毎フレーム 800×915×4 を copy+乗算すると焼き時間が伸びるため

### [tachie] A4-11
- **変更**: draw_speaker_plate の y を 0.619 → **0.252**((SUBTITLE_Y+SUB_LINE_H*0.80+0.300)/2 で算出)に変更し、chara="none" のユニットでも常時表示する。併せて check_overlap の字幕帯上端 0.180 → 0.2052 を直す
- **根拠**: 名札の実測寸法は 0.0995×0.0511(ずんだもん)。調査員案の y=0.213 では下端が0.1875となり字幕帯の実上端0.2052に名札の35%が沈む(調査員自身が A4-2 案4を同じ理由で却下していた)。0.252 なら実効0.2265〜0.2775で帯(上端0.2052)と図(下端0.300)の双方に触れない。移動が必要な理由も実測: shime の見出しと名札が46.8px重なっている

### [tachie] A4-2
- **変更**: ①check_overlap.check_video() の has_chara に u.chara=="none" を反映 → ②zones_for_format() に CHARA_RECTS["bl"] を別矩形で追加 → ③実在の6件(kousiki/kurikoshi2/riyuu2/shime/son/tsugi の見出し)を修正 → ④check_long で bars/curve/table/compare2 は chara="none" 必須(L001は既に適合＝追認)。立ち絵座標は動かさない
- **根拠**: 実際に check_overlap を書き換えて L001 に通した結果に基づく。①を飛ばすと73件中67件が誤検出になり、ゲートは無視されるか立ち絵を無意味に動かす方向に効く。②の和集合実装は x0.010〜0.990 で画面全体が禁止領域になる。③は調査員が「見出し系は現状どおりで良い」と免除していた領域で実在の重なりが全部そこにあった

### [tachie] A4-3
- **変更**: 維持(左だけ反転 flip=(pos=="bl"))。確度を B → A に引き上げ
- **根拠**: 改変可否の一次資料がリポジトリ内にあった。production/assets/zunda/sakamoto_readme.txt(坂本アヒル氏の同梱readme原文)に「公式の規約に準じての商用利用や、改変・加工しての利用も可能です」と明記。左右反転は改変に含まれ、明示的に許可されている。調査員の「見つからなかっただけで明示的に許可されてはいない」という留保は不要だった

### [voice] B1-1-metan-speed
- **変更**: synthesize() に METAN_SPEED_ADJ = 0.87 を追加し、speaker == METAN_SPEAKER のとき speed に掛ける(実効 1.2075 → 1.050)
- **根拠**: L001の113文すべてを両話者・同一設定で再合成した全数実測で ずんだもん415.7秒 / めたん361.8秒 = 0.870。14文の別標本でも0.875(文ごとsd0.015)。話者間の速さの段差が消えることだけを目的とし、維持率への効果は主張しない。調査員の0.90は1標本推定なので0.87に差し替え

### [voice] B1-1-signature-bug
- **変更**: check_long.py:87 を render_signature(units, scenes, out_name=out_name, bands=getattr(mod,"BANDS",None)) にする
- **根拠**: 検証者が両方の署名を計算して原因を特定(bands無し=f2c55af6… / bands有り=1f5ae28b…=signature.txtと一致)。これを直さない限り bands を使う長尺は永久に推定判定で、定数較正は無意味。調査員は署名不一致という症状は書いたが原因も対策も出していなかった

### [voice] B1-2/B1-6-pitch-by-speaker
- **変更**: synthesize() に PITCH_BY_SPEAKER = {3: -0.03, 2: -0.06} を導入(Unit.pitch の明示指定を優先)
- **根拠**: 検証者が別のF0推定器で再現: めたんノーマルは2.74半音差、pitch -0.06 で4.86半音差。演技を変えずに得られる分離としてはスタイル変更(ツンツン4.10半音)より大きい唯一の手段。根拠は自チャンネル実測のみで閉じており、到達不能だった聴覚研究の閾値には依存しない

### [voice] B1-5-truepeak
- **変更**: assemble() の最終ミックスを loudnorm=I=-14:TP=-1.5:LRA=11 に変更
- **根拠**: ffmpeg ebur128 の実測で完成品のトゥルーピークが -0.5 dBFS(検証者が再現)。AAC化後の上振れに対して余裕がほぼゼロという実測を根拠にする。規格値 -1 dBTP 側は一次・二次とも到達できず確度Cなので根拠から外した

### [voice] B1-5-aresample
- **変更**: 両方の loudnorm の直後に aresample=48000 を入れる
- **根拠**: 検証者のffprobe実測で 24kHz素材 → narration/mixed 192kHz → mp4 96kHz AAC の膨張を確認。ffmpegのloudnormは真ピーク制限のため内部で192kHzに上げ、後段にaresampleが無いとそのまま出す。元帯域は12kHzで情報は増えず、中間wavが154MB(本来約19MB)。音質は変わらずファイルが1/4になりYouTube推奨の48kHzに揃う。副作用なし


## 確度B(検索の一致。実装する)

### [cover] A9-4-color-rule
- **変更**: 「4色以内」を捨て、背景/文字/強調の3色+黒縁とし、立ち絵とアンチエイリアスは数えないと定義を明文化する
- **根拠**: thumbnail.png の異なるRGB値は49,519色で完全一致を確認。設計色も7つあり「4色以内」は定義が無いため実物に対して意味を持っていなかった。ACCENTS 5色が対背景6.84〜9.49でAAを通ることも確認。ただし「3色が理想」という外部定石は今回検証できていない

### [cover] A9-2-em-gate
- **変更**: サムネの文字は2ブロックまで。判定は字数ではなく advance 幅で行う(数字≤4.9em/165pt、語句≤6.0em/131pt)
- **根拠**: 幾何(帯1066px・em実測・自動縮小の挙動)は自前で確認済みで確度A。ただし「画面高16%以上」という床そのものは BBC 22px 由来で、今回その一次・二次資料に一切到達できていないため全体はB止まり

### [cover] A8-1-thumb-split
- **変更**: scenes_long.thumb(main, word, accent) を新設し、動画用 cover() とサムネを分離する
- **根拠**: 1枚の painter が0.07秒のカバーフレームとサムネイルを兼ねている構造は実装から確認(shortlib.py 1232-1238行、render.py 498-513行)。要件の違う2つを1つで満たしている点は妥当。ただし「サムネを専用工程にする」という定石側の外部根拠は検証できていない

### [cover] A8-3-thresholds
- **変更**: 最初の数値15秒以内・最初の図20秒以内は維持する
- **根拠**: 実測3.0s/9.0sで両方を大きく下回っており動かす理由が無い。閾値自体の外部根拠が無いことは調査員も認めており、その記述は正しい

### [emphasis] A7-1
- **変更**: (0) BANDS のラベルと chapter() の title を同じ語に揃える(2章・3章が別名になっている。検証員が追加) (1) 章札4ユニットに chara="none" (2) 章札の直前ユニットに pad=0.5(A7-2 と同一の変更) (3) 章札ユニットでは章チップを描かない — 実装は render_video の CURRENT_BAND=None の一択(painter 内のフラグは描画順で間に合わない)。章題・章番号の位置とサイズ(0.680/40pt、0.500/92pt)は変えない
- **根拠**: (1)(2)は longform-design.md §6(図が主役なら chara="none"、視線計測=確度A)と§止め(章札の直前0.5s)が既に決めていることの未実装。(3)は chapter() の docstring 自身が Mayer の冗長性を引いており、同一画面に「第N章」を3箇所(チップ・中央・字幕「では第N章。…」)出すのは自分の理屈に反する。(0)は検証員が新たに見つけた不統一。章札を廃止する案③は外部の title card 論が単一・未読ソースなので採らない判断も妥当

### [emphasis] A6-4 / A6-3
- **変更**: どちらも維持(不採用)。A6-4 は案②③④⑤とも新規には入れない。既存の立ち絵強調ジャンプ(L001 で11回中9回が chara="none" で無効)も現状のまま放置する — 直すと「図が主役なら立ち絵を消す」(確度A)を破るため
- **根拠**: 「入れない」判断の根拠が longform-design.md §7「足さずに抜く」と Moreno & Mayer 2000 の一貫性の原則(自リポが確度Aで引いている)という内部方針であり、外部の未読ソースに依存していない。A6-3 の案②(全編 zoompan)を却下する技術的根拠も検証済み: CHARA_RECTS["br"] = [0.798, 0.200, 0.192, 0.391] で立ち絵の右端が 0.990、中心から拡大すると画面外に出る。A6-4 の振動が実装困難(figure座標に直描き)という根拠も妥当。ただし A6-4 の「現状」記述は誤りだったので訂正済み

### [figure] A5-1-waterfall
- **変更**: scenes_long.py に waterfall(title, steps) を追加する(steps=[(ラベル,増減値,start/plus/minus/total)])
- **根拠**: FT Visual Vocabulary は github.com から本文を取得でき、Waterfall が part-to-whole(と flow)に、Diverging bar が deviation に載っていることを実際に確認した。加えて自リポジトリの実測として L001 が「利益−損=課税対象」を compare2 24ユニット(全体の21%)で代用しており、1枚で描ける型が無いという欠落は実在する

### [figure] A5-1-checklist除外
- **変更**: FIGURE_KINDS から checklist を外す(描画関数としては残す)
- **根拠**: check_long.py 自身が冒頭で「量を増やせば通るゲートは WARN に落とす」と宣言しており、checklist を図に数えると「箇条書きを4枚めくれば図なし25秒を回避できる」抜け道になる。自リポジトリの check_figure.py も MIN_SHAPES=1 で「図形0+テキスト3以上」を落としており方針として一貫している

### [figure] A5-4-持ち場clamp
- **変更**: barsN の text_fit に max_w=slot*0.95 を渡す(数値・見出しの両方)。slot は A5-3 変更後の値を使う
- **根拠**: text_fit の既定 max_w は 0.92(画面全幅)で持ち場を見ていないことをコードで確認。8文字の金額ラベル(L002 に実在)を6本並べると幾何的に重なる。ただし実写の不具合ではなく合成ケースなので A ではなく B

### [figure] A5-4-数値を見出しより大きくする
- **変更**: barsN の数値ラベルを図の見出し(30pt)より大きくする方向自体は採る
- **根拠**: barsN の数値と _frame の見出しがどちらも 30pt(41.7px)で、字幕 SUB_FS=40(55.6px)より小さいことをコードで確認。longform-design.md の「数字は図の上で言う」と内部で矛盾している。方向は内部原則からの演繹

### [figure] A5-6-同一キー連続WARN
- **変更**: 同じ scene キーが5ユニット以上 かつ その区間の合計秒が25秒を超えたら WARN
- **根拠**: 最長連続が L001=5(uchiwake)、L002=6(sagasu2)であることは実行して一致。ただし uchiwake の5連続は anim=1.4 の1本+anim=0.0 の4本で「同じ図の上で数字を1つずつ言う」意図的な作りであり、一律 WARN は方針と衝突する。秒数条件を足して承認

### [figure] A5-7-記者ハンドブック式に統一
- **変更**: fmt.yen(n, round_to=None) を新設し、1万円未満は3桁コンマ、1万円以上は万を漢字にして以下を詰める、率は算用数字+%、年はコンマなしに統一する
- **根拠**: 1本の動画の中で3流儀が混在していること(band 1枚の中に 20万円 / 15万9370円 / 40,630円)、字幕 20315円 と図 20,315円 の不一致を grep で全数確認。C を選ぶ根拠が外部の定石ではなく内部原則(数字は図の上で言う=字面と発話が一致すべき)であることも前版が正しく書けている

### [layout] A2-1-1
- **変更**: SAFE_T=0.95 / SAFE_TEXT_B=0.10 を新設し、check_overlap.py に「図の中の Text が x 0.05-0.95 / y 0.10-0.95 の内側」ゲートを足す(字幕・帯・フッターは除外。TITLE_Y 修正とセット)
- **根拠**: SAFE_L/SAFE_R がどこからも参照されていない死んだ定数であることは grep で確認(shortlib.py:87/120/123 のみ)。ゲート化の方向は正しい。ただし「すべての Text」では字幕2行目(実測下端 0.0553)が落ちるので図の中の文字に限定し、TITLE_Y 0.915 とセットで導入する必要がある。範囲 0.05/0.95 の外部的正当性(SMPTE)は本検証では未確認。

### [layout] A2-2
- **変更**: 素材枠パネル: 塗り alpha 0.030 → 0.075、枠線 alpha 0.28 → 0(廃止)、矩形 (0.030,0.225,0.940,0.680) → (0.040,0.225,0.920,0.685)(SUBTITLE_Y 据え置きに合わせて y を 0.250 から読み替え)
- **根拠**: コントラスト実測を独立に再現できた。1:35 のフレームでパネル外 RGB(34,32,28)・内(41,39,35)→ 1.091:1、alpha 0.075 の理論値 1.248:1 も一致。枠線が 1.09〜1.48:1 でほぼ不可視な装飾であることは事実で、廃止は一貫性の原則から妥当。ただし廃止理由は「立ち絵を横切るから」ではない(訂正3)。0.075 という具体値自体は外部定石が無く C なので A/B 送り。

### [layout] A2-3-1
- **変更**: Z帯を定数で明示。ただし Z_CHARA=1.0(図より前・図の上の文字より後ろ)、Z_FIGTEXT=2.0 に読み替える
- **根拠**: 現状の zorder(背景 -10 / 図 Axes 0 / 立ち絵 Axes 0 / 帯 2.4 / 話者ライン 2.5 / fig.text 既定 3)はコードで確認し、既存ドキュメントの記述が誤っていることも確認。明示化は妥当。ただし調査員の Z_FIGTEXT=1.0 / Z_CHARA=2.0 は決定文「順序そのものは維持」と矛盾し、自分で却下した案3(数値が立ち絵に隠れる)そのもの。現状順序を保つ値に訂正した。

### [script] C3-5
- **変更**: リビール直後のユニットを「ずんだもんの台詞+感嘆詞始まり+face=surprised/troubled」の3点セットにする
- **根拠**: 実測で感嘆詞始まりは41台詞中1本のみ、表情だけが付いており音だけ聞くとリアクションが存在しない。ラジオテスト(longform-design.md §11 / plot-playbook.md W5)との不整合はA。ただし規則本体(duo-skit のリビール直後リアクション)は同文書が確度Cと自己申告している

### [script] C6-2
- **変更**: 次に見るものの名指しに「ショート版」という種別ではなくタイトルの語を最低1つ入れる
- **根拠**: 10-ending.md が『リンクしている動画を指し、名前を言い、視聴者が何を得るのかを説明する』を記録。現状の387.9秒/393.3秒の2文は種別名でしかなく、終了画面のどのカードを指すか音だけで分からずラジオテストに引っかかる。ただし外部本文は未読

### [subtitle] A3-13
- **変更**: 維持(1文=1ユニット・音声と完全同期)
- **根拠**: 実測を再現した。総字数 2,562字 / 尺 400.704秒 = 6.394字/秒、1ユニット平均 22.67字・3.546秒。『1秒4文字』が翻訳字幕(音声を頼れない前提)の基準であり、同じ日本語を同時に読み上げる用途にそのまま当てるのは誤用、という論理は外部出典に依存せず成立する。かつ変更案③は話速を落として8分を13分にするもので、longform-design.md の SPEED_SCALE 1.15 という既決の設計値と衝突する。ただし『6.39字/秒で問題ない』の側は自チャンネルのデータが無く確度C のままで、これは調査員も明記している

### [voice] B1-4-pad-and-stop
- **変更**: Unit.pad の既定 0.15 → 0.22、章の出口の止め 0.6 → 0.8
- **根拠**: 率での正当化(8.8%)はしきい値依存で落ちたが、しきい値を振っても壊れない事実として「0.3秒以上の耳で分かる間が全体の1.1〜3.2%(8〜31箇所)しかない」が残る。pad=0.15 はどう測っても間として聞こえない。ただし変更幅0.22/0.8そのものに根拠は無くA/Bテスト扱い

### [voice] B1-4-pre-pad
- **変更**: Unit.pre_pad を新設し tts_voicevox() の prePhonemeLength に流す(既定0.05、判定文とリビール行のみ0.35)
- **根拠**: 「重要な部分の前の間」がパイプラインに存在しない唯一の間である点は事実。ただし検証者の実測で prePhonemeLength は speedScale で割られて鳴るため、0.35指定でも実際は約0.25秒(+0.246秒)。それでも200msの線は超えるので採用は維持し、台本に0.35秒と書かないよう注記した

### [voice] B1-3-intonation-gate
- **変更**: 章の出口の判定文・リビール行・ヒーロー数字の行に抑揚を型で割り当て、適用率7.1%→15%以上にする。check_long.py に抑揚ゲート(intonation>=1.2 が10%未満、または相異なる値が2種類以下で不合格)を追加
- **根拠**: 適用率7.1%(8/113)とpitch/pause_scaleの上書き0件はAST解析で再現(確度A)。「設定しているつもりで、していない」のうち適用率の側は事実。ピーク値1.45そのものは効き幅の測定が2つの推定器で2倍ずれたため要再測定として据え置き


## 確度C(根拠不足。**実装しない**。A/Bテスト送り)

### [audio] B2-4b
- **変更**: synth_bgm に sections 引数、assemble() に bgm_sections 引数、render_video から bands と unit_secs で配線
- **根拠**: synth_bgm の呼び出し元は assemble()(shortlib.py:564)のみで、render_video は assemble()(:1277)を呼ぶだけ。初稿の『render_video から synth_bgm に渡す』では配線がつながらない。実装コストは低く副作用も無いので入れてよいが、効果(章ごとに編成を変えると維持率が上がる)はソース1本・本文未取得で確度C。焼いた後に bgm.wav が22.3秒周期になっているか波形で確認することを条件にした

### [audio] B3-1
- **変更**: SEの意味対応表(pop=提示 / don=判定 / impact=驚き / puchun=縦型のみ)を docs と templates に明文化
- **根拠**: L001 の使用実績を render.py のパースで確認(pop 9 / don 5 / impact 4 / puchun 0、113ユニット中18)。puchun が長尺で死んでいるのは事実。ただし根拠のEdTech研究1本+日本語ノウハウ記事2本はいずれも本文未確認なので『少数を一貫して』は B→C+ に下げた。それでもコスト0・効かなかった場合の損失も0で、台本工程の判断を減らす効果だけが確実にあるため実施を承認。ただし効果を主張してはいけない

### [audio] B2-5
- **変更**: BGMは自前生成を維持。BGM_OVERRIDE 使用時は配布元・ライセンス名・確認日・URL を記録。ただし先に assets/se/README.md に確認日とURLを追記する
- **根拠**: 現状維持の決定なので確度に依存しない。synth_bgm が numpy 合成のみ・seed 20260812+variant 固定・assets/bgm.wav が不在であることをコードと ls で確認。ただし『SE の README は既にこの形式』は誤りで、実物には確認日とURLが無い。見本を先に直す作業項目を追加した。権利面の根拠(Content ID 誤検知事例・各配布元の規約)は1本も本文確認できていないため B→C+

### [bg] A1-3-rule
- **変更**: 横罫 alpha 0.055→0.10 / lw 0.7→1.6 / 11本→7本、左縦罫 alpha 0.10→0.14 / lw 1.0→2.0。同時に A1-1 の GRID 引き上げを行い、罫線1.14:1 < グリッド1.35:1 の順序を保つ
- **根拠**: 現行が低解像度で著しく弱いことは自前再測で確認(1080p 4.0 → 480p 2.0 → 360p 1.0 LSB)。ただし調査員の「480p +1.2 で不可視」は再現せず実測2.0で、前提は誇張。合成色 #34332d / 背景比1.138:1 は再現した。上げ幅0.10/1.6 自体に外部の定石は無く自チャンネル判断

### [bg] A1-5-drift
- **変更**: ¥モチーフを中央パネル(x 0.030-0.970, y 0.225-0.905)の内側に描かない。7個→5個 / alpha 0.05→0.07 / 速度 0.010,0.006→0.006,0.0036 / DRIFT_FPS 4→2
- **根拠**: コード実読(range(7)/alpha 0.05/速度係数/FancyBboxPatch の矩形/DRIFT_FPS 行657・使用1256,1269)はすべて申告と一致。最も強い論拠は外部文献ではなくリポジトリ内の一貫性で、CLAUDE.md が既に持つ「図が主役のユニットは立ち絵を消す(図への注視を奪うから)」を¥の動きにも適用するだけ。hold-rate-2026-08.md の実測ボトルネックが「7割が最初の1〜2秒で消える」であり背景の動きではない点も追認した。ただし決定の主柱に据えられた Mayer コヒーレンス原理の効果量は外部検証できていない

### [bg] A1-2-grain
- **変更**: 静的グレインを背景に焼き込む(固定シード・輝度ノイズ σ=2.0/255・毎フレーム再生成しない)。ただし具体値と実装方式は再設計が必要
- **根拠**: 「紙目テクスチャは実装されていない」という指摘はコード実読で正しい(_bg_image_rich は放射グラデとビネットのみ。一方 shortlib.py 行651 付近のコメントは『罫線ノート+紙目+ビネット』と書いており、コメントが実装に先行している)。方向は妥当。ただし提案された『グラデはbicubic・グレインはnearest』は interpolation が new_canvas() 側にあるため同一配列では実現できず、工数見積りが誤り。また『静的ならビットレート増がほぼ無い』はIフレームの符号量増を無視しており未検証

### [cover] A9-3-outline
- **変更**: outline_for の10%は維持し、cover() の問い(44pt、INK_2)に stroke_fx(INK_2, outline=4.4, fatten=0) を追加する
- **根拠**: cover() 91行に path_effects の指定が無く、問いと注記だけ縁取りが欠けている点はコードで確認。背景 #1a1a19 に対しコントラスト15.4〜18.5:1 あり細くする理由が無いのも実測。ただし「5〜10%」という定石の外部3本は今回検証できていない

### [cover] A8-3-stall-gate
- **変更**: check_long.py に「開始60秒までは同一 scene の連続10.0秒以内」を追加する(131〜132行の直後)。ただし L001 の son は実測8.9秒/推定9.6秒で通るため台本の分割は不要
- **根拠**: 方向としては妥当だが、10.0秒という値の外部根拠(AIR Media-Tech / Gyre)は単一ソースかつ今回未検証。現行の長尺1本を1件も落とさないので、実効は「冒頭の書き足しの上限」にとどまる

### [cover] A8-2-face-split
- **変更**: 動画本編は答えを15秒以内に出す(維持)、サムネイルは数値は出すが回避法・条件・結論の向きは書かない(語句は「NISAは払う」)
- **根拠**: 調査員自身が「実証は見つからず、これは定石ではなく仮説」と書いており、その自己申告は正しい。日本語実務ブログは相互引用の域を出ず、今回はそのブログ自体にも到達できていない。A/Bテストの対象

### [cover] A9-1-metan-out
- **変更**: サムネイルから四国めたんを外す(動画本編での使用は変更しない)
- **根拠**: 素材制約は実測で確認(portrait.png 300x500 → CROP_H 290 → h_fr 0.46 で497px = 1.71倍拡大、1280x720配信で331px)。policy.md が音声の規約のみで画像の再配布条件が未確認という但し書きも原文で確認。ただし「顔は1体を大きく」の根拠は vidIQ 単一調査で未検証

### [cover] A9-6-dup-gate
- **変更**: サムネ語句とタイトルで同じ名詞を2つ以上共有させない(janome でゲート化)、検証は YouTube Studio の A/B テストで行う
- **根拠**: 重複回避の定石(stock-sun / aainc)も A/B テストの仕様(最大3つ・PC限定・Shorts不可)も、今回このセッションから一切確認できていない。support.google.com は遮断、二次記事も遮断

### [emphasis] A6-1 / A6-2
- **変更**: A6-1: 【】を長尺で1本5箇所復活、強調は色替え1つのみ、【】と se は結合しない(「【】黄+SE」の記述を撤回)。A6-2: hero_number() を追加し 150pt / EMPH、装飾は実質なし(輝度差と面積差で見せる)、draw_glow_text はショート専用として残す。**ただし両方とも確度Cなので、research-standards.md『確度Cで行動を変えない』に従い L002 の制作時に preview_long.py で目視してから確定させる**
- **根拠**: 「【】黄+SE は事実ではない」(Unit.se と _EMPH_RE は互いを参照していない)、「L001 の【】は0箇所」「draw_glow_text の長尺呼び出しは0件」「3層グローの内訳」はすべてコードで確認でき正しい。方向(0箇所は『絞る』ではなく『無い』/ 3層は多い)も内部観察として妥当。だが本数5・上限8・150pt という具体値に外部根拠が皆無で、「絞る」の定石を支えるとされた4本のブログは検証員も1本も本文に到達できなかった。行動を変えるには足りないので、いま確定させず目視で決める

### [figure] A5-3-角丸を入れない
- **変更**: 角丸は入れない(現状維持)
- **根拠**: 根拠3本はすべて本文未読で確度は C に下げたが、決定が現状維持のため行動は変わらず、research-standards.md の「確度Cで行動を変えない」に抵触しない

### [figure] A5-3-棒幅と中央寄せ
- **変更**: bw = min(0.150, slot*0.62)、n≤3 のとき slot を span*0.62/n にして中央寄せ
- **根拠**: 再計算して gap/bar が n=2 で 213%→61.3%、n=3〜6 も 61% に揃うことを確認。ただし採用する 50〜75% という帯そのものは資料が 20〜100% に割れており選択にすぎない。上限 0.115→0.150 が効くのは n=1 のときだけ

### [figure] A5-5-anim維持とずらし0.20
- **変更**: anim 全体長 1.4〜1.6 秒は維持、barsN のずらし係数を 0.28 → 0.20
- **根拠**: ずらしの計算(1本 41.7%、ずらし 11.7%、6本目の開始 58%)を再計算して一致。0.20 なら 42% になる。ただし幾何の理屈だけで効果の根拠は無く、調査員自身が確度Cとし A/B 送りにしている判断が正しい

### [figure] A5-6-25秒維持と実測A撤回
- **変更**: 閾値 25秒は維持し、前版の「25秒に定石の裏付けがある(実測A)」という判定を撤回する
- **根拠**: 外部5本はすべて実務ブログで数字が 5〜10秒から 60〜90秒まで割れており、今回 egress 遮断で本文も読めていない。動かす根拠が無い以上据え置きが正しく、A の看板を下ろすのも正しい

### [layout] A2-4-2
- **変更**: 図の中の文字要素は3つまでを check_figure.py に WARN として追加
- **根拠**: 低リスクで可逆、既存の TABLE_NUMS 除外規則を流用できる。根拠(1スライド1メッセージ)は日本語実務側の一致だが本検証では未確認なので、決定ではなく WARN 止まりという調査員の設計が妥当。

### [meta] D3
- **変更**: チャプターは5行のまま維持する。L002 を8行にする A/B は立てない。仕様適合の出典は docs/research/elements/script.md:286,327(10-ending.md ではない)、確度は A ではなく B
- **根拠**: 5行はいずれも仕様(3個以上・0:00始まり・昇順・各10秒以上、最短43秒)を満たす。ただし出典が誤っており、10-ending.md を全文 grep しても「3個以上」「各10秒以上」は書かれていない。実際の記載先 script.md は『※公式ヘルプ本文は今回 fetch 不可』と明記しているので本文未読=B が上限。章数の最適値には外部・内部とも根拠が無く、長尺の実測が0本(L001は8/22公開予定)なので Studio の章別維持率を見てから決める

### [meta] D1
- **変更**: タイトルを「NISAの損は損益通算できない?余分に払う40,630円」(28字)に変更する。ただし理由は主題語「損益通算」の回収であって、28字ルール違反ではない
- **根拠**: L_TAGS["L001"] に「損益通算」があり概要欄本文にも出るのに、動画が答える問いそのものの語がタイトルにだけ無い(確度A)。数字を残す判断も実測に合う(demand-2026-08.md で nisa1800万円 129.1% / nisa 結果 105.0% に対し nisa 59.5% / 新nisa 40.2% を該当行で確認)。【】不採用も demand:254-255「効いているのは【】ではなく、その前の問いのほう」で裏付けあり。一方 28字は posting-plan §7 という Shorts 用チェックリスト由来で、element-taxonomy は 28〜30字(現行30字は適合)、longform-design.md に長尺の字数規定は無い。長尺への適用は外挿なので確度は C

### [script] C5-3
- **変更**: 同一金額の反復を全ユニット数の6%(113なら7回)以下とする WARN を check_long に追加(現状 40,630円が14回)
- **根拠**: 07-progress.md(4日前)が『12回をやめる、4〜5回にする』と決めたのに12→14回へ悪化しているのを実測で確認。決定を文章で書くだけでは守られない証明なのでゲート化する。ただし6%という値に外部根拠は無い

### [script] C3-4
- **変更**: ずんだもんの平均字数をめたんの70%以下(22.6字→16字以下)、疑問形+リアクションを29%→60%以上。check_long に WARN で実装
- **根拠**: 実測でめたん22.7字・ずんだもん22.6字とほぼ同一で、『掛け合い』を名乗りながら実体は2人の交互ナレーション。duo-skit が定義した聞き手役が台本レベルで実現されていない(ずれの検出はA)。ただし70%・60%という具体値に外部根拠は無く、実施時はC3-3と同時に設計しないとめたんの連続台詞が伸びて片方が破れる

### [script] C4-3
- **変更**: 同一の文頭接続語の使用回数 ≤ 全ユニット数の10%(113なら11回)を WARN で追加。現状 そして14・だから13 が超過
- **根拠**: 木下是雄『接続語で貼らない』を sakubun-gensoku.md が原則5・確度Aで採用済みなのに、実測で4文に1文が『そして』か『だから』で始まる(文頭語の実測値は検証で全数一致)。現行のスケール方式は長尺ほど甘くなり検出できない。10%という値は外部根拠が無く、実測(そして12.4%・だから11.5%)を1〜2ポイント下回る位置に置いた仮の値

### [script] C2-2(順序を変更して採用)
- **変更**: まず C0-1 を直して実測で判定させ、そのうえで章尺 90秒未満を WARN にする(不合格は60〜150のまま)
- **根拠**: 設計100〜130に対しゲート60〜150で2.5倍ゆるい、という指摘は正しい。実測 ch3=60.2秒で不合格まで0.2秒しかなく、初版の推定63.5秒より深刻。章尺は中身を足せば伸びるので不合格にすると薄める圧力になる(L002 +89ユニットの前科)ためWARNが妥当。90秒という値に外部根拠は無い

### [script] C1-6
- **変更**: longform-design.md の冒頭「0〜40秒」を「0〜45秒」に直し、check_long に「冒頭ブロック ≤45秒」の WARN を追加
- **根拠**: 文書40秒に対し実測43.2秒のずれは事実(A)。40秒という値に外部根拠は無く自リポジトリの設計値で、45〜75秒の離脱の山の手前に収まっている。ずれたまま放置するのが最も悪い。ただし45秒という値自体はC

### [script] C1-3
- **変更**: 最初の図のゲートを20秒以内→12秒以内に締める(実測9.0秒なので台本の変更は不要)
- **根拠**: 20秒という値に外部の根拠は無い自リポジトリの設計値。旧L001は19.9秒(02-opening.md で確認)、新L001は9.0秒で作れており、通ってしまう幅を残す理由が無い。ただし12秒という閾値も外部根拠は無く実績値からの締め上げ

### [script] C1-4
- **変更**: 「25〜35秒に再フックを1つ置く」を型として longform-design.md §2 に明文化する(器=ずんだもんの驚き1台詞)
- **根拠**: L001 は31.7秒に該当台詞があり機能しているが意図して置いたものではない。外部は miraflow/prapermedia のスニペット2本のみで秒数の根拠が示されておらず、実装形を内部で決める分には害が無い程度

### [script] C3-3
- **変更**: めたん連続台詞の上限を3から「章の中は5、冒頭と章の出口の前後は2」に変える
- **根拠**: 実測で連続3が15回と最頻値になっており『上限3』が『常に3』になっている(単調さの検出はA)。sakubun-gensoku.md §0 が警告する『検査工程を作文工程として使う』と同じ形。ただし外部慣行3〜5は単一系列のスニペットで、具体値2・5はA/Bテスト対象

### [script] C4-1
- **変更**: 1文の上限20〜30字は据え置き、聞き手(ずんだもん)にだけ16字以下を課す
- **根拠**: 実測で30字超が2本・20〜30字が75%で上限側は既に守られている(A)。最大37字が既に40字以内なので上限を緩める実利が無い。下限側(短い聞き手台詞)が存在しないほうが問題。16字という値はC3-4と同じくC

### [script] C6-5
- **変更**: 締めの最後に参加の問いを1ユニット置く。4択ではなく自由回答の1文
- **根拠**: demand-2026-08.md 追記3 の S011 コメント0件との突き合わせは単発データ。4択(sc.chips)は縦型ショートのレイアウト部品で横型長尺での効果は未検証。定石も見つかっていない自チャンネルA/Bテスト待ちの項目

### [script] C7-1(確度を下げて採用)
- **変更**: 総尺は内容で決める方針を維持し、check_long に「7分00秒〜8分10秒は WARN」を追加する
- **根拠**: ミッドロール8分は 01-length.md が YouTubeヘルプを一次資料として確度Aで記録(ただし本文は今回fetch不可)。ただし初版の『実測6分59秒でWARN帯まで1秒』という論拠は誤りで、実測6分41秒で19秒の余裕がある。閾値7:00の位置には外部根拠が無いので確度Cの仮線として入れる

### [script] C2-6(縮小して採用)
- **変更**: ゲート化はせず、longform-design.md §5 に「図の更新の1回を章の中央付近(章開始+50〜70秒)に置く」の一文を足すだけにする
- **根拠**: 提案されていた『各章に図の更新を最低2回』は全章が既に3〜8回で一度も発火しない無効な閾値だったため却下。足りないのは回数ではなく配置の指定のほうなので、明文化のみに縮める

### [tachie] A4-12(暫定分のみ)
- **変更**: metan.talk_bob_px を talking の真偽から mouth 段階連動へ(mouth==2→6.0px / 1→3.0px / 0→0.0px)。本命のパーツ分割素材の入手は、ユーザーに配布ページと同梱readmeを確認してもらってから
- **根拠**: 暫定改修は自コード内で完結し権利に触れないので実施可。現状が 6.0*|sin(2π·2t)|(毎秒4回・素材290px基準6px=画面上8.7px)で talking の真偽だけで駆動されることは実測確認済み。一方、分割素材側は seiga.nicovideo.jp / booth.pm / wikiwiki.jp が全て egress 遮断で存在を1件も確認できず、しかも im10791276 の出どころは自リポジトリの README という自己参照だったため C に降格し、research-standards の「確度Cで行動を変えない」に従って入手手順に確認を挟ませた

### [voice] B1-4-pause-scale
- **変更**: Unit.pause_scale の既定 1.1 → 1.25
- **根拠**: pauseLengthScale が 0.25.2 の audio_query に存在し尺を単調に伸ばすことは検証者が engine を直接叩いて再現(確度A)。文の中に間が入る点は境界だけ伸ばす案より自然。ただし1.25という値に根拠は無く、尺への寄与も+1.5%と小さい → A/Bテスト扱い
