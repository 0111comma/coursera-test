# 効果音アセット

- `impact.mp3` — Cinematic Impact Boom(Universfield)。ユーザー提供。
  Pixabay Content License(商用可・クレジット不要)。フリーズ演出のリベール音として使用。
- ファイル名(拡張子除く)が `Unit(se=...)` の種類名になる(shortlib.synth_se_track)。
- 合成SE(pop/don/puchun)はshortlib内で生成。実ファイルが同名で置かれていればそちらが優先…ではなく、
  合成に無い種類のみファイルを探す。
