# ゲートの誤検出を、**理由を書いたうえで**外すための一覧。
#
# 形式:  動画ID:ゲート名:ユニット番号   # 理由(必須)
#
# **理由の無い行は無効**にしてある。黙って外せないようにするため。
# ここに足すのは「ゲートが間違っている」ときだけ。
# 「直すのが面倒」で足すと、赤いのが普通になって誰も見なくなる。

# --- S032(言い回しと論理を直した版・2026-08-23)
# **台本を書き直したら、この節は一度まるごと消してゲートを走らせ直す。**

S032:teinei:5  # 「90歳」と「2人に1人」の2つ。「2人に1人」は割合の1語で、割ると意味が消える

# flow の3件は、どれも**同じ型の転換**。前の文の語では受けられないが、
# 視聴者は問い→答え・恐怖→対処・痛み→対処として自然に追える。
S032:flow:5    # 「生活費はどうしますか?」→「90歳まで生きる女性は2人に1人」。問いに統計で答える
S032:flow:8    # 「貯金はゼロ。年金だけで8年です」→「90歳まで持たせるなら毎月3万3000円」。
               # 恐怖を出したあと、では何をするかへ移る転換
S032:flow:11   # 「暮らしを削ることになります」→「決めるのは金額ではなく何歳まで持たせるか」。
               # 痛みを受けて、では何を変えるかへ移る転換
