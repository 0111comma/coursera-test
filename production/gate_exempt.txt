# ゲートの誤検出を、**理由を書いたうえで**外すための一覧。
#
# 形式:  動画ID:ゲート名:ユニット番号   # 理由(必須)
#
# **理由の無い行は無効**にしてある。黙って外せないようにするため。
# ここに足すのは「ゲートが間違っている」ときだけ。
# 「直すのが面倒」で足すと、赤いのが普通になって誰も見なくなる。

# --- S032(2026-08-23。冒頭をユーザーの33字案に戻した版)
# **台本を書き直したら、この節は一度まるごと消してゲートを走らせ直す。**
# ユニット番号がずれるので、消さずに直すと別の文を免除してしまう。

S032:teinei:5  # 「4人に1人」を 1 と 4 の2個に割って数えている。
               # 割合を表す1語なので、割ると意味が消える。新しい数は「90歳」の1つだけ

S032:flow:5    # 「81歳。思ったより早くないですか?」→「いま90歳まで生きる人は、男性でも4人に1人」。
               # 「81歳は早い」を打ち消す統計。逆接の関係だが、打ち消しているのは
               # **命題どうし**(金は先に尽きる ↔ 命は続く)なので、
               # 表面に否定語が出ない。ゲートは文字の一致しか見られない。
               # 「でも、」を足して明示する案は試したが、字幕が3行になり
               # 1行目が「でも、」だけになって見た目が壊れたので採らなかった
S032:flow:7    # 「90歳までの8年は、貯金ゼロで年金だけ」→「毎月いくら使いたいか。その金額は忘れて」。
               # 恐怖を出したあと、では何を変えるかへ移る**設計上の転換点**。
               # ここで前の語を引き継ぐと、恐怖を引きずったまま解決に入ることになる
S032:flow:9    # 「金額ではなく、何歳まで持たせるかです」→「90歳まで持たせるなら、300か月です」。
               # **「持たせる」を共有している。**ゲートは漢字2字以上の連なりを見るので、
               # 「持」1字の語幹を拾えない(既知の穴。docs/research の11-gate.md)
S032:teinei:9  # 「25年」と「300か月」は**同じ期間の言い換え**。
               # 割ると換算そのものが消える。この文の用件が換算(25年 = 300か月)なので、
               # 新しい数は実質1つ。図の副題「65歳から90歳までの25年」が導出を持つ

# --- 公開済みの動画のカット速度(2026-08-24)
# 全29本が基準(2.4秒)を超えている。中央値3.46秒、競合は1.6〜1.8秒。
# **公開済みは触らない**というユーザーの指示(2026-08-24)により、
# 直さずに免除する。**新しく作るものには適用する。**
# 数字は docs/research/tempo-2026-08-24.md に記録した。
S001:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S002:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S003:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S004:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S005:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S006:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S007:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S008:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S009:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S010:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S011:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S012:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S013:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S014:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S015:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S016:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S017:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S018:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S019:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S020:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S021:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S022:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S023:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S027:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S028:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S029:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S030:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S031:tempo:0  # 公開済み(または公開判断済み)。触らない方針
S032:tempo:0  # 公開済み(または公開判断済み)。触らない方針

# --- S033(2026-08-29 全面書き直し版)
# **台本を書き直したら、この節は一度まるごと消してゲートを走らせ直す。**
# (旧節は2026-08-24の台本のユニット番号だったので、まるごと消して引き直した)

# ■ 問い・式・列挙を2カットに割っている箇所(溜めて出すショートの基本の型)。
#   ゲートは「問いと答え」「式と答え」が繋がっていることを文字の一致では見られない。
S033:flow:2    # 「サブスク3つで、月いくら?」→「NetflixにSpotify、プライム。」。
               # **問いと、その名指しの答え**(3つ=この3社)
S033:flow:4    # 「この3つ、ありますよね?」→「1590円、1080円、492円。」。
               # 直前で名指しした3社の**単価の列挙**(足し算の入力)
S033:flow:5    # 「1590円、1080円、492円。」→「合わせて月3162円です。」。**足し算の入力と出力**
S033:flow:7    # 「月3162円を、1日に直すと。」→「たった105円。」。**割り算の式とその答え**
S033:flow:10   # 「これを35歳から65歳まで。」→「360か月、お金を払い続けると。」。
               # 期間と、その**換算**(30年=360か月)。S032の「25年=300か月」と同じ
S033:flow:11   # 「360か月、お金を払い続けると。」→「出ていくお金は114万円。」。
               # **式の入力(360か月払う)とその出力**。「お金」は共通語だが
               # ゲートは漢字2字の連なりしか拾えない(お+金。既知の穴)
S033:flow:14   # 「その114万円、積んだらどうなるか。」→「積んだ場合、年5%と仮定します。」。
               # **「積んだ」を共有している。**ゲートは漢字2字以上の連なりを見るので
               # 「積」1字の語幹を拾えない(S032:flow:9 と同じ既知の穴)
S033:flow:17   # 「これで、あなたのお金が入ってくる側に。」→「いまのは、3つ全部やめた場合。」。
               # 開示の熱を前提の訂正で受ける**設計上の転換点**(S032:flow:7 と同型)。
               # ここで前の語を引き継ぐと263万円の余韻のまま条件の話に入れない
S033:flow:20   # 「1080円のものを1つ止めれば。」→「止めた約39万円を積めば、約90万円。」。
               # **条件とその結果**。「止め」の語幹1字はゲートが拾えない
S033:flow:21   # 「止めた約39万円を積めば、約90万円。」→「あなたの額は、月額×360か月。」。
               # 例(1080円)から**視聴者自身の式への一般化**(持ち帰りの転換点)

# ■ 1文に数字が複数出るが、**1つの事実の入力と出力**になっている箇所。
S033:teinei:9  # 「これを35歳から65歳まで、30年。」。範囲(35→65歳)と長さ(30年)は
               # **同じ期間の言い換え**。割ると換算そのものが消える(S032の25年=300か月と同じ)
S033:teinei:4  # 「1590円、1080円、492円。」。**3つを並べて足す文**なので、
               # 数字が3つ出るのはこの文の用件そのもの。割ると足し算が消える
S033:teinei:20 # 「止めた約39万円を積めば、約90万円。」。**入力(出した39万)と
               # 出力(積んだ結果90万)**。割ると「止めるだけで90万もらえる」誤読が再発する
